{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1779807612,"nonce":"RsxhVGijrEJiSdn+GvNzWtqbQQw=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABDbjQIw","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/181","origin":"github"},"title":"izzy: migrate legacy segmentation/pointcloud consumers after perception#17 ~/pointcloud rename","message":"## Context\n\n`rolker/unh_marine_perception#17` (merged to `jazzy` in PR #18) renamed the `segments_to_pointcloud` publisher from the hardcoded relative topic `segmentation/pointcloud` to the node-private `~/pointcloud`. For the **legacy map-frame instance** izzy runs (the segmentation→costmap path), the resolved output topic therefore changed:\n\n- **old:** `…/front/oak/segmentation/pointcloud`\n- **new:** `…/front/oak/segments_to_pointcloud/pointcloud`  *(node name `segments_to_pointcloud` + `~/pointcloud`)*\n\nizzy's consumers still point at the **old** name, so as of the perception#17 merge they are silently receiving nothing on `jazzy`.\n\n\u003e **Not on the BizzyBoat June-4 path.** Bizzy doesn't run the legacy instance — Phase A (#178) launches the *reflex* instance and remaps `~/pointcloud → /bizzy/collision_monitor/pointcloud`. This issue is **izzy-only**: izzy's costmap / monitor / rviz feed go empty until migrated.\n\n## Affected consumers (verified)\n\n**This repo (`unh_echoboats_project11`)**\n- `izzyboat_project11/config/izzyboat.yaml:139` — annunciator/monitor source `segmentation_pointcloud`\n- `izzyboat_project11/config/izzyboat.rviz:423` — rviz PointCloud2 display\n\n**`rolker/seafloor_echoboat_project11`** (shared nav2 params; lands via its own PR referencing this issue)\n- `echoboat_project11/config/nav2_params.yaml:158` and `:207` — two costmap layer `topic:` entries\n\n## Fix options (implementer's call)\n\n1. **Update the consumers** to the new `…/segments_to_pointcloud/pointcloud` name (one line each).\n2. **Add a launch `SetRemap`** in izzy's launch so the legacy instance republishes `~/pointcloud → segmentation/pointcloud`, leaving all consumers untouched (mirrors the Phase A remap pattern). Prefer this if other tooling also assumes the old name.\n\nPick one and apply consistently across all four references.\n\n## References\n- Part of rolker/unh_marine_perception#17 (the rename; merged in PR #18)\n- Reflex-layer umbrella: #170 (closed); Phase A: #178; Phase B: rolker/seafloor_echoboat_project11#25\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.7 (1M context)`","files":null}]}