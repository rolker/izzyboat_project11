{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1782300941,"nonce":"PXgwfeWpnkc5lC9f5i/3GvV1qzc=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABGjGGHg","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/324","origin":"github"},"title":"Bridge rqt_boat_state panel topics (state, rc/out, rc/in, cmd_vel, sound_speed) over udp_bridge","message":"## Summary\n\nBridge the topics the new operator **`rqt_boat_state`** panel (rolker/rqt_operator_tools#85) needs from BizzyBoat → operator over `udp_bridge`. Several are already bridged; five are missing.\n\n## Gap\n\n`bizzyboat.yaml`'s `udp_bridge` operator remote (profiles `wifi` / `vpn` / `cell`) already sends `odom`, `marine/heartbeat`, `mavros/battery`, and sidescan `water_temperature` — which the panel uses for heading/SOG, piloting mode, battery, and water temp.\n\n**Missing (add):**\n\n| Source topic | Panel use |\n|---|---|\n| `mavros/state` | FCU mode → command-authority banner |\n| `mavros/rc/out` | actual throttle/steering PWM |\n| `mavros/rc/in` | RC-override commanded overlay |\n| `mavros/setpoint_velocity/cmd_vel` | commanded speed + commanded yaw-rate |\n| `sensors/sound_speed/sound_speed` | sound-speed readout/trend |\n\n**Excluded:** the manual helm (`piloting_mode/manual/helm`) — that originates on the operator side, so it must not be bridged back.\n\n## Plan\n\nAdd all five to **all three** connection profiles (`wifi`, `vpn`, `cell`) at native rate — no throttling (the messages are small; per operator direction 2026-06-24). Follow the existing `topics_list` + `topics: {source: …}` convention with relative sources (resolved under `/bizzy`), mirroring the existing `mavros_*` naming.\n\n## Acceptance\n\n- [ ] The five sources appear in `wifi`, `vpn`, and `cell` `topics_list` + `topics` maps\n- [ ] Config validates / launches without error\n- [ ] Operator-side names line up with the panel's defaults (or note any remap needed)\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.8 (1M context)`","files":null}]}