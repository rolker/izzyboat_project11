{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1777069537,"nonce":"OazrpMojNpXIJaoawZO1sAWkkA8=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABAdhRiA","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/81","origin":"github"},"title":"Field import: unh_echoboats_project11 (2026-04-24) — DIVERGED, merge required","message":"Field import of 6 commits pushed to `gitcloud/jazzy` during the 2026-04-24 pier session. **Diverged** — local `jazzy` is 12 ahead, remote is 6 ahead; fork point is ~24 h ago (merge commit `cbb933f` for PR #46).\n\n## Commits to import\n\n```\n98e7fe5 bizzyboat: remove foxglove bridge and studio from operator tmux script\n56844a5 bizzyboat: add operator camera grid config (3x3, 9 panes)\nb6e6e6f feat(bizzyboat): add record_camera_topics.sh helper\n01695d3 tune(bizzyboat): drop non-forward OAK H.265 bitrate to 800 kbps\n331bfa9 fix(bizzyboat): prefix mavros + sea_surface_estimator TF frames with bizzy/\n45fcf79 docs: add gabby 2026-04-24 field log\n```\n\n## Pre-review\n\n- **`331bfa9` (silent-failure fix, highest interest).** The `sea_surface_estimator` node declares three TF-frame parameters (`sea_surface_frame`, `chart_datum_frame`, `mhhw_frame`) that defaulted to unprefixed names, but `bizzyboat.yaml` only overrode the publisher side (`chart_datum_node`). `lookupTransform` threw on the unprefixed frames and the `catch` returned `false` — tide-range validation was silently never firing. Config-only fix in `bizzyboat.yaml`. Field log has the full symptom trace.\n- **`01695d3` (OAK H.265 bitrate tune).** Per-camera rates: forward stays at 1000 kbps; starboard/port drop to 800; aft was bumped from the staged 500 to 800 after block artifacts appeared at 500 during the session. Totals ~3.4 Mbps across all four cameras.\n- **`56844a5` (operator camera grid config).** 3×3 YAML that pairs with the `rqt_camera_grid` plugin merged this morning ([rqt_operator_tools#22](https://github.com/rolker/rqt_operator_tools/pull/22)).\n- **`45fcf79` (271-line gabby field log).** Includes a useful SIGTTOU diagnosis for `ros2 bag record` under `timeout` — `timeout` places the recorder in a separate process group, `ros2 bag record` enables TTY raw mode for the pause/resume feature, SIGTTOU stops the process, the timeout SIGINT then sits undelivered. Fix: `--disable-keyboard-controls`.\n- **`b6e6e6f` (new `record_camera_topics.sh`).** Follows the existing `test_cmd_vel.sh` pattern (executable bash, not in `CMakeLists.txt install(PROGRAMS ...)`). Idempotent via timestamped output dirs under `~/data/logs/bizzy_images/`.\n- **`98e7fe5` (foxglove removal from tmux script).** Matches the operator pipeline transition to `rqt_camera_grid`.\n\n## Divergence\n\n**This PR cannot fast-forward.** Local `jazzy` has 12 commits the field didn't see (2026-04-22/23 hydro-payload install docs, offsets SVG + generator, SBG validation, 2026-04-23 session log, 2026-04-24 morning operator-side log, pier-session checklist). Gitcloud `jazzy` has the 6 above.\n\nThe branch `feature/issue-\u003cN\u003e` will be created from `gitcloud/jazzy` and carries only the 6 field commits. A human reviewer (or a follow-up worktree) must **merge `jazzy` into `feature/issue-\u003cN\u003e`** before the PR can merge to `jazzy`. No content conflict expected — the local side added new files under docs/ while the field side touched `bizzyboat_project11/config/bizzyboat.yaml` (config-only) and `bizzyboat_project11/launch/oak_cameras_launch.py` (bitrate params).\n\n## Related\n\nReconciled via workspace skill `/import-field-changes` from a connected dev machine. Sibling PRs in the same pier-session batch: [CCOMJHC/ccomjhc_project11](https://github.com/CCOMJHC/ccomjhc_project11) (screenshooter path), [rolker/mru_transform](https://github.com/rolker/mru_transform) (TF timeout deadlock fix).\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.7 (1M context)`","files":null}]}