{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1782515855,"nonce":"NJRSi2wN1NFYmRNgO3Jbgy63hBc=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABG3X3RA","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/335","origin":"github"},"title":"Field import: unh_echoboats_project11 (2026-06-26)","message":"Field changes from the **gabby** field deployment session on **2026-06-26** (Lake Massabesic), pushed to `gitcloud/jazzy`. Importing to GitHub for review per `/import-field-changes`.\n\n## Commits being imported\n\n- `697e098` fix(nav2): disable bathymetry_layer on both costmaps (floods lake lethal)\n- `7c3d349` docs(logs): gabby field log for 2026-06-26 deployment session\n\n`gitcloud/jazzy` is **ahead by 2, not diverged** — clean fast-forward import.\n\n## What changed\n\n**`bizzyboat_project11/config/nav2_overlay.yaml`** — sets `bathymetry_layer.enabled: False` on both `local_costmap` and `global_costmap` (dated, commented). Field mitigation: the bathymetry store was **rebuilt the morning of deploy (09:55)** and, with `unsurveyed_is_lethal: True` + the depth gate, flooded the entire lake **LETHAL**. Operator confirmed disabling it cleared the full-lethal costmap after a stack restart.\n\n**`docs/logs/2026/2026-06-26_gabby_logs.md`** — new per-host field log for the session.\n\n## Pre-review against Quality Standard\n\n- **Clean.** The config change is a documented, dated, reversible param flip (\"re-enable when the prior is vetted\"). No tests apply to a YAML enable/disable toggle. No hardcoded paths/credentials introduced. The log is documentation only.\n- **This is a mitigation, not a root-cause fix.** The real issue is that the bathymetry costmap layer floods unsurveyed/freshly-rebuilt areas LETHAL (`unsurveyed_is_lethal` + depth gate). This is distinct from the #220 tide-frame zero-flood, which the field log explicitly **ruled out** here (`map-\u003emap_tide z=48.78m`, sane). The proper fix is the uncertainty-aware cost model already scoped in the bathymetry-costmap design work — re-enabling the layer should wait on that. Worth tracking as a follow-up rather than blocking this import.\n- **Out of scope for this PR but flagged from the log:** a persistent **FCU 3D-gyro Fail** (survived a power cycle) was the suspected cause of full-throttle-on-manual + nav jumps. That's an FCU/hardware investigation with no code change in this import.\n\n## Diverged?\n\nNo — fast-forward.\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.8 (1M context)`","files":null}]}