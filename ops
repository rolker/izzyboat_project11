{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1777600260,"nonce":"B3VtjL+4XmWZeUyoCxo59O+4hNQ=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABBAQ8kw","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/118","origin":"github"},"title":"Field import: unh_echoboats_project11 (2026-04-30)","message":"Three field commits on `gitcloud/jazzy` need to come back to GitHub for review.\nThe local `jazzy` is also ahead by 25 (recent dev-log and PR-merge series),\nso this branch is **diverged** — a merge with `jazzy` will be needed before\nthis can land.\n\n## Commits to import\n\n- `2d93693` refactor: rename diagnostics_*.bag → operator_\u003cdatetime\u003e; nest under bags/\n- `5c8f181` feat: launch screenshooter as tmux window; drop autostart_*.txt redirection\n- `f0337b7` feat(startup): set SCREENSHOOTER_LABEL=operator explicitly\n\n## Diff\n\n`bizzyboat_project11/launch/bag_recorder_operator_launch.py`:\n\n- Bag layout\n  `~/data/logs/operator/\u003cYYYY-MM-DD\u003e/diagnostics_\u003cHH.MM.SS\u003e/` →\n  `~/data/logs/operator/\u003cYYYY-MM-DD\u003e/bags/operator_\u003cYYYY-MM-DDTHH.MM.SS\u003e/`.\n  Operator-prefixed name (vs. station-agnostic `diagnostics_`) and groups all\n  bags under a `bags/` subdir per day.\n\n`bizzyboat_project11/scripts/start_tmux_operator_project11.bash`:\n\n- Drop the script-wide `autostart_*.txt` redirection wrapper. Output now\n  goes wherever tmux/cron sends it.\n- Add a new `screenshooter` tmux window that runs `screenshooter.bash` from\n  the installed `ccomjhc_project11` share dir, with `SCREENSHOOTER_LABEL=operator`.\n\n## Pre-review against Quality Standard\n\n- ✅ Bag rename: pure layout change, no recorded topic changes; harmless.\n- ⚠️ Removing autostart logging: previously captured stdout/stderr to\n  `${LOGDIR}/autostart_${NOW}.txt`. Without it, cron-launch failures depend\n  on `cron` / `systemd-journal` being checked instead. Intentional per the\n  commit message but worth confirming the observability fallback is\n  documented somewhere.\n- ⚠️ Hardcoded path `/home/field/project11/layers/main/site_ws/install/ccomjhc_project11/share/...`\n  to launch screenshooter — host-specific assumption (consistent with how\n  other lines in the script are structured).\n- ⚠️ Cross-repo coupling: depends on the parallel `ccomjhc_project11` field-\n  import PR that ships the rewritten `screenshooter.bash` consuming\n  `SCREENSHOOTER_LABEL`.\n\n## Diverged\n\n`gitcloud/jazzy` is 3 commits ahead of local `jazzy`; local `jazzy` is 25\ncommits ahead of `gitcloud/jazzy`. Merge with the default branch is required\nbefore this PR can be merged.\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.7 (1M context)`","files":null}]}