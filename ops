{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1779979732,"nonce":"qvUedPFw0eJ2eKecqfcxjL67GaY=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABDq-Cxg","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/192","origin":"github"},"title":"Adopt workspace /start-deployment skill (.agents/deployment.yaml + docs/logs/README.md shrink)","message":"## Goal\n\nAdopt the workspace `/start-deployment` skill ([ros2_agent_workspace#501](https://github.com/rolker/ros2_agent_workspace/issues/501), merged) on BizzyBoat — both files needed are bundled in this issue / one PR:\n\n1. **`.agents/deployment.yaml`** — per-project config that drives the workspace skill (platform identity, default branch, layer, packages, log_dir, dev-side defaults for tides/currents/weather, plus the field-side `issue_sync` commands that pipe git-bug + gitcloud).\n2. **`docs/logs/README.md` shrink** — point at the workspace skill ([ADR-0014](https://github.com/rolker/ros2_agent_workspace/blob/main/docs/decisions/0014-deployment-mode.md), [`.agent/knowledge/deployment_mode.md`](https://github.com/rolker/ros2_agent_workspace/blob/main/.agent/knowledge/deployment_mode.md)) for the generic lifecycle / file-layout / urgency-contract content; keep only BizzyBoat-specific overrides (sample logs, the frozen monolithic-log anti-pattern reference, the user-curated Summary / Lessons-Learned section guidance).\n\n## Why now\n\nThe next BizzyBoat deployment is the first chance to use the workspace skill end-to-end. The config file is the gate: without it, `/start-deployment` stops at step 1. The README shrink is bundled because it's the next-cleanest backlog-reduction step — the workspace doc now carries everything generic.\n\n## Scope (single PR, two atomic commits)\n\n- **Commit 1**: `.agents/deployment.yaml` from the workspace template, populated with BizzyBoat values.\n- **Commit 2**: `docs/logs/README.md` shrunk to BizzyBoat-specific overrides + pointers to the workspace docs.\n\nOut of scope: any change to the existing `docs/logs/2026/*` log files, any change to `docs/bizzyboat_deployment_log.md` (still frozen per PR #93).\n\n## Verification\n\nDry-run the skill from a dev session after the PR lands locally:\n\n- `/start-deployment` from inside the project repo (a fresh worktree)\n- Step 1 reads the config without error\n- Step 2 detects dev side correctly\n- Three-state detection finds (or creates) the deployment issue\n\nField-side `issue_sync` (`git bug pull gitcloud`, `git bug ls --label deployment --status open`, `git bug show {id}`) can be verified during the first actual field deployment.\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.7 (1M context)`","files":null}]}