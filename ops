{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1779904973,"nonce":"0LqIRL7mpOhdfOlXA0qiWu8PK7c=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABDkhZyw","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/187","origin":"github"},"title":"Forward windowed local-costmap + nav-feedback topics to operator (Part of unh_marine_autonomy#127)","message":"## Goal\n\nWire the new windowed local-costmap (rolker/unh_marine_navigation#38, merged) to\nthe operator over the udp_bridge, and audit/forward other low-bandwidth nav-stack\nfeedback topics so the operator has a usable situational-awareness picture on both\nthe WiFi and VPN links.\n\nPart of rolker/unh_marine_autonomy#127.\n\n## Changes (BizzyBoat)\n\n**Launch** (`bizzyboat_project11/launch/nav_launch.py`): run\n`marine_nav_utilities/costmap_window_node` on the boat in the `\u003cns\u003e/local_costmap`\nnamespace (subscribes `costmap`, publishes `costmap_windowed`), `window_size:=200.0`.\n\n**Bridge** (`bizzyboat_project11/config/bizzyboat.yaml`, `/**/udp_bridge`):\n\n- Drop raw `local_costmap` (`local_costmap/costmap`, ~78% loss — see #127); forward\n  `local_costmap_windowed` (`local_costmap/costmap_windowed`) instead: **WiFi full\n  rate, VPN `period: 2.0`**, on both connections.\n- Add to VPN (already on WiFi): `path_follower_visualization`, `local_costmap_footprint`.\n- Add to both (currently unbridged): `collision_monitor/slowdown_polygon`,\n  `collision_monitor/stop_polygon`, `collision_monitor_state` (#170 reflex zones/state).\n\n**package.xml**: `exec_depend` on `marine_nav_utilities` (launch runs its executable).\n\n## Out of scope / follow-up\n\n- CAMP rendering of the danger-zone polygons (separate question).\n- Promoting the node launch to the generic `echoboat_project11` package for IzzyBoat\n  (this PR keeps it BizzyBoat-local for the pier test).\n\n## Validation\n\n- Config/launch sanity locally; real validation is the **pier test on the boat**\n  (deploy: propagate merged `marine_nav_utilities` + this config to gabby, rebuild core,\n  confirm CAMP shows the windowed costmap + path + zones, measure WiFi vs VPN delivery).\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.7 (1M context)`","files":null}]}