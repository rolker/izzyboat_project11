{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1780851165,"nonce":"pP0WkDeUqd914b2vCKZVhxBY6LQ=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABEqkZHw","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/231","origin":"github"},"title":"manifest: marine_tools mis-filed in core layer, should be sensors","message":"**Manifest layer bug.** `marine_tools` is declared in the **core** layer\n(`config/repos/core.repos`) but it belongs in **sensors** — it's `sensors` in the\ndev (`unh_marine_autonomy`) and operator (`ccomjhc_project11`) manifests, and it's\nactually a sensors-layer package (checked out in `sensors_ws/src/marine_tools` on\ndev).\n\nOn the boat this clones `marine_tools` into `core_ws/src` and builds it a layer too\nlow — `core` cannot depend on `sensors`/`ui`, so this is a latent layering hazard\nand a cross-manifest inconsistency.\n\n**Fix:** move the `marine_tools` entry from `config/repos/core.repos` to\n`config/repos/sensors.repos` (same gitcloud URL, version jazzy).\n\nFound during a manifest audit for the shared colormap effort\n(rolker/unh_marine_autonomy#137).\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.8 (1M context)`","files":null}]}