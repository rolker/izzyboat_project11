{"author":{"id":"746a4d30afc20b78119cb8ed7c194f988174fc252f0e9d9eb92022244ef86db3"},"ops":[{"type":1,"timestamp":1781003047,"nonce":"q12NbrpRVYajAslCuoxmAw1XqY4=","metadata":{"github-id":"I_kwDOK4fCvM8AAAABE3SHyA","github-url":"https://github.com/rolker/unh_echoboats_project11/issues/241","origin":"github"},"title":"Field import: unh_echoboats_project11 battery monitoring (2026-06-09)","message":"Field change pushed to `gitcloud/jazzy` after the #239/#240 import, imported for review.\n\n## Commit\n\n- `391dbb3` battery monitoring: log voltage every minute, auto-select FCU vs MAVROS\n\n## Diff (net, vs `jazzy`)\n\n- `bizzyboat_project11/scripts/mavros_battery.sh` (new, +93): read\n  `/bizzy/mavros/battery` from the running stack with `--qos-profile sensor_data`\n  (matches MAVROS's BEST_EFFORT publisher) for when MAVROS holds the single-owner\n  `/dev/fcu` serial port.\n- `bizzyboat_project11/scripts/battery_logger.sh` (new, +64): cron-driven; `flock`\n  (no overlap) + `fuser`-based source select (port free → FCU, busy → MAVROS),\n  appends to `~/data/logs/bizzy_battery/battery_\u003cdate\u003e.csv`; logs nothing on a gap.\n- `bizzyboat_project11/scripts/fcu_battery.sh` (+26/-?): add `--csv` mode\n  (`volts,amps,remaining`).\n\n## Import method\n\n**Merge, not branch-from-remote.** `gitcloud/jazzy` lacks the #239 doc commits\n(they reached origin via the #240 merge), so a branch-from-`gitcloud/jazzy` PR\nwould spuriously show `docs/logs/2026/2026-06-08_dev_logs.md` deleted (−387). The\nimport branch is cut from `origin/jazzy` and **merges** `gitcloud/jazzy` (merge\nbase `6db622d`, already on origin), bringing only `391dbb3`'s net changes while\npreserving the field SHA so gitcloud reconciles by fast-forward after merge.\n\n## Pre-review (against Quality Standard)\n\n- `mavros_battery.sh` subscribes with `--qos-profile sensor_data` — correct for\n  MAVROS's BEST_EFFORT battery publisher (a default RELIABLE sub matches nothing).\n- **No current meter on BizzyBoat**: the CSV carries a `current_a` column. FCU\n  `current_battery == -1` → empty (handled); MAVROS may emit `0`/NaN, so the\n  column can be empty or misleading. Consider dropping it or documenting that\n  current is not instrumented on this hull.\n- Hardcoded field-host path `/home/field/project11/.../site_ws/install/setup.bash`\n  in `mavros_battery.sh` — guarded with `[ -f ]`, but a field-host assumption.\n- Cron install is on gabby only and not version-controlled (noted in the commit).\n\nPart of the BizzyBoat battery-monitoring tooling that began with `fcu_battery.sh`\n(#239 / PR #240).\n\n---\n**Authored-By**: `Claude Code Agent`\n**Model**: `Claude Opus 4.8 (1M context)`","files":null}]}