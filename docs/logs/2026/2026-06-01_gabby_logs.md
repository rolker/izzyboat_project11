# Field log — gabby — 2026-06-01

**Host**: gabby
**Operator**: Roland + Claude Code Agent (Claude Opus 4.8, 1M context)
**Mode**: field (gitcloud origin)
**Deployment issue**: git-bug `f1fb95e` — "Deployment 2026-06-01: clean survey run — override/resume (#52) + turning limits + cross-track"
**Started**: 2026-06-01 13:22 -04:00

## Timeline

**2026-06-01 13:22 -04:00** — Deployment mode activated on gabby via `/start-deployment` (field side, gitcloud origin). First activation of git-bug issue `f1fb95e`. Issue title already canonical and `## Logs` section present — no drift. **TODO (dev):** stamp `- [gabby log](docs/logs/2026/2026-06-01_gabby_logs.md)` under the deployment issue's `## Logs` section next time `/start-deployment` runs on dev (field side can't edit the issue — no `gh`, git-bug has no edit verb here). git-bug refs fetched via plain `git fetch refs/bugs/*` (git-bug's own SSH-agent transport is unavailable: `SSH_AUTH_SOCK` unset).

**2026-06-01 13:29 -04:00** — Operator reported no costmap visible on the operator station (CAMP/salmon). On gabby, costmaps were publishing normally: `/bizzy/local_costmap/costmap` (OccupancyGrid) had 1 publisher / 0 subscribers, `costmap_windowed` had 1 pub / 1 sub, `global_costmap/costmap` 1 pub. Symptom was operator-side only → transport/discovery, not production. Consistent with the `rmw_zenoh` transient_local (latched) costmap race. **Resolved:** operator applied the costmap-sending hack in `udp_bridge` — costmap now showing on the operator side. (Operator: "did it in udp bridge and it works now.")

**2026-06-01 13:31 -04:00** — Operator going to launch the boat. Stack up, costmap confirmed on operator side. Moving prep → launch.

**2026-06-01 13:45 -04:00** — Boat in the water (underway). Started 45-min (2700 s) camera capture via `bizzyboat_project11/scripts/record_camera_topics.sh 2700`. Bag: `~/data/logs/bizzy_images/bag_2026-06-01T13.45.10_ffmpeg_seg`. All 24 topics subscribed — 4 OAK cameras (forward/starboard/aft/port) ffmpeg + segmentation raw/compressed + camera_info, TF, diagnostics, robot_description, `/bizzy/local_costmap/costmap`. Recorder started clean (the two startup `rmw_zenoh_cpp` liveliness/typesupport errors are the usual zenoh+rosbag2 noise; recording proceeded normally).

**2026-06-01 13:46 -04:00** — Operator running a survey pattern to test the deployment's must-verify items (override/resume, turning limits, cross-track, survey-line obstacle avoidance, small-obstacle marking, CAMP overlays). Camera capture ongoing. Boat-side bag should also be capturing `pid_state` for offline cross-track analysis.

**2026-06-01 13:48 -04:00** — Testing **mid-line goto override** (unh_marine_navigation #52/#53, PRIMARY). Expected: boat re-targets to the new goto point (updatable — does not latch on the original); goto auto-completes at the point and then the survey **resumes from the boat's current position** (goto does not hold — that's hover). Awaiting operator observation.

**2026-06-01 13:49 -04:00** — ⚠️ **Result: mid-line goto override → "it restarted the line"** (operator). After the goto override completed, the survey did **not** resume from the boat's current position — it re-ran the line from the start. This is the failure mode the resume-from-position change (#52/#53) was built to avoid. **Deferred to wrap-up RCA** (not investigating live): candidate per issue "Watch out for" #1 — mid-line resume relies on `RobotOnPath` (5 m cross-track tolerance); if the goto endpoint left the boat reading as off-line, resume falls back to a full line re-run. Bag is capturing for offline review. Must-verify item "Updatable goto" not yet separately confirmed (re-target behavior vs. resume behavior are distinct).

**2026-06-01 13:51 -04:00** — Operator reports CAMP (operator station) keeps crashing; troubleshooting on the **salmon** side with the salmon agent. Operator-station issue, not gabby — gabby stack + camera capture unaffected and still running. Detail will live in the salmon host log.

**2026-06-01 14:06 -04:00** — Operator: target in front of boat not appearing in costmap; asked to check forward segmentation. Quick diagnosis on gabby (`/bizzy/local_costmap/local_costmap`):
- **Today's #27 max-pool fix IS live**: `sea_surface_layer.max_pool_bins=True`. `observation_sources=[forward,port,starboard,aft]`, `forward.image_mask_rects=[]` (no mask), `maximum_range=150`, `obstacle_prob_min=0.35`, `min_grazing_angle_deg=0`. Forward segmentation topic publishing (1 pub / 4 subs). → Layer config is correct; **not** the #26 marking bug recurring.
- **Local costmap is a 400×400 @ 0.25 m rolling window = 100 m × 100 m (±50 m from boat).** Sensor range is 150 m but the costmap only covers ±50 m → **a target >~50 m ahead is outside the local costmap window and won't mark until the boat closes in** (matches avoidance note #2: in-window only). Leading candidate.
- If target is within ~50 m and still not marking → next check is whether forward segmentation classifies it as obstacle (reads red) vs water, or projected evidence < `obstacle_prob_min` 0.35. Decisive view is the forward segmentation image (CAMP overlay, currently down) or a frame grab. Bag is capturing forward segmentation raw for tuner replay (how #26 was originally found). `obstacle_prob_min` is the tuning lever if a real target reads below threshold — operator's call, and verify any `param set` under zenoh.

**2026-06-01 14:08 -04:00** — Operator update: target is **~10 m ahead** (well inside the ±50 m window — rules out the range explanation), **bearing varies as the boat swings on hover**, and it **occasionally shows up** in the costmap (intermittent). Params: `decay_half_life_s=30` (long — not passive decay), `max_evidence_step=0.85`, `obstacle_clamp=5.0`, `clear_floor=-2.0`. Hypothesis: **input-side flicker** — a small close target gets intermittently classified/projected as obstacle as the boat yaws, and when a camera reads that cell as water in between it's **actively cleared** (clear_floor −2.0) faster than the 30 s decay. Not the marking-config bug; it's small-obstacle detection robustness under hover-yaw. **This is itself a valuable must-verify finding** (small obstacles mark, but intermittently under yaw). Bag is capturing the full sequence for tuner replay — the right tool to tune this, same workflow that found #26. Deep tuning deferred to replay; no live param changes made.

**2026-06-01 14:13 -04:00** — Operator approved trying the live lever. **Set `sea_surface_layer.clear_floor` −2.0 → −0.5** on `/bizzy/local_costmap/local_costmap` (less-negative floor → a marked cell can't be driven as far back toward free between sightings, countering the active-clearing flicker). One variable at a time. `param set` reported success and `param get` confirmed `-0.5` (verified against zenoh silent-drop). Whether the sea_surface_layer plugin re-reads `clear_floor` live (vs. only at load) is the open question — watching the costmap for whether the ~10 m target now persists across the hover-yaw. Original value −2.0 to restore. Change is live-only (not in YAML) → wiped on next relaunch; if it helps, push to `bizzyboat.yaml` for durability.

**2026-06-01 14:16 -04:00** — ✅ **`clear_floor` −0.5 helped** — operator: "targets are better now." Marking of the ~10 m target is more reliable through the hover-yaw. Confirms the sea_surface_layer plugin **does re-read `clear_floor` at runtime**. **Action item:** persist `sea_surface_layer.clear_floor: -0.5` to `bizzyboat.yaml` (currently live-only, wiped on relaunch) — deferred to wrap-up per sterile-cockpit.
- **Survey-line obstacle avoidance (#30/#51):** operator saw the "AVOIDING" message/overlay on a **prior** line but **not** the current pass — intermittent. Plausibly tied to the now-improved obstacle marking: while the target flickered out of the costmap, the corridor avoider had no obstacle in-corridor to react to. Now that marking is steadier, worth watching whether avoidance triggers more consistently on the next line. (Also gated on the obstacle being in the survey corridor ahead + in the local-costmap window.) Logged as a must-verify observation; not investigating live.

**2026-06-01 14:22 -04:00** — Operator requested setting "max grazing angle" to 1.2; clarified there is no max param — only `sea_surface_layer.min_grazing_angle_deg` (degrees, was 0.0). Operator confirmed **degrees**. **Set `min_grazing_angle_deg` 0.0 → 1.2°** on `/bizzy/local_costmap/local_costmap`; `param set` reported success and `get` confirmed `1.2` (verified vs zenoh silent-drop). Mild filter — rejects only the most extreme near-horizon (low-grazing) rays whose water-plane projection is least reliable; pairs with the stickier `clear_floor` to trim far/shallow phantom marks. Live-only → wiped on relaunch; fold into `bizzyboat.yaml` alongside `clear_floor` at wrap-up if kept. 2nd live param change this session (one variable at a time: clear_floor first, now grazing).

**2026-06-01 14:26 -04:00** — ⚠️ **Two coupled problems reported.** (1) Operator: "collision avoidance slowdowns are paralysing the boat by removing steering authority." (2) Operator: "false positives now are messing with the line planning." Immediate mitigation advised: operator take manual/pilot control to recover steering way.
- **Two-path root-cause split** (re-confirms "CA ≠ SeaSurfaceLayer tuning"): **line-planning FPs** come from the **costmap** (sea_surface_layer → corridor avoider); **CA slowdown** comes from the **Collision Monitor** reading `reflex_cloud` (independent path; costmap tuning does NOT affect it).
- **Owned + reverted:** the line-planning FP surge was amplified by my earlier `clear_floor -0.5` (stickier marks → phantoms persist → planner bends around ghosts). **Reverted `clear_floor` -0.5 → -2.0** (verified). Kept `min_grazing_angle_deg 1.2` (helps trim FPs).
- **CA slowdown config** (`/bizzy/collision_monitor`): `CollisionSlowdown.slowdown_ratio=0.3` (slows to 30% of cmd), polygon `[[20,3],[20,-3],[0,-3],[0,3]]` = **20 m × 6 m box ahead**, source `reflex_cloud`, enabled. Any FP in that 20 m box drops speed to 30% → below steering-authority threshold → paralysis. **Lever is in CA, safety-relevant → operator risk-acceptance call; not changed unilaterally.** Options presented: raise `slowdown_ratio` (keep more way-on), shrink the 20 m polygon, or fix at source (segmentation FPs in reflex cloud — replay tuning). `CollisionStop` left intact regardless. No CA param changed yet.

**2026-06-01 14:28 -04:00** — Operator approved: raise slowdown + shrink polygon. On `/bizzy/collision_monitor`: **`CollisionSlowdown.slowdown_ratio` 0.3 → 0.5** (keeps ~half speed = steering way) and **`CollisionSlowdown.points` shrunk 20 m → 10 m forward** (`[[10,3],[10,-3],[0,-3],[0,3]]`, ±3 m lateral unchanged). Both `param set` successful + `get`-verified (string-param set for `points` took cleanly). `CollisionStop` untouched — close-range safety stop intact. Combined effect: slowdown is gentler AND only triggers for obstacles within 10 m, so distant FPs stop paralysing steering. Open question (same as costmap params): whether Collision Monitor re-reads these live vs. at load — operator watching boat behavior. **Live-only → wiped on relaunch.** Wrap-up YAML candidates so far: `min_grazing_angle_deg 1.2`, `slowdown_ratio 0.5`, slowdown polygon 10 m (clear_floor is back at original −2.0, no YAML change needed). Underlying segmentation FPs (feed both reflex_cloud + costmap) remain the real fix → replay tuning.

**2026-06-01 14:31 -04:00** — 📦 45-min camera capture complete (exit 0, flushed clean): `bag_2026-06-01T13.45.10_ffmpeg_seg_0.mcap` **3.1 GB**, metadata.yaml present. ~13:45→14:30 window — covers the override/resume test, the marking/FP tuning, and the CA slowdown episode for offline replay.

**2026-06-01 14:31 -04:00** — Operator: "the part that adjusts a survey line to avoid obstacles doesn't seem to be working anymore" (corridor path-adjuster #30/#51). **Likely my doing:** the avoider reads the **local costmap** for obstacles to bend around; reverting `clear_floor` -0.5 → -2.0 at 14:26 restored the intermittent-dropout marking, starving the avoider of reliable obstacles. **Genuine trade-off, not winnable with `clear_floor` alone:** sticky marks → avoider works but costmap FPs return to line planning; un-sticky marks → clean planning but avoider underfed. The right disambiguator is the **grazing filter** (rejects FAR low-grazing FPs while keeping the NEAR ~10 m real target) — currently only 1.2° (mild). Proposed combined move (awaiting operator): restore stickiness (`clear_floor` ~-0.5 or -1.0) **and** raise `min_grazing_angle_deg` (~8°) to kill far FPs at source. Secondary factor for replay: avoider `obstacle_avoidance_weight` default is 0.02 (unvalidated placeholder) — may be too weak to bend the line even with good marks. No further live param flips without operator call (avoiding flip-flop).

**2026-06-01 14:36 -04:00** — Operator pushed back: grazing >1.5° shrinks sensing radius too much; asked for the calc. **Geometry:** grazing θ at water range d, camera height h → tan θ = h/d, so max range **R = h / tan θ**. Forward camera height above `bizzy/map_tide` (sea-surface) from TF = **1.406 m** (Z stable while X/Y move with boat). Table: 1.2°→67 m, **1.5°→54 m**, 2.0°→40 m, 5.0°→16 m, 8.0°→**10 m**. Each 0.5° above 1.5° costs ~13 m (~20–25%); my proposed 8° = **−81% range** (54→10 m). **Operator correct — retracted the 8° suggestion.** Key result: since the **local costmap is only ±50 m**, grazing ≤~1.5° (R≥54 m) keeps full window coverage but >~1.6° starts blinding the window → **grazing has ~zero headroom as an FP lever** on this low (1.4 m) camera. **Pivot:** the right FP lever is confidence-based — `obstacle_prob_min` (currently 0.35), bearing/range-agnostic, suppresses weak glare FPs while keeping strong near-target marks. Revised proposal: restore `clear_floor` stickiness for the avoider + raise `obstacle_prob_min` to hold FPs down (instead of grazing). Awaiting operator.

**2026-06-01 14:38 -04:00** — Operator: "tune both." On `/bizzy/local_costmap/local_costmap`: **`clear_floor` -2.0 → -0.5** (re-feed avoider with persistent near obstacles) + **`obstacle_prob_min` 0.35 → 0.5** (confidence gate to suppress weak glare FPs without touching range). Both `param set` successful + `get`-verified. Watching: (a) avoider bends the line around a real obstacle again, (b) far/ghost FPs stay out of line planning. If ghosts still appear, raise `obstacle_prob_min` further (0.6); if avoider still underfed, `clear_floor` toward -1.0. **Current live tuning state** (all live-only, wiped on relaunch → YAML at wrap-up): `clear_floor -0.5`, `obstacle_prob_min 0.5`, `min_grazing_angle_deg 1.2` (sea_surface_layer); `slowdown_ratio 0.5`, slowdown polygon 10 m (collision_monitor). Grazing confirmed NOT a viable FP lever (low camera).

**2026-06-01 14:50 -04:00** — Operator: "too many false positives" + "line adjuster still not working when going toward a target on the costmap."
- **FP backoff:** `clear_floor` -0.5 → **-1.25** (halfway — less sticky → ghosts clear faster; the real FP knob). Kept **`obstacle_prob_min 0.5`** — going "halfway" on it (→0.425) would *lower* the confidence gate and admit MORE FPs, opposite of intent; if FPs persist, raise it (0.6), don't lower. Verified.
- **Avoider not bending — investigated, NOT a tuning issue:** `survey_avoidance.*` live params are **absent** from `/bizzy/bt_task_navigator` (30 params, none of them the avoider's; spec says they should be live ROS params there). So the corridor path-adjuster (#30/#51) **is not active in the running behavior tree** → it can't bend the line regardless of marking. **Ruled out stale build:** nav node PID 6667 started **13:09:07**, *after* the 13:05 build — it loaded the fresh install. No plugin-load errors in `~/.ros/log/2026-06-01-13-09-07-*/`; no log mention of adjust/survey. Params file references `run_tasks` tree + `marine_nav_behavior_tree_bt_plugins`. **Leading hypothesis:** BT node params are declared per-loaded-tree (task-dependent); the avoider node isn't in the currently-active task path / didn't register for this run (consistent with operator seeing "AVOIDING" on an earlier line but not now). Root cause (which `run_tasks.xml` variant loaded + whether `AdjustPathForObstacles` is in the tree & `plugin_lib_names`) deferred to replay/wrap-up — too deep for a live knob. Bag has the full sequence. **Options handed to operator:** (a) defer avoider RCA to replay; (b) clean relaunch of nav group to reload trees (disruptive mid-survey, may not fix if it's an XML/registration gap). Time-boxed; handed back.

**2026-06-01 14:57 -04:00** — Avoider came back (a later task loaded the tree with the node — confirms the per-loaded-tree / task-dependent hypothesis above). Operator: it's working "but doing way too little" (deviation too small). **Tried to raise the corridor weights live — can't:** `survey_avoidance.*` params are NOT declared on `/bizzy/bt_task_navigator` (full 30-param list checked, none avoidance/weight/deviation/lateral under any name). So the avoider's tunables (`obstacle_avoidance_weight` default 0.02 vs `line_following_weight` 1.0 = 50:1 toward hugging the line; `max_deviation`) are **baked into the loaded BT XML (run_tasks.xml ports), fixed at tree-load — not live ROS params.** ⚠️ **This contradicts the deployment-issue claim** (#30/#51 comment: "Every tunable is a live ROS parameter on the bt_navigator node … `ros2 param set <bt_navigator-node> survey_avoidance.<name>`"). Deployed build does not expose them. **No live knob to increase deviation this run.** Options: (a) accept the conservative avoidance for now (it IS avoiding, just modestly — and the issue itself says tune weights in sim first); (b) edit `run_tasks.xml` ports + reload tree / relaunch nav group — source change + disruptive, defer. **Wrap-up items:** (1) avoider under-tuned — raise `obstacle_avoidance_weight` from 0.02 placeholder (sim-validate the ratio first); (2) **live-tunability gap** — wire `survey_avoidance.*` as real ROS params as the issue claims, or correct the issue/docs. No live change made.

**2026-06-01 15:03 -04:00** — Operator: "look at why params are not live." Read avoider source (`adjust_path_for_obstacles.cpp`). **Root cause (code-grounded):**
1. Params are declared **lazily on first `tick()`** — inside the `if (!costmap_sub_)` setup block (L474, L492–500), not at tree load. So they don't exist until the avoider node has ticked at least once after a tree (re)load.
2. They're declared on **`config().blackboard->get<rclcpp::Node>("node")`** (L466) — in Nav2 that's the bt_navigator's *internal* BT client node **`bt_task_navigator_run_tasks_rclcpp_node`**, NOT the main `bt_task_navigator`. The deployment-issue instructions say `ros2 param set <bt_navigator-node> survey_avoidance.<name>` → points at the **wrong node**, where the params never exist. That alone explains why they looked "not live."
3. **The internal `_rclcpp_node` doesn't service external param RPCs** — `param list`/`get` to it **hang/timeout** (8 s bounded get → timeout), while the same CLI works fine against `local_costmap` and `collision_monitor` under the same zenoh. So even targeting the *right* node, the documented `ros2 param set` / rqt_reconfigure workflow can't reach them. The "live tunable" design is **effectively broken on the boat**: tunables are declared on a node Nav2 doesn't expose for external parameter services.
- **Net:** avoider weights are NOT live-tunable via CLI this run, regardless of node targeted. To change them now requires editing `run_tasks.xml` port defaults + tree reload (source change, disruptive — defer). **Real fix (wrap-up / issue on #30/#51):** declare `survey_avoidance.*` on the main bt_navigator lifecycle node (externally param-serviced) and read from there, instead of the internal BT rclcpp node; also declare at configure, not lazily on first tick. Time-boxed; handed back. Durable finding to capture as project memory at wrap-up.

**2026-06-01 15:07 -04:00** — Operator: make costmap less sensitive to weak targets but stronger ones more persistent (workaround for apparent wandering preventing registration). On `/bizzy/local_costmap/local_costmap`: **`obstacle_prob_min` 0.5 → 0.6** (raise marking confidence gate → weak/glare hits don't mark) + **`obstacle_clamp` 5.0 → 10.0** (raise evidence ceiling → only strongly/repeatedly-hit cells bank high evidence, giving real targets headroom to survive decay + the clearing the wander causes; weak cells never approach the clamp so they don't benefit). `clear_floor` left -1.25, decay left 30 s. Both verified. **Trade-off:** a passed strong target may leave a short stale mark (more banked evidence takes longer to clear) — bounded by the 30 s decay. Watch: real near target persists through the hover-swing; weak FPs drop out. Current sea_surface_layer state: `obstacle_prob_min 0.6`, `obstacle_clamp 10.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.2`, `decay_half_life_s 30`.

**2026-06-01 15:09 -04:00** — Operator: "raise it a bit more." Interpreted as the persistence lever → **`obstacle_clamp` 10.0 → 14.0** (verified). More headroom for strong targets to ride through the wander. Flagged to operator that if "it" meant the confidence gate (`obstacle_prob_min`), bump that instead. Watch for longer stale trails behind passed targets (clamp 14 takes longer to decay/clear).

**2026-06-01 15:10 -04:00** — Operator clarified: not the clamp. **Reverted `obstacle_clamp` 14.0 → 5.0** (original) and **raised `obstacle_prob_min` 0.6 → 0.7** (verified). Persistence approach dropped; confidence-gating is the chosen lever for the FP/weak-target problem. Watch: 0.7 is a fairly high gate — confirm genuine (but weaker) targets still mark; if real targets start dropping, ease back toward 0.6. Current sea_surface_layer state: `obstacle_prob_min 0.7`, `obstacle_clamp 5.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.2`, `decay_half_life_s 30`.

**2026-06-01 15:12 -04:00** — Operator: set "max graze" (= `min_grazing_angle_deg`, the only grazing param) to 1.5. **Set 1.2 → 1.5** (verified). R≈54 m at h=1.41 m — still covers the full ±50 m local window; the established ceiling before grazing starts blinding the costmap. Current sea_surface_layer state: `obstacle_prob_min 0.7`, `obstacle_clamp 5.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`, `decay_half_life_s 30`.

**2026-06-01 15:15 -04:00** — Operator: the CA slowdown-box shrink "didn't take effect." **Confirmed-by-design:** Nav2 Collision Monitor builds polygon shapes at configure/activate; `CollisionSlowdown.points` is stored (verified still 10 m) but the running monitor keeps the 20 m polygon it built at startup. The scalar `slowdown_ratio` (0.5) likely DID apply (handled by the dynamic-params callback); the **polygon shape did not** (not rebuilt at runtime). To actually shrink the box requires a collision_monitor lifecycle re-configure (briefly drops CA — safety, avoid mid-op) or a nav relaunch. **No live shape change this run.** → wrap-up: confirm which CA params are runtime-dynamic; set the box in YAML. Same class of bug as the avoider params (set succeeds, node doesn't re-read).

**2026-06-01 15:15 -04:00** — Operator: saw a diversion with "AVOIDING" printed at a spot with **no target** — stale avoidance line? **Likely a stale/ghost COSTMAP mark, not a stale overlay** (operator saw a real diversion → avoider faithfully bent around something on the costmap). Candidate sources: (a) a passed target's mark lingering (persistence: `clear_floor -1.25` + 30 s decay); (b) a brief FP that marked before `obstacle_prob_min` went to 0.7; (c) the wander/timing issue mis-projecting a real target to a phantom cell, which then persisted. Avoider reacts to whatever's on the costmap, so it diverts around a ghost. Should clear as the mark decays (~tens of s at 30 s half-life). Faster-clear levers: shorten `decay_half_life_s`, or make `clear_floor` more negative. 0.7 gate reduces NEW ghosts. Ties together wander + persistence + stale-avoidance — strong replay-analysis candidate.

**2026-06-01 15:16 -04:00** — Operator: drop decay. **Set `decay_half_life_s` 30.0 → 10.0** (verified). Stale marks/ghosts fade ~3× faster → fewer no-target "AVOIDING" diversions. **Trade-off (tension with the earlier persistence goal):** faster decay also makes real targets fade quicker between sightings, so the wander-dropout could return. Watch both: ghost-diversions should drop; if real targets start winking out again, that's the decay cutting too fast (ease back toward ~15–20 s, or this is fundamentally the timing/wander problem to fix offline). Current sea_surface_layer state: `obstacle_prob_min 0.7`, `obstacle_clamp 5.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`, `decay_half_life_s 10`.

**2026-06-01 15:17 -04:00** — Operator: set decay back to 30. **Reverted `decay_half_life_s` 10.0 → 30.0** (verified). Current sea_surface_layer state: `obstacle_prob_min 0.7`, `obstacle_clamp 5.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`, `decay_half_life_s 30`.

**2026-06-01 15:34 -04:00** — Live test: **`obstacle_prob_min` 0.7 → 0.8**, **`obstacle_clamp` 5.0 → 15.0** (verified). Live-only test values — NOT yet in baked config (config has 0.7 / default 5.0); update `nav2_overlay.yaml` if these prove out. Live state: `obstacle_prob_min 0.8`, `obstacle_clamp 15.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`, `decay_half_life_s 30`.

**2026-06-01 15:35 -04:00** — Operator: bake those into config. Updated `nav2_overlay.yaml`: `obstacle_prob_min` 0.7 → **0.8**, added `obstacle_clamp: 15.0`. Rebuilt bizzyboat_project11; installed copy verified. **Baked config now:** sea_surface_layer `obstacle_prob_min 0.8`, `obstacle_clamp 15.0`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`; CollisionSlowdown box 10 m + `slowdown_ratio 0.5`; avoider `obstacle_avoidance_weight 0.15` (run_tasks.xml). Still uncommitted pending operator relaunch + validate.

**2026-06-01 15:44 -04:00** — Operator relaunched nav group. **Relaunch healthy + config loaded:** bt_task_navigator restarted 15:40:20; costmap `obstacle_clamp` reads 15.0 (confirms new nav2_overlay.yaml loaded). ✅ **CA box shrink CONFIRMED working** (operator: "collision avoidance boxes are smaller now"). Avoider loaded — `/bizzy/survey_obstacle_avoidance` overlay topic has a publisher (node instantiated + ticked). **But operator: not seeing an avoidance path.** Avoider only draws/deviates around costmap-marked cells; prime suspect is `obstacle_prob_min 0.8` (aggressive test gate) suppressing the real target so nothing marks → nothing to avoid. Discriminator handed to operator: check CAMP costmap layer — if target not marking, lower `obstacle_prob_min` (→0.6); if marking but no deviation, it's the avoider (weight/geometry). No change made yet.

**2026-06-01 15:49 -04:00** — Operator: avoider now "avoiding something where nothing exists, seems stale." Stale costmap ghost — consistent with `obstacle_clamp 15` (high banked evidence → slow to decay/clear after target gone or transient FP). Avoider diverts around the marked ghost. Both failure modes now observed (clamp 5 + fast decay → wander dropout; clamp 15 → stale ghosts) → confirms root is the timing/wander projection (offline fix). Proposed live test: `obstacle_clamp` 15 → 6, `decay_half_life_s` 30 → 15 to clear ghosts faster. Awaiting operator value/go.

**2026-06-01 15:52 -04:00** — Operator: costmap is NOT showing a mark there — "I think you are wrong about it being the clamp." **Correct — I was wrong** (clamp/costmap-mark theory dropped for this instance). Echo of `/bizzy/survey_obstacle_avoidance` timed out (6 s, nothing) → overlay topic not publishing now; CAMP is showing **stale latched markers**, costmap clean → it's a **stale visualization overlay**, NOT a real diversion (distinct from the 15:15 case which was a real costmap mark).

**2026-06-01 15:55 -04:00** — Code review of `adjust_path_for_obstacles.cpp` (operator request). **Root cause of stale phantom overlay:** node is a `BT::SyncActionNode` with **no `halt()` override and no destructor**. Overlay cleared only by `clear_viz()` (DELETEALL) reached on a no-deviation tick (L707) / passthrough. Ticked every cycle by the `survey_line` ReactiveSequence (run_tasks.xml L409) while active, but when that branch stops ticking it **mid-deviation** (`was_avoiding_=true` — line complete / hover / task switch / recovery / cancel), the DELETEALL never fires. Markers have a 2 s `lifetime` (L334/351) as the intended safety net, but they're in a fixed map frame and **CAMP isn't honoring lifetime** → last AVOIDING markers persist at the last-deviation world spot. **Not real avoidance** — path output is nominal when not deviating; boat isn't tugged by the ghost. **Fix (proposed, ~10 lines, NOT applied):** factor the DELETEALL into a helper, call from a `halt()` override + destructor so the overlay clears deterministically on branch exit, independent of CAMP lifetime handling. Nuance: BT.CPP may not call `halt()` on an IDLE Sync node → destructor is the belt-and-suspenders. Needs rebuild + relaunch. Avoider's core deviation logic (corridor solver) looks sound — this is a viz-lifecycle bug, separate from the weight/tuning.

**2026-06-01 16:03 -04:00** — Operator: implement the fix + make the deviation clearly visible (~2 m). Implemented in `unh_marine_navigation` (field mode):
- **Overlay-clear fix** (`adjust_path_for_obstacles.cpp` + `.h`): `BT::SyncActionNode::halt()` is **`final` in BT.CPP 4.9** (verified in action_node.h L71) — can't override, so used the **destructor**. Factored the DELETEALL into a `clearOverlay()` member (no-op unless `was_avoiding_`); `clear_viz` lambda now calls it; added `~AdjustPathForObstacles()` calling it; cached `node_` + `robot_frame_` on first tick so the destructor can publish safely. **Coverage:** clears on teardown (tree reload / new goal / shutdown — dominant case). Gap: pure intra-tree branch switch (survey→hover, same loaded tree, node not destroyed) still relies on the 2 s marker lifetime, which CAMP ignores — full fix there needs CAMP to honour lifetime or a different viz arch (wrap-up note).
- **Visible deviation**: `run_tasks.xml` `obstacle_avoidance_weight` 0.15 → **1.0** (1:1 with line_following_weight). Magnitude is emergent (solver weighs line-follow vs obstacle cost) — can't pin exactly to 2 m via weight; 1.0 should give a clearly visible bend. Tune up/down after observing.
- Rebuilt `marine_nav_behavior_tree` (code, 23 s, clean compile); run_tasks.xml symlinked (live). **Ready for next relaunch.** Uncommitted pending operator relaunch + validate.

**2026-06-01 16:07 -04:00** — Operator: high-confidence (≥0.8) obstacles that appear only once should persist longer. Live set on `/bizzy/local_costmap/local_costmap`: `obstacle_prob_min` kept **0.8** (only ≥0.8 marks), `decay_half_life_s` 30 → **60** (slower passive fade of a single-sighting mark), `clear_floor` -1.25 → **-0.5** (a one-time hit resists active clearing by later water-looks). `obstacle_clamp` 15.0 (baked). Verified. **Trade-off:** real obstacles the boat passes will linger ~60 s+ as stale costmap marks — mitigated by the 0.8 gate (few FPs get in) + the overlay-clear fix; but the avoider could still divert around a genuinely-passed obstacle's lingering mark. Live-only, not baked — bake if it proves out.

**2026-06-01 16:09 -04:00** — Operator: grazing angle for 35–40 m range. R=h/tan θ, h=1.406 m → 2.0°=40.3 m, 2.1°=38.3 m, 2.2°=36.6 m, 2.3°=35.0 m. **Set `min_grazing_angle_deg` 1.5 → 2.1** (≈38 m) (verified). Note: 38 m < ±50 m local-costmap window, so cameras now under-fill the window's far edge (obstacles 38–50 m ahead won't grazing-project) — operator's deliberate near-field choice. Live-only.

**2026-06-01 16:10 -04:00** — Operator: bake these too. Updated `nav2_overlay.yaml`: `clear_floor` -1.25 → **-0.5**, `min_grazing_angle_deg` 1.5 → **2.1**, added **`decay_half_life_s: 60.0`**. Rebuilt bizzyboat_project11; installed copy verified. **Full baked sea_surface_layer config:** `obstacle_prob_min 0.8`, `obstacle_clamp 15.0`, `clear_floor -0.5`, `min_grazing_angle_deg 2.1`, `decay_half_life_s 60.0`. (CA box 10 m + `slowdown_ratio 0.5`; avoider `obstacle_avoidance_weight 1.0` + overlay-clear code fix already staged.) All uncommitted — pending operator validate.

**2026-06-01 16:19 -04:00** — Avoider update-tracking investigation (operator: path updates only for past parts, not ahead). Facts: nav restarted 16:11 (new build, weight 1.0 + overlay fix); overlay `/bizzy/survey_obstacle_avoidance` publishing ~7 Hz (avoider actively re-solving, NOT frozen/one-shot); `chatter_damping_weight` default 0.0 (no temporal stickiness); no passthrough WARN in log (TF/pose/costmap guards passing); costmap_raw input live ~1.4 Hz. Operator detail: at line start an avoidance path for the whole line appears; updates only for **past** parts. **Mechanism (code-grounded):** avoider solves deviation only for active range = stations AHEAD of boat + in rolling local costmap (L659-679); passed stations forced toward nominal (offset ~0) → that's the "past updating." Ahead re-solves each tick but only changes if ahead costmap marks change. **Leading hypothesis (testable, not asserted — wrong twice today):** the 16:07 persistence change (`decay 60`, `clear_floor -0.5`) freezes the ahead marks → ahead avoidance computed against static costmap → looks locked from line start. Proposed test: `decay_half_life_s`→15, `clear_floor`→-1.5; if ahead path then tracks, confirmed. Awaiting operator go.

**2026-06-01 16:39 -04:00** — Boat recovered; session moving to wrap-up. Operator: commit + push everything modified. **Committed/pushed this deployment (field mode, jazzy):**
- `unh_marine_navigation`: avoider overlay-clear-on-teardown fix (`adjust_path_for_obstacles.cpp/.h`); `obstacle_avoidance_weight` → 1.0 (`run_tasks.xml`).
- `unh_echoboats_project11`: `nav2_overlay.yaml` field tuning (sea_surface_layer `obstacle_prob_min 0.8`/`obstacle_clamp 15.0`/`clear_floor -0.5`/`min_grazing_angle_deg 2.1`/`decay_half_life_s 60.0`; CollisionSlowdown box 10 m + `slowdown_ratio 0.5`); this log.
- **Open / deferred to bag replay:** avoider ahead-path not tracking the costmap (updates only behind the boat) — root cause not pinned live; solver re-solves fresh each tick (no math freeze), so it's input-freshness or output-propagation — instrument at the dock. Bags: `bag_2026-06-01T13.45.10`, `15.22.12` (+ earlier). Collision-monitor dynamic-polygon + avoider param-exposure-on-lifecycle-node remain tested-PR work, not hot-patched.

**2026-06-01 16:42 -04:00** — Pushed (field, jazzy). Commit SHAs for dock reconciliation:
- `unh_marine_navigation`: `ea7277d` (overlay clear-on-teardown), `9ce5bb6` (obstacle_avoidance_weight 1.0).
- `unh_echoboats_project11`: `009ccea` (nav2_overlay.yaml field tuning), `95dfaa6` (this log).

**Dock TODO (consolidated from this session):**
1. **Avoider ahead-path not tracking the live costmap** (updates only behind the boat) — the one unresolved field symptom. Solver re-solves fresh every tick (DP rebuilt from scratch, `chatter_damping_weight=0` so no cross-tick carry), input is a live rolling costmap (`costmap_raw` ~1.4 Hz, `rolling_window=true`), overlay republishes ~7 Hz — so the freeze is NOT in the math. **Instrument:** add throttled logging in `tick()` of `[active_begin, active_end)`, a checksum of the ahead `obstacle_costs` rows, and the ahead `*solved` offsets, run against bags `bag_2026-06-01T13.45.10` / `15.22.12`. If the ahead cost-checksum changes but the offsets don't → output/render; if neither changes → the avoider's `costmap_raw` content ahead isn't actually refreshing (vs. the grid CAMP shows).
2. **Avoider live-param exposure** — declare `survey_avoidance.*` on the main bt_navigator **lifecycle** node at configure time (currently lazy, on the internal `_rclcpp_node` which doesn't service param RPCs → not CLI/rqt-tunable, `halt()` is `final` in BT.CPP 4.9).
3. **collision_monitor polygon shape is configure-time** (not runtime-reconfigurable; `slowdown_ratio` is). Make dynamic or accept relaunch-to-apply.
4. **CAMP doesn't honour marker `lifetime`** — the destructor DELETEALL (committed) covers teardown; intra-tree branch-switch still leaks until CAMP honours lifetime or the viz arch changes.
5. **Sim-validate all field-tuned values** before trusting (avoider weight ratio, costmap persistence, CA box). Dev-side wrap-up (Summary/Lessons) goes in the dev log, not here.
- **Reference geometry:** BizzyBoat forward OAK camera ≈ **1.41 m** above the `map_tide` sea-surface plane → grazing max-range `R = 1.41 / tan(θ)` (θ in `min_grazing_angle_deg`).

**2026-06-01 15:22 -04:00** — Operator: record another 30 min. Started `record_camera_topics.sh 1800` → `~/data/logs/bizzy_images/bag_2026-06-01T15.22.12_ffmpeg_seg` (background).

**2026-06-01 15:30 -04:00** — Operator approved config-bake + relaunch plan ("fix the avoider" both, "fix CA live params", weight 0.15; operator will relaunch when ready). Operator note: limited future deployments → live fixes need to happen; if config+relaunch doesn't work, will do a live code fix. **Decision: bake all tuning + avoider weight + CA box into config (no risky live code change to make them work; the dynamic-reconfigure CODE fixes — avoider param-exposure + collision_monitor dynamic polygon (SAFETY) — deferred to tested PRs, not hot-patched live).** Both repos are field mode. Edits:
- `unh_marine_navigation` `marine_nav_bt_task_navigator/behavior_trees/run_tasks.xml`: AdjustPathForObstacles instance → `obstacle_avoidance_weight="0.15"` (was unset → 0.02 C++ default; conservative, untuned-on-water).
- `unh_echoboats_project11` `bizzyboat_project11/config/nav2_overlay.yaml`: sea_surface_layer += `obstacle_prob_min 0.7`, `clear_floor -1.25`, `min_grazing_angle_deg 1.5`; CollisionSlowdown `points` 20m→10m box, `slowdown_ratio` 0.3→0.5. (obstacle_clamp 5.0 / decay 30 = defaults, not written. CollisionStop untouched.)
- Rebuilt `marine_nav_bt_task_navigator` (core_ws) + `bizzyboat_project11` (platforms_ws); **verified installed copies** carry all values (run_tasks.xml symlinked to source via --symlink-install; nav2_overlay.yaml copied). **Running stack unaffected until relaunch.** Commit deferred until operator relaunches + validates (then commit both field repos; if it fails, iterate / live code fix per operator).