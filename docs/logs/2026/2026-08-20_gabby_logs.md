# 2026-08-20 — gabby log (BizzyBoat, Isles of Shoals deployment prep)

Deployment issue: pending (backfill from a dev host) — no open deployment issue exists for the Isles of Shoals campaign; `git bug bug` on a refreshed cache shows the most recent deployment issue as 6270cd1 "Deployment 2026-08-06" (closed). Dev side to create + link.
Host: gabby
Side: field
Started: 2026-08-20 09:56 -04:00

## 2026-08-20

**2026-08-20 09:56 -04:00** — LOG STARTED at operator request. Phase: prep, for the Isles of Shoals deployment starting next week. Boat is tied up at the UNH pier and on the charger; operator is at CCOM standing up the ROC (remote operations centre). Plan for today is to bring the stack up once the ROC end is ready and verify updated tide, datum, S57 and other systems. Deployment mode NOT formally activated — this log was started directly rather than via `/start-deployment`, because the field side cannot create a deployment issue and none exists yet for the campaign. Header carries the 'pending (backfill from a dev host)' marker per the 2026-08-06 precedent, which the dev-side reconciliation scan picks up. Repo on jazzy, tree clean at start, field mode confirmed (origin git@gitcloud:field/unh_echoboats_project11.git).

**2026-08-20 08:32 -04:00** — WORKSPACE SYNCED AND REBUILT ahead of the day's verification work (`make sync` then `make build`, build report stamped 08:32:25). All repos on their default branches, all pulls clean fast-forwards, no local divergence anywhere. Two repos moved: **unh_echoboats_project11** 20f8b55 → 95fdbc7 — deployment logs for 2026-08-05 (dev), 2026-08-06 (dev + gabby), a new 2026-08-19 pandy log for the Shoals ROC operator station, and `docs/roc_operator_setup_2026-08-19.md`; **ccomjhc_project11** da4b771 → ec4a2d8 — dnsmasq host and DHCP entries for bizzyboat + operator (`configuration/dnsmasq/bizzyboat.hosts`, `operator.hosts`, `operator_dhcp.uci`) plus `docs/dns_naming.md`. Those two are the ROC-side network and operator-station material the operator is working from at CCOM today. Build: all 5 layers green — underlay 6/6, core 40/40, platforms 6/6, sensors 18/18, site 1/1, 71 packages total, warnings only (geodesy, manda_coverage, marine_autonomy, marine_nav_behavior_tree, marine_nav_bt_task_navigator, marine_nav_crabbing_path_follower, marine_sidescan_mosaic, udp_bridge, mru_transform, cube_bathymetry, depthai_marine, marine_radar_layer, marine_radar_tracker, marine_tools, simrad_halo_radar), zero errors. Nothing in the pulled changes touched source, so the rebuild was a refresh rather than a functional change.

**2026-08-20 09:53 -04:00** — BATTERY BASELINE — boat confirmed on the charger and fully charged, from the passive log `~/data/logs/bizzy_battery/battery_2026-08-20.csv`. Current reading 29.02 V at 09:53, logger live (last sample in the same minute the check ran). The charge curve on 2026-08-19 is textbook CC/CV: 27.05 V at 10:00, 28.72 at 12:00, 28.90 at 14:00, 28.98 at 16:00, knee at ~17:00, then flat. It has held the top of its range for ~17 hours — overnight hourly means 29.03–29.04, this morning 29.01–29.04. For scale, the highest voltage anywhere in the last 90 days of logs is 29.06, so this is sitting essentially at the ceiling. Well clear of the BATT_LOW_VOLT 23.0 head-home threshold.

**2026-08-20 09:53 -04:00** — BATTERY DATA CAVEAT worth carrying into the deployment: the log records **voltage only**. Every row this month has empty `current_a` and `remaining_pct` columns with `source=fcu`, so "on the charger" is an inference from voltage pinned at the ceiling for 17 hours (which a discharging bank would not do), not a direct measurement of charge current. Contrast the 2026-08-06 recovery entry, which had mavros rows carrying 0.0 A alongside the fcu voltage — that source is not present in today's file. If charge/discharge current matters during the Shoals campaign (e.g. confirming the charger is actually pushing, or measuring draw underway), that gap needs closing before it is needed rather than during.

**2026-08-20 09:53 -04:00** — MINOR, NOT ACTIONED — a ~0.03 V sag across this morning, 29.04 at 07:00 drifting to 29.01–29.02 by 09:50, with two samples at 28.98 at 08:33–08:34 that are the day's only readings below 29.00. Those two land about one minute after `make build` finished (08:32:25); compile load on gabby drawing briefly on the same bus is a plausible explanation, but this is a temporal coincidence noted as a hypothesis, NOT a measured cause — no current data exists to confirm it (see the caveat entry above). Magnitude is noise-level on a bank flat at the top of its range all night. Recorded so it is on the record, not because it warrants action.

**2026-08-20 09:53 -04:00** — LOGGING COVERAGE GAP in the battery record, flagged so it does not surprise anyone reading history: no files between `battery_2026-08-07.csv` (ends 07:43) and `battery_2026-08-17.csv` (39 samples, 16:23), then nothing until 2026-08-19 10:00. Presumably the boat was powered down across that window — consistent with the 2026-08-06 deployment having wrapped up — but that is inference, not something confirmed from a log. Within today's file, sampling is ~1/min with occasional single dropped samples (58–59 per hour against a nominal 60); no meaningful gaps.

**2026-08-20 09:56 -04:00** — VERIFICATION SCOPE for when the stack comes up, from the operator's stated list (tide, datum, S57, "and other systems"). One item flagged in advance rather than discovered underway: **chart_layer is currently DISABLED on this boat** — it was turned off deliberately (commit 61937ab) because the operating area had no S57 coverage, and with no charts an S57Layer marks everything as land. The Isles of Shoals is a different area and the coverage question needs re-asking: if Shoals has chart coverage, re-enabling chart_layer is a config change that should be made and tested at the pier, deliberately, not left to be found on the water. Not touched — flagging only, pending the operator's call. Related open items that bear on the same chart/costmap surface and are still unresolved from the Broadkill campaign: b6708fb (RCA ~60% lethal costmap at Broadkill/Lewes, ENC cell selection vs costmap resolution), 6ab6aff (local_costmap NO_INFORMATION renders FREE — uncharted water looks navigable), and the 2026-08-06 finding that `bathymetry_layer::evaluateCell` max-costs across co-located samples so a trusted surveyed-clear reading cannot pull a charted-caution cell down.

**2026-08-20 09:56 -04:00** — STACK IS DOWN and intentionally so — not started today, ROS untouched, per the operator's plan to bring it up once the ROC end at CCOM is ready. No node/topic queries have been run against the boat.

**2026-08-20 10:01 -04:00** — PRE-COMMIT / BRANCH-GUARD SNAG on gabby, resolved the low-effort way at operator instruction (no bandwidth to redesign it mid-prep). FINDING: every repo on gabby is field mode, INCLUDING the workspace repo itself (origin git@gitcloud:field/ros2_agent_workspace.git). That repo is on `main`, still lists `--branch main --branch jazzy --branch rolling` in `no-commit-to-branch`, AND is the one clone here with the pre-commit hook actually installed (`.git/hooks/pre-commit`, pre-commit-generated). Verified by running it rather than reasoning about it: `don't commit to branch... Failed, exit code 1`. So workspace-repo commits on gabby are blocked — the AGENTS.md "Field Mode hook caveat", live on the boat. Note the asymmetry that produced it: the clone that HAS the hook installed is the one whose config still carries the guard, while the clones with the guard disabled have no hook installed.

**2026-08-20 10:01 -04:00** — DECISION (interim, deliberately minimal): use `SKIP=no-commit-to-branch git -c user.name=... commit ...` when committing to the WORKSPACE repo on gabby. This is pre-commit's own supported per-hook skip, NOT `--no-verify` — verified that every other hook still runs and only the branch guard is suppressed. Chosen over the alternatives because it changes nothing on disk and has zero blast radius: editing `.pre-commit-config.yaml` would propagate to every clone including dev, and uninstalling gabby's hook would silently drop ALL linting on the one repo here that actually has hooks. Nothing was committed, edited, or uninstalled. PROPER FIX still owed on the dev side — align the workspace repo's guard with the field-mode reality the way `unh_echoboats_project11` did on 2026-07-29 (eb4a2de / d42df1b).

**2026-08-20 10:01 -04:00** — LATENT, LEFT ALONE: five field-mode project repos still carry `no-commit-to-branch` in their config — s57_tools, udp_bridge, unh_marine_navigation, seafloor_echoboat_project11, unh_marine_perception. None has the hook installed, so none is blocking anything today. WATCH ITEM for this campaign: **s57_tools is on that list and S57 is on today's verification slate** — an S57 hotfix at the pier commits fine as things stand, but would break the moment anyone runs `pre-commit install` in that clone. Same trap for the other four. Not touched, per the interim-minimal decision above.

**2026-08-20 10:01 -04:00** — PROCESS NOTE — `unh_echoboats_project11` on gabby still has NO pre-commit hook installed (`.git/hooks/pre-commit` absent), the same gap recorded on 2026-08-06. Today's log commit therefore ran unhooked; the suite was run manually against the file first via the workspace venv's pre-commit and passed (trailing-whitespace, end-of-file, merge-conflict, mixed-line-ending, large-files; yaml/xml/cmake/yamllint not applicable). This clone's config has the branch guard DISABLED repo-wide, so `pre-commit install` here is safe and would not block field commits to jazzy — still recommended, still not done.

**2026-08-20 11:23 -04:00** — AIS FEED FOUND LIVE on gabby, at operator request to listen on UDP 2125. ~1.2 datagrams/s of raw !AIVDM sentences arriving from 10.242.80.203 — a ZeroTier address, i.e. relayed from a pier-side shore receiver, NOT the boat LAN (enp7s0 192.168.20.5/24). Nothing was bound to 2125 beforehand, so nothing on this host was consuming it. 20 s decode: 10 distinct MMSIs across message types 1 (Class A position), 18 (Class B position), 21 (AtoN), 24 (Class B static), plus type 5 in the multi-fragment traffic. MID 366/367/368 (US commercial), 338 (US Class B/recreational) and 99x (aids to navigation) — real Piscataqua traffic, not a test pattern. Operator reports the receiver is at the pier and may reach the Shoals area, with a Shoals-based receiver also possible; working assumption is solid AIS coverage for the campaign.

**2026-08-20 11:23 -04:00** — AIS INGEST NEEDS NO NEW CODE — `nmea_relay` already implements a UDP reader (`udp_in.bind(('', port))`) and selects it on `input_type`; the shipped `marine_ais_tools/config/parameters.yaml` is merely set to `serial` / /dev/ttyACM0 / 38400. Flipping to `input_type: udp`, `input_port: 2125` uses a path that already exists. GAP: `ais_contact_tracker` is NOT in `ais_parser_with_nmea_relay.launch.py` (relay + parser only), so it needs adding with `/messages` remapped to `/ais/messages`. Neither change made — both are boat-side wiring, deliberately left for a separate deliberate change.

**2026-08-20 11:23 -04:00** — COVARIANCE GAP CLOSED in `ais_contact_tracker` (commit afaa775, marine_ais). The tracker carried a literal `# todo, figure out covariances` and left BOTH the pose covariance and the twist covariance zero-filled on every contact — consumers got position and velocity with no way to weight either, and a zero on the diagonal asserts a perfectly known quantity. `fillCovariances` now maps the transmitted position-accuracy bit (ITU-R M.1371: high => better than 10 m) onto x/y variance, and marks quantities AIS never carries (altitude, roll, pitch) or that a given report omitted with a large finite 'unknown' stand-in rather than zero. All sigmas are ROS parameters so the model can be revised against observation rather than recompiled.

**2026-08-20 11:23 -04:00** — IDENTITY-QUATERNION TRAP found by a failing test, worth recording because it is the kind of thing that reads as correct forever. The ROS default for `geometry_msgs/Quaternion` is the IDENTITY (w=1), not a zeroed quaternion. `ais_parser` only writes `pose.orientation` when the report carried a true heading (the decoder correctly nulls it at the AIS 511 'not available' sentinel), so an ABSENT heading was byte-identical to a REAL heading of due east — and no consumer could tell. MEASURED ON THE LIVE FEED: only 4 of 12 contacts report a true heading, so 8 would have been silently oriented east. Root-caused rather than worked around: `Navigation.msg` gains an explicit `heading_valid` flag set by `ais_parser`, which the tracker folds into the yaw covariance so downstream consumers (including the new layer) can distinguish the two. Additive field at the end of the message; nothing outside the marine_ais repo depends on it.

**2026-08-20 11:23 -04:00** — AIS COSTMAP LAYER IMPLEMENTED — new `ais_layer` package in the marine_ais repo (commit de4657f), siting the layer with its data source as `marine_radar_layer` and `s57_layer` do. Replicates CAMP's display-side interpolation, adapted for planning: each contact is dead-reckoned forward from its fix along COG at SOG and the swept region BETWEEN last-known and predicted position is painted, with an uncertainty envelope that grows with time since fix. Asymmetry is deliberate and documented in-code — over-painting costs a detour around empty water, under-painting costs a collision, so where the layer must guess it guesses wide. Gating: `navigational_status` blocks dead reckoning for moored/at-anchor/aground; expiry is per reporting class via `position_message_id`; a contact with no SOG/COG grows a reachable set at `unknown_speed` instead of being propagated; heading falls back COG-then-circle rather than fabricating an orientation; non-finite lat/lon/altitude are rejected before the ECEF conversion (the parser emits NaN for all three when a report omits them); previous-cycle bounds are re-requested so the master costmap clears where contacts have moved on. If the feed dies, contacts age out and the layer contributes nothing rather than stranding the planner.

**2026-08-20 11:23 -04:00** — VERIFICATION of the AIS work. Unit: 49 tests green in `ais_layer` (26 gtest cases over the geometry helpers, status gating, per-class expiry and the full interpolation model, plus a pluginlib load test) and 15 green in `marine_ais_tools`. Live: fed the REAL udp/2125 stream through the real parser and tracker in-process for 45 s — 59 sentences, 12 contacts, ZERO remaining zero-variance entries. Ran under `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp ROS_DOMAIN_ID=77` specifically so it could not touch the boat's normal graph (no zenoh router is up; the stack is still down). Real-feed findings that synthetic tests would not have produced: the position-accuracy bit genuinely varies across the fleet (25.00 vs 625.00 variance, i.e. sigma 5 m vs 25 m), and 3 of 12 contacts report NO SOG/COG at all — the reachable-set path is ~25% of live traffic, not a defensive corner case. Full workspace rebuild green afterwards, core 40 -> 41 packages.

**2026-08-20 11:23 -04:00** — PRE-EXISTING TEST FAILURES found and handled differently by scope. (1) `marine_ais_tools` `test_flake8` was ALREADY RED at HEAD — three errors (two Q000, one E225) on lines predating this work; verified by running flake8 against the HEAD copy of the file. Fixed, since they sit in the file being edited and left the package suite red. (2) `marine_ais_msgs` `xmllint` fails on `package.xml`: `test_depend` appears after `export`, which the package-format-3 schema rejects. NOT fixed — unrelated to the message change and in a file this work does not otherwise touch. One-line reorder for whoever picks it up.

**2026-08-20 11:23 -04:00** — NOT DONE, deliberately: `ais_layer` is NOT wired into any robot's nav2 config. Enabling it on BizzyBoat means adding the plugin to `nav2_overlay.yaml` and standing up the ingest chain, which changes boat behaviour and belongs in its own reviewed change rather than riding along with the implementation. Tracked as git-bug f619133 in marine_ais. Related open question still on the table from the earlier chart discussion: whether AtoNs (type 21, deliberately out of scope for this layer) should reach the costmap at all given `s57_layer` already accounts for charted aids.

**2026-08-20 11:44 -04:00** — AIS WIRED INTO THE BOAT (commit 30f53cb) at operator request, for testing at the pier when the stack comes up. Three parts. (1) `config/ais.yaml` — nmea_relay set to `input_type: udp`, `input_port: 2125`, since the receiver is ASHORE relaying over ZeroTier; marine_ais_tools' own default is a serial receiver on /dev/ttyACM0, so this overrides locally rather than changing the shared default. Also pins the tracker's uncertainty model (position_sigma_high/low, speed_sigma, rate_of_turn_sigma) so the boat's assumptions sit in one readable place. (2) `launch/ais_launch.py` — nmea_relay + ais_parser + ais_contact_tracker pushed into a shared `<ns>/ais` namespace; all three use RELATIVE topic names so the namespace alone wires them with zero remaps. Included from `core_launch.py`, on by default, `ais:=false` to disable. Note marine_ais_tools ships its own launch file but it omits ais_contact_tracker entirely, so it cannot produce contacts — hence the boat's own composition. (3) `nav2_overlay.yaml` — `ais_layer` added to BOTH costmaps, after bathymetry_layer and before inflation_layer.

**2026-08-20 11:44 -04:00** — COST CHOICE for the first AIS deployments, recorded because it is the one knob most likely to need flipping: `contact_cost: 252` (MAX_NON_OBSTACLE), NOT 254 (LETHAL). A vessel is a genuine obstacle and LETHAL is the eventual target, but the boat spends its time tied up among MOORED vessels that report on AIS, and a lethal ring around each neighbour at the pier can enclose the boat and leave the planner no way out of the berth. 252 keeps traffic strongly repulsive while leaving an escape route plannable, and keeps this away from the Broadkill mostly-lethal-costmap failure mode (b6708fb). RAISE TO 254 once two things have been watched: the boat still departs a berth cleanly with neighbours alongside, and underway traffic produces a detour rather than an abort. Layer ORDERING is not an arbitration question here — ais_layer combines with updateWithMax so it can only raise cost and can never un-write a charted hazard or a depth keepout; it sits before inflation_layer so it gets inflated. All other model parameters (per-class expiry, prediction cap, sigma scaling, envelope cap, sample count) deliberately left at defaults: the first bring-up is meant to MEASURE what the design paints against real traffic, not to confirm a guess.

**2026-08-20 11:44 -04:00** — LAUNCH CHAIN VERIFIED END-TO-END on gabby before the stack comes up, run under `RMW_IMPLEMENTATION=rmw_cyclonedds_cpp ROS_DOMAIN_ID=77` specifically so it could not touch the boat's normal graph (no zenoh router is up). Launched the REAL `ais_launch.py`: all three nodes came up as /bizzy/ais/{nmea_relay,ais_parser,ais_contact_tracker}, topics resolved to /bizzy/ais/{nmea,messages,contacts,atons} exactly as the namespace design intended, and a real contact published — MMSI 368027920, Class B (position_message_id 18), 43.06053 N 70.70566 W (Piscataqua, off the pier), navigational_status 15 (undefined). The published `frame_id: ais` is the proof that ais.yaml actually reached the nodes: both that frame_id and the UDP transport differ from the package defaults, so data flowing at all means the override took. Test processes stopped afterwards and UDP 2125 confirmed released. Full workspace rebuild green (core 41, platforms 6).

**2026-08-20 11:44 -04:00** — WHAT TO WATCH at stack bring-up, in priority order. (1) DEPARTURE FROM THE BERTH — the 252-vs-254 question above; if the boat cannot plan out of the pier with AIS on, that is the cost choice, not a bug, and `ais:=false` or `ais_layer.enabled:=false` is the immediate stand-down. (2) WHAT IT PAINTS AT REST — moored neighbours should appear as compact patches (no dead reckoning, envelope = sigma_scale x position sigma, i.e. ~10 m for a high-accuracy fix, ~50 m for a low-accuracy one). Anything much larger at the dock means the status gating is not doing what is expected. (3) CONTACTS WITH NO SOG/COG — measured at 3 of 12 on the live feed, these grow a reachable set at unknown_speed (default 5 m/s) instead of being propagated, so a long-quiet one can open to a large circle; `max_envelope` (default 500 m) is the cap and is the knob if that is too aggressive. (4) CPU — the local costmap is already coarsened to 0.5 m / 350 m because the 4-OAK sea_surface raytracing could not hold 5 Hz; ais_layer adds per-contact rasterisation, so watch for "missed control rate" warnings returning. (5) That a ZeroTier drop degrades to no-AIS rather than to stale ghosts.

**2026-08-20 12:52 -04:00** — AIS LAYER PAINTING ON THE COSTMAP — operator: "I see red blob at the coast guard pier!" First visual confirmation of ais_layer contributing to the live costmap. Namespace fix verified after relaunch: nodes now at /bizzy/ais/{nmea_relay,ais_parser,ais_contact_tracker} and `/bizzy/ais/contacts` reports Publisher count 1 / Subscription count 2 (was 0/2 before the fix, which is why nothing painted on the first bring-up).

**2026-08-20 12:52 -04:00** — BLOB IDENTIFIED — MMSI 366999563, range 52 m, bearing 123 deg from the boat at 43.071976 N 70.711472 W, i.e. approximately 43.07172 N 70.71094 W. Consistent with the Coast Guard pier position the operator reports. Status underway/engine, NO SOG/COG transmitted. Age 3 s at time of reading, position sigma 5.0 m (high-accuracy bit set on this report), envelope 16 m — so a compact patch of roughly 26 m, NOT the costmap-wide blanket the unbounded defaults produced. The unknown_speed 1.0 / max_envelope 150.0 bounding is doing what it was set to do.

**2026-08-20 12:52 -04:00** — TWO CAVEATS ON THE PAINTED SIZE, both worth watching rather than acting on yet. (1) NO STATIC REPORT received for this contact yet — `static_info.name` is empty and `footprint` has no points, so the layer is falling back to `default_vessel_radius` (10 m). A Coast Guard cutter is considerably larger than that, so the blob currently UNDERSTATES the vessel; it will grow to the true hull outline when a type 5 / type 24 static message arrives and the tracker computes the footprint. Do not read the current blob size as the layer's steady-state output. (2) THE ACCURACY BIT VARIES BETWEEN REPORTS FROM THE SAME VESSEL — this MMSI read position sigma 25.0 m on an earlier pass and 5.0 m on this one, so the envelope will visibly breathe between reports (32 m vs 16 m at low age). Expected given the model reads the transmitted bit per report, but it means a pulsing blob is normal, not a fault.

**2026-08-20 12:52 -04:00** — NaN POSITION SEEN IN LIVE DATA, guard confirmed working. Contact MMSI 338543271 is publishing with NaN latitude/longitude (ais_parser emits NaN when a report omits them). ais_layer rejects it before the ECEF conversion via the explicit finite check, so it never reaches the costmap. Worth recording because the ad-hoc range-check script written to answer the operator's question did NOT guard, and the contact slipped through its window filter — NaN comparisons are false, so `abs(dn) > HALF` did not exclude it. Same class of bug the layer guard exists to prevent, demonstrated on real data within minutes of it going live.
