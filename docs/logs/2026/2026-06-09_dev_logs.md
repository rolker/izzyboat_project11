# 2026-06-09 — dev log (BizzyBoat deployment #245)

Deployment issue: https://github.com/rolker/unh_echoboats_project11/issues/245
Host: deadpool
Side: dev
Started: 2026-06-09 12:48 -04:00

## 2026-06-09

**2026-06-09 12:48 -04:00** — Sidescan check from deadpool during student lunch: no live sidescan visible on this host — no ROS topic (sim down, boat sidescan not bridged to deadpool), no Garmin flow on :50220 (imagery) / :50227 (control), and the newest capture bag is yesterday's (`bag_2026-06-08T14.49.00_sidescan_raw`, last written 06-08 14:52). No 2026-06-09 sidescan bag present on deadpool. Capture/live feed is boat-side (gabby/Garmin); not SSHing without operator go-ahead.

**2026-06-09 13:15 -04:00** — Sidescan tested on the water during the lunch window (operator-run). Result: **works mostly** — some artifacts seen in the data, otherwise looks good (operator-reported). Artifact characterization deferred to wrap-up / the #239 decode dig if a bag from this test is available.

**2026-06-09 16:38 -04:00** — deadpool `/` hit **100% full** (5.1 G free of 1.9 T) — recording-host disk pressure during ops. Mitigation: cleared `~/.ros/log` (9.6 G, 16k stale run-log dirs back to 2025-10) → 15 G free. Untouched: `~/data` (bags/sonar/survey data). Further safe headroom available if needed: `~/.cache` ~5 G, colcon build/install/log ~5.8 G, stale merged worktrees ~1.4 G.

**2026-06-09 18:09 -04:00** — gabby battery-voltage log captured for the full ops day: `~/data/logs/bizzy_battery/battery_2026-06-09.csv`, 641 samples (~1/min) 06:40:58–17:31:04 EDT, `source=fcu`. Voltage 28.90 V start → 27.86 V end; day range 26.61–29.04 V. `current_a`/`remaining_pct` empty (expected — no current meter; voltage is the real signal).

**2026-06-10 (dev-side analysis)** — **Sidescan artifact root-caused** from
`~/data/logs/bizzy_sidescan/bag_2026-06-09T12.30.06_sidescan_raw` (60 s live
transmit; GCV-20, all 3 channels ~10.6 Hz, clean cadence, 0% saturation, no
dropouts — acquisition is healthy). The "solid constant bright lines" are
**packet metadata mis-decoded as sonar samples** in the `garmin_sidescan`
driver:

- A scan line = 7 UDP packets accumulated by `PingAssembler`
  (`garmin_sidescan/decode.py`). `echo_layer()` (GCV-20) / `dark_layer()`
  (GCV-10) slice each packet's samples as `[FH+4 : SH/SHS or end-of-packet]`,
  but each packet's layer is actually `[leading per-ping sub-header (first
  packet only)] + [real samples] + [~12–16 B trailer]`. Neither the leading
  sub-header nor the trailer is stripped.
- **Trailer** (`43 8X 96 03 | 49/4a XX YY | 52 80 10 | 5a …`, with `XX` an
  incrementing chunk counter) lands at each of the 7 packet-concatenation
  boundaries → the evenly-spaced bright vertical stripes. Verified the exact
  bytes match the constant columns (down ch: idx 302=`0x0049`, 303=`0x8052`,
  304=`0x5a10` = `49 00 52 80 10`).
- **Leading sub-header** (a constant `XXYY` pair repeated ~19 samples; down
  `b2 ad…`, side `32 b3…`, varies per ping) → the bright near-range band.
- Affects **all three channels** (down-look 0x0d has no `SH` terminator so
  reads to packet end; side-scan 0x0e/0x0f finds `SH` but the trailer sits
  *before* it, so it's still included).
- Also unpopulated (separate, lower-priority): `sample_rate`=0 (only derived
  when a range was commanded), `ping_info.frequency`=0 (not in payload; needs
  `freq_*_hz` params), `sample0`=0.

Fix lands in `marine_tools/garmin_sidescan/decode.py` (strip per-packet
trailer + first-packet leading sub-header). Reference decoder + packet notes
exist at `~/garmin_sidescan` (`gcv_decode2.py`, `GCV20_LIVE_NOTES.md`).
Analysis scripts: `.agent/scratchpad/ss_*.py` (workspace repo). Needs a
`marine_tools` issue + worktree to fix. The mid-run range-scale changes
(variable samples/ping) are a separate, lower-priority display concern.

## Summary

Second student-operations day; first day running the Garmin sidescan + M3
on the water under students. Cross-host integration of the gabby (boat) and
salmon (operator station) field logs:

- **Sidescan (GCV20) — works, with artifacts.** Brought up boat-side (gabby):
  driver connected, safety interlocks held transmit until AML SV read valid
  (1495 m/s, in the 1400–1600 window), then transmit ON across all three
  channels (port/stbd/down) at ~10.6 Hz. Operator assessment on the water:
  "works mostly," some artifacts in the imagery. Auto-logged to
  `~/data/logs/bizzyboat_sonar/`; targeted debug captures to
  `~/data/logs/bizzy_sidescan/` via the new `record_sidescan_debug.sh`.
  **Artifact characterization is the open dig** (below).
- **M3 multibeam — soundings flow, grid does not.** `detections` 10 Hz →
  `soundings` 9 Hz both healthy with valid TF, but `cube_bathymetry` (active,
  fed, valid TF) emitted **zero `m3/grid`** messages. Undiagnosed (ruled out
  lifecycle + TF startup race).
- **M3 not bagged by sonar_logger.** Logger still subscribed only to the
  decommissioned DeltaT topic; M3 products went unrecorded on the ROS side.
  Mitigated in-session with an ad-hoc bag (346 MB, in-water
  `detections`+`soundings`+TF+odom, `~/data/logs/bizzy_m3/`); durable config
  fix (add `m3/detections`) staged in `bizzyboat.yaml` but needs a
  rebuild + relaunch to take effect.
- **CA false-positive e-stops.** Camera sea-surface reflex pointcloud trips
  `ca_safety` on a *single* spurious point in the stop polygon (no min-points
  / persistence / confidence filter exists). Mitigated by shrinking the stop
  box 5.0×4.0 → 3.0×3.0 m (runtime only — reverted by stack shutdown).
- **Two "core wedge" incidents (salmon).** Two independent mid-run failures
  (~12:31 and ~16:04 EDT), both cleared **only by restarting core** (Zenoh
  router restart did nothing): (1) core stopped delivering *any* subscription
  data while discovery stayed healthy; (2) the command relay
  `command_bridge_sender` froze, replaying one stale frame with a frozen
  embedded timestamp. Link/UDP bridge healthy throughout both.
- **deadpool disk 100% full** mid-ops (recording host); mitigated by clearing
  9.6 G of stale `~/.ros/log` (→15 G free); `~/data` untouched. gabby `/home`
  was never a constraint (~1.7 TB free).
- **Battery:** full-day voltage trend 28.90 → 27.86 V (day range 26.61–29.04 V)
  over ~11 h; no swap (charged in place).

## Lessons Learned

- **One spurious segmentation point should not trip a hard stop.** The reflex
  CA path has no points-persistence/confidence floor; the only runtime lever
  is stop-box geometry, which trades away real-obstacle reaction distance. The
  durable fix is a min-points/persistence filter — follow-up issue.
- **Core can wedge mid-run with discovery still healthy.** Diagnostic tells:
  discovery-OK-but-no-subscription-data points at core (not the link); a
  frozen embedded timestamp on a relayed topic means the relay is stuck, not
  the link. Restarting core is the only known recovery — core needs a
  liveness/health signal and `command_bridge_sender` a stale-frame watchdog.
- **Re-point sonar_logger off the decommissioned DeltaT.** New sonars (M3)
  silently go unrecorded until the logger YAML is updated and the logger is
  rebuilt + relaunched; `install(DIRECTORY)`-copied config won't hot-reload.
- **Track the deployment phase.** During recovery the boat-side agent
  over-investigated the intended stack-down (silent marine channel, empty node
  graph, no router) as a fault instead of asking — phase had moved to recovery.

## Follow-ups (for the wrap-up PR / new issues)

1. **Sidescan artifact characterization** — dig into the captured bags
   (`~/data/logs/bizzy_sidescan/`, `~/data/logs/bizzyboat_sonar/`); carries the
   #239 decode dig forward. *(In progress this session.)*
2. **M3 `cube_bathymetry` produces no grid** despite active state + 9 Hz input
   + valid TF — undiagnosed. New issue.
3. **sonar_logger M3 config** — staged in `bizzyboat.yaml`; needs rebuild +
   relaunch to go live, and the dead `deltat/soundings` line removed.
4. **CA min-points/persistence filter** — durable fix so one spurious point
   can't e-stop; persist the 3.0×3.0 stop-box to YAML if kept meanwhile.
5. **core-bringup robustness** — why core wedges mid-run; liveness signal +
   `command_bridge_sender` stale-frame watchdog. New issue.
6. **mercat data access** — rclone-over-sftp or SMB mount from salmon
   (drops gabby as proxy). Decision pending.
