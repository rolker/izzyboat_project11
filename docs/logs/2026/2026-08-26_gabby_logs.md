# 2026-08-26 — gabby log (BizzyBoat deployment — issue pending)

Deployment issue: pending (backfill from a dev host)
Host: gabby
Side: field
Started: 2026-08-26 08:35 -04:00

## 2026-08-26

**2026-08-26 08:35 -04:00** — Deployment mode activated on gabby (field side, origin git@gitcloud:field/unh_echoboats_project11.git). Issue-less start per operator - no git-bug lookup run, header carries the pending marker for dev-side backfill. unh_echoboats_project11 on jazzy, tree clean at start.

**2026-08-26 08:35 -04:00** — udp_bridge admission-control fix pulled and built on gabby. Fast-forwarded jazzy ed372a8 -> 93096b2 (63 commits, merge of udp_bridge PR #77 / issue #52) - the fix for the 2026-08-25 AIMD admission-control collapse logged yesterday. Core changes: stop the AIMD admission collapse, restore message-level admission granularity, clamp the refractory gate at both ends, bound statistics windows above as well as below, coerce integer literals for double params at the declaration site, and a new WARN when a runtime rate limit makes admission control inert. link_headroom_fraction is retired to a presence-tripwire. AIMD cap now surfaced on the connection diagnostic. Built clean in core_ws with --symlink-install; only pre-existing -Wpedantic flexible-array-member warnings in packet.h. udp_bridge_node reinstalled 08:30. NOT yet relaunched - running bridge still has the old binary.

**2026-08-26 08:35 -04:00** — udp_bridge test suite on gabby: 296 tests, 15 skipped, 1 distinct failure - and the failure is pre-existing, not from the #52 fix. All new/touched suites green: test_admission_control 33/33, test_admission_field_replay 3/3, test_message_atomicity 6/6, test_connection_diagnostic 9/9, test_node_name_limits 18/18. (colcon's summary says 2 failures; it is the same test double-counted across CTest Test.xml and the gtest XML.)

**2026-08-26 08:35 -04:00** — FINDING (deferred to wrap-up, not blocking): udp_bridge's qos_design.md matching rules do not hold under the RMW we actually run. The one failing test is QosMatchingIntegration.BestEffortPublisher_DoesNotMatchReliableSubscriber (test_qos_matching_integration.cpp:191), which asserts a BEST_EFFORT publisher must NOT match a RELIABLE subscriber. Confirmed rather than assumed: the file is untouched by this pull (git log ed372a8..93096b2 on it is empty; last changed in ef542ff), and re-running that single test under RMW_IMPLEMENTATION=rmw_cyclonedds_cpp PASSES, with DDS logging 'Last incompatible policy: RELIABILITY_QOS_POLICY'. Under the active rmw_zenoh_cpp it FAILS - zenoh does not enforce the reliability incompatibility that DDS does, so the mismatch the test treats as impossible does occur. The test's own failure message anticipates exactly this: 'If this is now allowed under the active rmw, the rest of qos_design.md needs revisiting.' BizzyBoat runs zenoh, so the documented QoS matching table is DDS-shaped and does not describe our runtime. Operator declined filing an issue for now - recorded here for wrap-up.

**2026-08-26 08:35 -04:00** — Config check for the retired udp_bridge param: link_headroom_fraction appears nowhere in live configs across layers/main. Only two hits, both in udp_bridge's own example_params.yaml and both commented out (doc note line 122, '#link_headroom_fraction: 0.2' line 141). No boat config will trip the new presence-tripwire WARN on the next bridge relaunch.

**2026-08-26 08:44 -04:00** — PLAN FOR TODAY (operator): continue mapping around Appledore while implementing fixes. Second consecutive Appledore day - continues the 2026-08-25 outing.

**2026-08-26 08:45 -04:00** — Pre-transit NTRIP check - no action needed, same as yesterday. ccomjhc_project11 clean on jazzy, bizzyboat_ntrip.yaml still the NH default on MaCORS (macorsrtk.massdot.state.ma.us:10000, mountpoint RTCM3MSM_IMAX). No overnight site edit to revert, and Appledore is still NH water so this is the correct caster. Confirm the RTCM 1005 baseline once on station (expect roughly 35-40 km offshore vs 27.4 km at the Judd Gregg berth) - check the 1005 baseline, not the fix flag.

**2026-08-26 08:48 -04:00** — WIRE-FORMAT VERIFICATION before restarting udp_bridge, operator asked whether the #52 fix needs a synchronized both-ends restart. ANSWER: NO - the wire format is unchanged and a one-sided restart is safe. Evidence: (1) every wire-carrying file is untouched by ed372a8..93096b2 - packet.h, wrapped_packet.h/.cpp, remote_node.h, defragmenter.h/.cpp all have empty git log over the range; the diff touches only connection, statistics, udp_bridge and the new connection_diagnostic header. (2) Zero serialization edits in the whole diff - no PacketType, serialize/deserialize, memcpy, reinterpret_cast or htons/ntohs/htonl/ntohl lines changed. (3) The one field with cross-node meaning kept identical semantics: the receive rate echoed back in BridgeInfo, which the congestion detector divides against locally-sent bytes. Commit 9a7079c only RENAMED its window - 'double five_secs_ago = time - 5' became 'time - kReceiveRateWindowSeconds' with the constant defined as 5.0. Same number, so an old peer's reported rate is measured over the same 5 s window as a new peer's; no filter skew across the version boundary. (4) The 'version-skewed peer' comment in the diff is a defensive clamp on effective_rate_limit_ (a float near UINT32_MAX casts to 0 and admits nothing), which makes the new code MORE tolerant of a stale peer, not less.

**2026-08-26 08:51 -04:00** — POST-RESTART VERIFICATION - the #52 fix is live and the cap is NOT collapsing. New binary confirmed running, not inferred: udp_bridge_node pid 1158519 started 08:49:49, binary mtime 08:30:10, so the process postdates the build. Link state read at 08:50:47 on connection vpn to pandy.vpn.bizzy.p11.lan:4200 - effective_rate_limit 1500000, i.e. sitting at the FULL maximum_bytes_per_second cap with no backoff. Compare yesterday 07:52 during the event: 523216, backed off but not at floor. Drops are zero in all three categories - message success 311983 B/s dropped 0.0 (yesterday 355801 success against 87793 dropped), overhead success 7311 B/s dropped 0.0 (yesterday 826 dropped), resend success 1904 B/s dropped 0.0. received_bytes_per_second 3193. CAVEAT ON SCOPE: this is roughly one minute after restart and the boat is paused at the end of a survey line, so offered load is not necessarily representative of an active line. The meaningful test is whether the cap holds once mapping resumes and coverage_tiles is flowing again. WATCH ITEM: resend_giveup_count already reads 8 this soon after start; it read 0 during yesterday's 07:52 event. Could be re-establishment churn, not called as anything yet.

**2026-08-26 08:52 -04:00** — RTCM/RTK CHECK on station at Appledore, operator requested. CORRECTIONS FLOWING AND THE FIX LOOKS GENUINE, but the reference-station identity is NOT yet confirmed - see the gap below. Measured: /bizzy/sensors/rtcm publishing at 6.0-8.0 Hz. GPS1 raw reports fix_type 6 = RTK Fixed with 28 satellites visible, h_acc 16 mm and v_acc 25 mm - centimetre-grade accuracy, which corroborates a real short-baseline fix rather than a held-over flag. Position 42.98415 N 70.60999 W, i.e. on station at Appledore. Caster config re-verified earlier this morning as the NH MaCORS default, tree clean. GAP: I could not read the 1005/1006 reference-station identity or the baseline distance this time. The mavros topics that would carry it (gpsstatus/gps1/rtk, gps_rtk/rtk_baseline) are not publishing, /diagnostics has no ntrip entry, and there is no helper script in the repo for it - yesterday's station-42-at-26.2-km reading came from somewhere I have not re-identified. Per the 2026-08 Delaware lesson the 1005 baseline is the check that actually matters and the fix flag is not a substitute, so this is an open item, not a pass. Parsing 1005 out of /bizzy/sensors/rtcm directly would close it.

**2026-08-26 09:40 -04:00** — Started a 120 s ad-hoc camera capture at operator request, using bizzyboat_project11/scripts/record_camera_topics.sh 120 (the repo's own helper, not a custom recorder). Captures the four OAK ffmpeg streams plus segmentation raw+compressed, TF, tf_static, robot_description, diagnostics and /bizzy/local_costmap/costmap as an mcap bag under ~/data/logs/bizzy_images/. Recording starts shortly after the udp_bridge restart at 08:49:49, so this bag also spans the first minutes of the #52 admission-control fix running under load.

**2026-08-26 09:42 -04:00** — Camera capture complete and verified on disk: ~/data/logs/bizzy_images/bag_2026-08-26T09.40.25_ffmpeg_seg, 58 MB single mcap plus metadata.yaml, recorder ran 09:40:25 to 09:42:25 and stopped cleanly (rosbag2 flushed cache, exit 0). All four OAK ffmpeg streams, segmentation raw+compressed with camera_info, TF, robot_description and diagnostics subscribed.

**2026-08-26 09:43 -04:00** — BRIDGE UNDER REAL LOAD at 09:42, ~52 min after the restart and during active survey - MIXED RESULT, cap healthy but resend giveups climbing. GOOD: effective_rate_limit still 1500000, the full cap, with no backoff at all despite message success now 411106 B/s (up from 311983 at the 08:50 idle line-end reading). Dropped is 0.0 in all three categories - message, overhead and resend. That is the #52 admission-control fix doing exactly what it was meant to: yesterday at a LOWER offered load of 355801 B/s the cap had already collapsed to 523216 and was refusing 87793 B/s. CONCERN: resend_giveup_count has gone 8 -> 1102 over those ~52 minutes, roughly 21 per minute. Yesterday's 07:52 event reading was 0. So the link is still losing packets and the resend machinery is abandoning some of them - a DIFFERENT failure mode from yesterday's admission-refusal drops, and one the #52 fix does not address. Zero admission drops plus nonzero giveups means loss is happening on the wire rather than at the gate. Not yet characterised as steady versus bursty; a delta over a fixed window would settle that. Flagging as an open item - operator-visible symptom would still be occasional feed gaps.

**2026-08-26 09:45 -04:00** — OPERATOR REPORT at 09:44: 'this is much better since cameras have not dropped out yet' - no camera dropouts observed from the operator station since the udp_bridge restart at 08:49:49. That is the symptom that drove yesterday's whole investigation (intermittent RGB feed loss, worsening to 15-20 s vanishes offshore by 07:52), and it has not recurred in the ~55 min since the #52 fix went live. Operator observation is the ground truth on the symptom; it agrees with the measured link state at 09:42 (cap at full 1500000, zero dropped bytes in all three categories at a higher offered load than yesterday). CONTEXT FOR THE GIVEUP COUNTER: this also reframes the resend_giveup_count 8 -> 1102 noted above. Giveups are climbing without producing an operator-visible dropout, so on today's evidence they are not translating into feed loss. Carrying it to wrap-up as a subclinical signal to understand rather than an active fault - no action taken during ops.

**2026-08-26 09:50 -04:00** — cube_bathymetry live-tiles fix pulled and built on gabby. Fast-forwarded jazzy to 5c87f84 (33 commits, PR #136 / issue #112, 'Field import: opt-in dirty sub-window tile publishing'). This is the fix for the coverage_tiles bandwidth identified yesterday as the single largest link contributor. Built clean in sensors_ws with --symlink-install (only pre-existing unused-variable and GDAL warnings). TESTS: the merge commit itself carried the caveat 'tests unrun' - they are now run and green on gabby, 602 tests, 0 errors, 0 failures, 70 skipped. NOT yet restarted; cube_bathymetry is a lifecycle node and these params are read at configure.

**2026-08-26 09:51 -04:00** — CONSUMER-COMPATIBILITY VERIFICATION for the cube_bathymetry sub-window change, done because publish_dirty_subwindow now DEFAULTS TO TRUE and its own parameter descriptor warns that 'a consumer that ignores the window fields would paint a patch over the whole tile and corrupt the operator's coverage view, so verify (a) before enabling against a new consumer.' VERDICT: the currently-deployed consumer already supports it - no operator-side update needed. Consumer is marine_web_view/coverage_renderer.py in unh_marine_autonomy. Checked against the LOCAL running version, not the 44 unpulled commits: (a) it patches at the window offset - reads msg.window_row/window_col and calls tile_util.apply_window, which raises rather than silently clipping if the window does not fit; (b) it dequantizes per message - decode_band is passed band.scale/band.offset/band.nodata off each message, so the per-window auto-ranged backscatter scaling is handled. It also independently defends the hazard the producer flags: it computes 'complete' from window_row/col/width/height and only advances possession on a WHOLE tile, so a partial does not let the cache claim the catalog's newest version while holding a hole. Comment in that file anticipates this exact change - 'keeps the guarantee if sub-window pushes ever arrive'. No message interface changed in the pull (no .msg/.srv edits), so the wire type is unchanged. Boat config sets neither publish_dirty_subwindow nor subwindow_refresh_interval, so defaults apply: subwindow enabled, refresh 60 s. Note 0 would disable healing and make a lost patch permanent - it is not set, and should not be.

**2026-08-26 10:04 -04:00** — RESTART METHOD for the new cube_bathymetry build, operator asked whether a single-node restart suffices or the launch group must go. ANSWER: single-node restart is enough, but it is a TWO-STEP - the process comes back by itself, the lifecycle does not. Read from cube_bathymetry_launch.py: the node is a LifecycleNode with respawn=True and respawn_delay=2, and the configure/activate pair is a LifecycleTransition action sitting in the LaunchDescription. That is a ONE-SHOT launch action which already fired when the launch started; it is not an OnProcessStart handler, so it will NOT re-fire on respawn. Killing the process therefore gets it restarted in ~2 s off the same command line - which points at the installed executable, now the new binary, and at the installed bizzyboat.yaml - but it comes up UNCONFIGURED and must be driven manually with ros2 lifecycle set <node> configure then activate. On configure it re-reads params (picking up publish_dirty_subwindow=true) and reloads the persisted grid from draft_dir, currently /home/field/data/world/depths, so the survey so far is not lost. This is the exception to the usual do-not-kill-launched-nodes rule: params come from an installed yaml rather than a /tmp/launch_params_* file, so the respawn re-execs cleanly. Current state: both /bizzy/sensors/m3/cube_bathymetry and /bizzy/sensors/deltat/cube_bathymetry read active [3]; only the m3 one carries live tiles.

**2026-08-26 10:05 -04:00** — Disabled the stray DeltaT cube_bathymetry instance in the launch, at operator request. VERIFIED IT WAS ACTUALLY RUNNING FIRST, not assumed from the roster: /bizzy/sensors/deltat/cube_bathymetry read active [3] with live process pid 1158059, gridding nothing since the Imagenex 837 is out of use. Commented out the cube_bathymetry_launch.py IncludeLaunchDescription inside the sensors/deltat GroupAction in bizzyboat_project11/launch/perception_launch.py (lines 135-143), leaving the deltat_launch.py include above it untouched and a dated comment saying why and how to re-enable. File parses (python3 -m py_compile). The installed copy under platforms_ws/install is a SYMLINK to the source, so no rebuild is needed - the change is live for the next launch. NOT yet in effect on the running stack: the existing deltat cube node is still up and will stay up until perception_launch is next restarted. Change is uncommitted, batching per the urgency contract.

**2026-08-26 10:09 -04:00** — CUBE RESTARTED ONTO THE NEW BUILD at operator request, M3 instance only. Targeted pid 1158062 (__ns:=/bizzy/sensors/m3) specifically, leaving the deltat instance alone; sent SIGINT rather than SIGTERM so the grid persisted cleanly. Launch respawned it as pid 1183136 at 10:06:41. Behaved exactly as the launch analysis predicted: it came back UNCONFIGURED [1], confirming the LifecycleTransition is one-shot and does not re-fire on respawn. Drove it manually - configure -> inactive [2], activate -> active [3], both 'Transitioning successful'. New params confirmed live: publish_dirty_subwindow True, subwindow_refresh_interval 60.0. coverage_tiles publishing at 0.199 Hz, i.e. the 5 s refresh drain tick. NOTE ON A FALSE ALARM I RAISED MID-RESTART: I read the installed binary's mtime as 2026-06-18 and flagged it as stale. That was my error - stat does not dereference symlinks by default, and install/.../cube_bathymetry_node is a symlink to build/cube_bathymetry/cube_bathymetry_node whose real mtime is 2026-08-26 09:49. The restarted node is on this morning's build.

**2026-08-26 10:10 -04:00** — SUB-WINDOW PUSH MEASURED IN THE FIELD - the node instruments its own saving and it is large. Four consecutive 30 s windows from the launch log after the 10:06:41 restart: 927334 cells sent against 6451200 the whole-tile mode would have sent = 14.4 percent (~3.71 MB vs ~25.80 MB uncompressed); then 16.8 percent; then 0.1 percent (7331 cells against 5529600 - a quiet window); then 16.7 percent. So coverage_tiles is costing roughly one sixth of what it did, and far less when the survey is not turning up new cells. Every window reports '+0 re-sent whole by the heal', so the 60 s whole-tile refresh has not yet needed to fire. This is the topic measured yesterday as the single largest link contributor. Bridge at 10:09 after the change: effective_rate_limit still 1500000 with zero dropped in all three categories. WATCH ITEM UPDATE: resend_giveup_count 1102 at 09:42 -> 4184 at 10:09, so roughly 118/min against the earlier 21/min - it is accelerating, not settling. Still no operator-visible camera dropout reported. Unchanged as a wrap-up item, no action taken during ops. Also noted, from the ORIGINAL 08:49 startup and not the restart: one WARN 'No transform earth <- bizzy/m3 (at ping time or latest); dropping ping -- grid will not update'. Not recurring after the restart.

**2026-08-26 10:13 -04:00** — OPERATOR WAS RIGHT - the cube restart silently de-advertised its topics from udp_bridge, and they needed re-advertising by hand. Found after the 10:06:41 cube restart: /bizzy/sensors/m3/cube_bathymetry/coverage_tiles and coverage_catalog each had Publisher count 1 but SUBSCRIPTION COUNT 0, i.e. the bridge was no longer subscribed, and neither topic appeared anywhere in bridge_info. Tiles were being published and going nowhere. Re-advertised both over the operator/vpn connection with the values already persisted in bizzyboat.yaml (coverage_tiles queue_size 2 period 1.0; coverage_catalog queue_size 2 unthrottled) via the remote_advertise service, type udp_bridge_interfaces/srv/Subscribe. VERIFIED RATHER THAN TRUSTING THE RESPONSE - both subscription counts went 0 -> 1, and topic_statistics now shows real send traffic to destination_node operator on connection vpn: coverage_tiles 0.264 msg/s, send success 926.2 B/s with failed 0.0 and dropped 0.0, average_fragment_count 4.0; coverage_catalog send success 318.2 B/s. SYSTEMIC POINT FOR WRAP-UP: the advertisement is persisted in bizzyboat.yaml, yet it did not survive the PUBLISHER restarting - only the bridge restarting would re-read that config. So any node restart silently drops its outbound topics from the bridge with no warning anywhere, and the operator only finds out when a panel goes stale. Worth an issue against udp_bridge. ODDITY, UNEXPLAINED: even now, with both topics advertised and demonstrably sending, neither appears in the bridge_info topics list (still 501 lines, no cube or m3 entries) while topic_statistics shows them fine. So bridge_info is not a reliable place to check what is advertised - use topic_statistics.

**2026-08-26 10:17 -04:00** — Re-enabled coverage_catalog and coverage_tiles in the udp_bridge vpn topics_list, at operator request - this is the DURABLE half of the runtime remote_advertise done at 10:13. Root cause of the missing advertisement is now clear and it was not the cube restart alone: both topics were commented OUT of the vpn connection's topics_list in bizzyboat_project11/config/bizzyboat.yaml, stood down on the water 2026-08-25 with the note 'RESTORE once tiles are patched or the link budget grows'. cube_bathymetry#112 landed exactly that patching today, so the stand-down condition is met. Uncommented lines 410-411 and rewrote the comment block to record the stand-down, the restore, and the measured numbers behind it. manda_coverage_swath DELIBERATELY LEFT stood down - it was grouped into the same budget stand-down but #112 does not shrink it, so restoring it is a separate unmeasured decision for the operator, and the config now says so. YAML re-validated after the edit; installed copy under platforms_ws/install is a symlink to source, so the change is live for the next bridge restart with no rebuild. Current session already carries the topics via the runtime advertise, so no restart is needed to see tiles now.

**2026-08-26 10:17 -04:00** — MY ERROR, CAUGHT AND REVERTED, recorded because it touched a live config. My first attempt at the topics_list edit deleted 229 lines of bizzyboat.yaml. Cause: I located the edit region by searching for the string 'STOOD DOWN 2026-08-25', assuming it was unique. It is not - it also appears at line 180 on the CELL connection (no SIM card fitted), so the search matched the cell block and the replacement wiped everything from there down to the vpn RESTORE line. Caught immediately because the post-edit YAML validation failed at line 179 with a block-collection error, and git diff --stat showed 229 deletions against an expected handful. Reverted with git checkout -- on the file, confirmed it parses again and that git status shows it clean, then redid the edit against explicit line indices with an assert on the exact content of every line touched before writing. Final diff is 24 insertions / 12 deletions confined to the one region, YAML valid, and topics_list parses with both coverage entries present. No other file was affected and the running stack was never touched by the bad edit - it only ever existed on disk in the source tree.
