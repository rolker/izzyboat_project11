# 2026-08-27 — pandy log (BizzyBoat deployment — issue pending)

Deployment issue: pending (backfill from a dev host)
Host: pandy
Side: field
Started: 2026-08-27 07:26 -04:00

## 2026-08-27

**2026-08-27 07:26 -04:00** — DEPLOYMENT MODE ACTIVATED on pandy, field side (origin git@gitcloud:field/unh_echoboats_project11.git), repo on jazzy, clean tree. Issue-less start per ros2_agent_workspace#533 at operator direction - git-bug lookup skipped in favour of getting logging live; header carries the canonical pending marker for dev-side backfill anchored on 2026-08-27. Operator confirmed this is a NEW deployment, not day 2 of 2026-08-26 (that log remains open and issue-less, also awaiting dev backfill). Note vs 2026-08-24: git-bug IS now installed on this host (/home/field/.local/bin/git-bug), so the plain-git-refs workaround from that deployment is no longer needed if an issue lookup is wanted later.

**2026-08-27 07:30 -04:00** — STALE LIVE COVERAGE IN CAMP AFTER GABBY RESET - ROOT CAUSE FOUND, catalog publisher is ABSENT. Operator reports live coverage was reset on gabby but camp still paints the old data. camp's stale-tile removal is anti-entropy driven ENTIRELY by the catalog: SonarLiveCacheLayer::handleCatalog (sonar_live_cache_layer.cpp:511) reconciles the cache to exactly the catalog set (ADR-0006 D4) and prunes both the in-memory tile + its GPU texture (:547-555) and the disk copy (:556, fs::remove at :562). No catalog, no prune - there is no other path that removes a tile. VERIFIED LIVE, not inferred: 'ros2 topic info -v /bizzy/sensors/m3/cube_bathymetry/coverage_catalog' reports Publisher count: 0 with 1 subscriber, while .../coverage_tiles reports Publisher count: 1 and 2 subscribers. So the relay is still streaming tiles into camp but NOTHING is publishing the catalog, which is why the reset never propagated. Note this is a DIFFERENT failure from the one logged 2026-08-26 18:14 (camp TRANSIENT_LOCAL vs relay VOLATILE QoS mismatch) - the live subscriber now reads RELIABLE/VOLATILE, so the mismatch is not what is biting today; the producer is simply not there.

**2026-08-27 07:35 -04:00** — CORRECTION TO THE 07:30 ENTRY - THE CATALOG IS FLOWING; THE QOS MISMATCH IS THE REAL CAUSE, as gabby's agent said. The 07:30 'Publisher count: 0' was a stale discovery snapshot taken while the renderers were restarting, and the RELIABLE/VOLATILE subscriber I read to dismiss the mismatch was coverage_renderer's, NOT camp's. Re-measured: /operator/udp_bridge DOES publish /bizzy/sensors/m3/cube_bathymetry/coverage_catalog (Publisher count: 1) and it carries real data - an explicit VOLATILE subscriber gets a catalog immediately (level 11, row 35580, col 28001, stamp 1787830488). The endpoint QoS is the whole story: PUBLISHER udp_bridge = RELIABLE/VOLATILE; SUBSCRIBER camp = RELIABLE/TRANSIENT_LOCAL (sonar_live_cache_layer.cpp:237, 'qos.transient_local().reliable()'). TRANSIENT_LOCAL subscriber vs VOLATILE publisher is an INCOMPATIBLE pair, so camp's subscription never matches and handleCatalog never fires - no reconcile, no prune, stale coverage persists across a gabby-side reset. Reproduced both ways on the same topic: --qos-durability transient_local timed out with nothing in 15 s, --qos-durability volatile returned a message at once. coverage_renderer subscribes VOLATILE and is unaffected, which is why only camp shows the stale data. This is exactly the failure recorded 2026-08-26 18:14; it was never fixed, only described.

**2026-08-27 07:37 -04:00** — DISPATCHED BG AGENT TO FIX THE CAMP CATALOG QOS at operator instruction (option 2 of the two candidates offered). Scope: change camp's coverage_catalog subscription from RELIABLE/TRANSIENT_LOCAL to RELIABLE/VOLATILE at sonar_live_cache_layer.cpp:233-240 so it matches the udp_bridge republisher, correct the now-false comment that justifies transient-local by the BOAT-side producer QoS, re-examine the camp#169 last_catalog_ buffering whose comment rests on the transient-local latched-sample premise, and pin the durability with a test since nothing currently does. camp is FIELD MODE (origin git@gitcloud:field/camp.git) so the agent commits directly to jazzy with hooks, no worktree and no PR; reconciliation to GitHub happens at wrap-up via /import-field-changes. Agent is explicitly barred from restarting camp or any running node - camp is live in front of the operator, so the RESTART IS THE OPERATOR'S CALL and the fix does not take effect until then. Also barred from touching the live_tile_cache, ~/data, any other repo, and from host admin. Known tradeoff accepted: under VOLATILE camp no longer gets a latched catalog on join and must wait for the next publication.

**2026-08-27 07:45 -04:00** — CAMP CATALOG QOS FIX COMMITTED AND PUSHED - 532b6df34db071ebff2a76c7911a846421ed5310 on camp jazzy, pushed a6863ac..532b6df to git@gitcloud:field/camp.git, tree clean. VERIFIED INDEPENDENTLY, not just taken from the agent's report: HEAD is that SHA, jazzy is level with origin/jazzy, the committed body reads 'qos.durability(rclcpp::DurabilityPolicy::Volatile).reliable()' with depth 1, and grep finds NO remaining transient_local anywhere under live_coverage/. The profile was extracted into a free function catalogSubscriptionQos() so it can be pinned by a test. 4 files, +127/-17. Build passed (real C++ compile, 28.5 s); camp's suite passed 312 tests, 0 failures, 1 pre-existing unrelated skip (test_gggs_render, GL offscreen). New test/test_sonar_live_catalog_qos.cpp asserts VOLATILE + RELIABLE + KEEP_LAST depth 1 - nothing pinned this QoS before, which is why the bug survived. camp#169 last_catalog_ buffering was re-examined and left UNCHANGED, correctly: catalog_sub_ is reset only in the destructor, not by disableLiveCoverage(), so buffering still works, and with no latched sample it is now the ONLY path from a catalog seen while disabled to a reconcile on enable - its comment was the only false part and was rewritten. FIX IS NOT LIVE: the running camp still holds the old image; a RESTART IS REQUIRED and remains the operator's call.

**2026-08-27 07:50 -04:00** — MANUAL CACHE WIPE IS UNNECESSARY - A PLAIN CAMP RESTART SELF-CLEANS, superseding the wipe I proposed at 07:33 and 07:46. Three measured facts make the prune do the work: (1) camp WARM-LOADS the disk cache into tiles_ BEFORE subscribing, deliberately so the in-memory state is seeded and the warm-load cannot race a callback (sonar_live_cache_layer.cpp:323-326, ADR-0006 D3/D4) - so the stale disk tiles ARE in the reconciler's known set when the first catalog lands, land in to_prune, and are erased from memory AND from disk (fs::remove at :562). (2) The catalog publishes on a steady 5 s cadence, not only on change - measured 'ros2 topic hz' at 0.200 Hz, min 4.984 s max 5.013 s over a 25 s window - so the VOLATILE tradeoff of having no latched sample costs about FIVE SECONDS after subscribe, not an indefinite wait, which was the open risk when we accepted option 2. (3) camp respawns automatically, respawn=True respawn_delay=5, at marine_autonomy/launch/operator_ui_launch.py:109-110, so the operator's stop is self-healing. Net: restart camp and the stale coverage prunes itself within roughly 10 s, with NO destructive command run, NO added downtime beyond the respawn delay, and the still-valid tiles kept rather than re-fetched over the field link. Letting the prune run also EXERCISES the very mechanism that was broken, so a clean display is direct evidence the QoS fix works. Running camp pid 705500 started 06:57:19, before the 07:44 rebuild, so it does still hold the old image.

**2026-08-27 07:55 -04:00** — QOS FIX CONFIRMED WORKING, BUT THE OVERVIEW PYRAMID IS EXEMPT FROM THE PRUNE BY DESIGN - that is what the operator is still seeing. camp restarted 07:52:02 as pid 728001 and its coverage_catalog subscription now reads RELIABLE/VOLATILE, matching the udp_bridge publisher, so 532b6df is live and the subscription matches. The prune DID fire: the source cache went 107M -> 44M and every surviving file has an mtime at or after 07:52:09, i.e. the pre-reset fine tiles were deleted and re-requested. But 41M of the remaining 44M is the overviews/ subdir, and only about 3M is fine tiles (5 tiles at level 11). VERIFIED IN THE SOURCE, not inferred: the prune loop in handleCatalog deletes only fs::path(cache_dir_)/stem - the cache-dir ROOT, fine tiles only - and erases only from tiles_; it never touches cache_dir_/overviews/ nor overview_tiles_. foldIntoParent's comment at sonar_live_cache_layer.cpp:728-730 states the rule outright: overview tiles 'persist under the overviews/ cache sub-dir (the uma sidecar layout) and are NEVER ADDED TO THE RECONCILER (a local derived product; prune-on-absence must not touch them)'. Compounding it, folding is ACCUMULATIVE - overview.tile.foldChild(fine) only ever folds data IN, and there is no un-fold, so a stale contribution cannot be subtracted once folded. The only erase of overview_tiles_ (line 839) is inside evictIfOverBudget, which is memory-pressure eviction ordered by distance, not correctness. NET CONSEQUENCE, and this is a real design gap worth a camp issue at wrap-up: a boat-side store reset can NEVER be reflected in camp's overview pyramid by any code path. Old coverage is baked in permanently and reappears on every restart via the warm-load at :422-436, which is exactly what happened at 07:52:09.

**2026-08-27 07:58 -04:00** — DISPATCHED SECOND BG AGENT - MAKE PRUNE-ON-ABSENCE PROPAGATE INTO CAMP'S OVERVIEW PYRAMID, at operator instruction. Brief: the catalog is authoritative, so the overview tiles that SHOULD exist are exactly the ancestors of the catalogued fine indices; an overview with no catalogued descendant is wholly stale and should be removed from memory, GPU and disk, while one with only SOME surviving descendants holds folded contributions that cannot be subtracted and must be rebuilt from its survivors. Four constraints given: do NOT nuke the pyramid wholesale on routine churn (naively invalidating the full ancestor chain reaches level 0, which covers the world, so an ordinary prune would destroy everything); follow the camp#134 GL-context discipline when releasing overview textures or leak GPU memory; keep the evictIfOverBudget / overviewResidentBytes accounting intact; and establish what children are actually available before assuming a rebuild is possible, preferring REMOVAL over a knowingly-wrong tile because a zoomed-out gap is honest and stale coverage is not. Explicitly permitted to land the removal half alone if the rebuild half is too risky, provided it says so in the report AND in a comment - a correct partial fix beats a shaky complete one - but not to skip it silently. Also to correct the :728-730 comment, which currently asserts the buggy rule as intended design. Tests required to pin all three behaviours including the over-deletion guard, since nothing pinned this before. Same hard bars as the first agent: no restarting or killing camp (pid 728001 is live in front of the operator), NO touching the operator's live_tile_cache - its disposition is the operator's call, not the agent's - nothing under ~/data, no other repo, no host admin. Told to read 532b6df first for the comment style and the dated-marker convention that replaces issue numbers on this non-GitHub origin.

**2026-08-27 08:30 -04:00** — OVERVIEW-PYRAMID PRUNE FIX COMMITTED AND PUSHED - af5e378 'fix: propagate catalog prune-on-absence into the overview pyramid' on camp jazzy, pushed 532b6df..af5e378 to git@gitcloud:field/camp.git, tree clean and level with origin. 5 files, +1001/-28, including an ADR-0010 update and a 487-line test file. VERIFIED IN THE COMMITTED SOURCE, not taken from the agent's report: reconcilePyramid() is called from handleCatalog at :647 after the fine prune; the live set is the ancestors of the catalogued indices UNIONED with the ancestors of fine tiles still held locally, which is what keeps it consistent with the reconciler's per-tile timestamp gate so a held-but-too-new tile does not get its ancestors swept from under it; and the sweep walks the overviews/ DIRECTORY via indexFromStem at :915, not just the resident map - necessary because evictIfOverBudget drops overview entries from memory WITHOUT deleting their disk copies, so an evicted stale overview is invisible in overview_tiles_ and gets warm-loaded back on the next restart, which is precisely the mechanism that kept resurrecting the pandy pyramid. PRUNE-GATE PARITY IS THE IMPORTANT SAFETY CATCH, at :867-872: generation_time == 0 disables prune-on-absence in the reconciler per ADR-0008 D4b, and without matching that gate here an UNSTAMPED catalog would have deleted the ENTIRE pyramid while every fine tile stayed put. It has its own test. Both halves landed - removal and rebuild-from-surviving-children - with rebuilt entries mutated in place so no QOpenGLTexture is destroyed outside a current context. Agent reported build clean and 317 tests 0 failures against a re-measured 312 baseline, the +5 being 4 new cases plus one CTest aggregate entry; it also reported verifying the over-deletion guard by temporarily implementing the naive chain-invalidation design and confirming the new test FAILS against it. Known gap it could not close, recorded in ADR-0010's deferred list: content that went stale in a session where the descendant overviews were already correctly removed is undetectable, since overviews carry no provenance.

**2026-08-27 08:39 -04:00** — af5e378 SWEEP CONFIRMED WORKING ON THE LIVE HOST - THE STALE PYRAMID IS GONE, but the operator still cannot use the display, and the remaining cause is a THIRD and separate issue. camp restarted 08:33:04 as pid 771711 on the 08:26:05 binary. Overviews went 43M -> 11M and, decisively, ZERO of the 19 surviving overview files predate the restart - every one was written 08:33:09 to 08:33:13, i.e. the sweep deleted every pre-restart overview and rebuilt the ancestors from surviving children exactly as designed. Disk state is now level 0 through 8 with one tile each, 4 at level 9, 6 at level 10, over 11 fine tiles at level 11. So what remains on screen is NOT stale data. It is the ADR-0010 D1 / camp#160 LOD FALLBACK: itemsIntersecting (:1573-1578) appends EVERY resident overview tile coarse-to-fine and then the fine tiles on top, at EVERY zoom, with no zoom-based level selection - the comment's premise is 'where a fine tile is present it fully covers its parent', but a fine tile covers only a quarter of its parent's area, so wherever fine coverage is sparse the coarse parent paints through. The apex compounds it: kApexProtectLevel (:1174-1181) deliberately protects the coarse levels from eviction to guarantee zoom-out coverage, so a level-0 tile spanning the globe at the fixed TiledRasterTile edge - cells on the order of a hundred km across, folded from just 11 fine tiles - is painted over the chart at working zoom as a handful of enormous blocks. That matches the operator's 'huge rectangles occlude too much' exactly. This is PRE-EXISTING DESIGN, not a regression from 532b6df or af5e378. Operator is falling back to qinsy to confirm coverage meanwhile. NOT dispatching a third fix until the operator confirms what they are seeing is coarse blocky low-resolution coverage rather than survey lines in places the boat did not go today - I have twice now concluded this was fixed and been wrong, so the next step is their eyes on it, not another inference.

**2026-08-27 08:52 -04:00** — THE 650 m BLOCK IDENTIFIED BY MEASUREMENT - IT IS A LEVEL-0 APEX CELL. Operator reports a solid block about 650x650 m covering a good portion of the survey area, blocky but roughly in the right place. Measured every pyramid level's actual geometry by opening the cached GeoTIFFs with GDAL (read-only) rather than estimating: every tile is 960x960 cells at EVERY level, so ground cell size doubles per level up. Level 11 fine = 0.3 x 0.5 m cells, 1264 valid; L9 = 1.3 x 1.8 m, 6854 valid; L8 = 2.7 x 3.6 m, 16818 valid; L6 = 10.6 x 14.5 m, 1281 valid; L3 = 85.5 x 115.8 m, 51 valid; L1 = 344.7 x 463.1 m, 9 valid; L0 = 667.3 x 926.1 m with just 4 VALID CELLS. The operator's ~650 m block matches the level-0 cell width of 667.3 m almost exactly. So the mechanism is fully pinned: itemsIntersecting appends every resident overview level coarse-to-fine at every zoom with no scale-based selection, and kApexProtectLevel deliberately shields levels 0-2 from eviction to guarantee zoom-out coverage, so four level-0 cells each two thirds of a kilometre across - each one the MEAN of everything folded beneath it, which is meaningless at survey zoom - are painted across the survey area under the fine tiles and over the chart. Not staleness and not a regression from 532b6df or af5e378; this is the ADR-0010 D1 LOD fallback behaving as written. Dispatching a third bg agent for scale-aware level selection, with the measured table handed over so it can sanity-check its own work.

**2026-08-27 09:01 -04:00** — OPERATOR REDESIGNED THE LOD RULE AND IT IS BETTER THAN MINE - redirected the running agent mid-flight. My brief was scale-aware level selection (drop overview levels whose cells are far coarser than the view can show). The operator's rule instead: coarse data may be drawn when zoomed in ONLY as a placeholder for a fine tile KNOWN TO EXIST at the current view level but not yet loaded, CLIPPED TO THAT TILE'S OWN FOOTPRINT; the full extent of a lower-res tile is never painted; where no fine tile is known to exist, nothing is drawn. Three reasons it is the stronger rule. (1) It is EXACTLY COMPUTABLE rather than heuristic - the catalog is authoritative for 'tiles known to exist at this level' and evicted_fine_indices_ gives which are not resident, so the draw set is derived, not tuned. (2) It SUBSUMES THE ZOOM-OUT CASE with no threshold: zoomed out, nearly every catalogued tile is non-resident, so the union of their footprints IS the survey coverage, and the ADR-0010 D1 guarantee falls out of the rule instead of needing a protective threshold I would have had to tune. (3) It fixes the defect AT THE ROOT - my scale rule would have stopped level 0 drawing at survey zoom, but at any zoom where a coarse level DID qualify it would still smear that level across its full 640 x 889 km extent including never-surveyed water; the operator's rule never paints outside real coverage at all. IMPLEMENTATION COST IDENTIFIED AND HANDED TO THE AGENT: RasterFieldItem (raster_field_source.h:27) carries a texture plus a geographic extent and NO texture-coordinate window, so the renderer stretches the whole texture across the given bounds - clipping to a sub-footprint cannot be done by narrowing west/east/south/north, it needs an additive UV sub-rect defaulting to 0,0,1,1 honoured in the renderer, and that struct and renderer are SHARED with the chart RasterLayer and the GGGS tile layer, so backward compatibility for existing callers must be verified by reading them rather than assumed. Told the agent to say so in its report if it thinks the rule is wrong rather than silently implementing something else.

**2026-08-27 09:09 -04:00** — LIVE WEB DISPLAY - CURRENT APPROACH AND THE INTENDED FUTURE SHAPE, per operator. A separate agent session is working the live web display today. CURRENT, as an interim: previous day's coverage is supplied by EXTERNALLY PRODUCED coverage GeoTIFFs, and the web display shows only TODAY'S live coverage on top of them. FUTURE PLAN, stated by the operator as the direction to aim at: THE WORLD STORES SHOULD SUPPLY THE PREVIOUS DAY'S COVERAGE, replacing the externally produced GeoTIFFs. Recording this now so the interim is not mistaken for the intended design later. MY NOTE, not the operator's words - this is the same direction recorded yesterday 2026-08-26 18:08 for the bathy store gate ('this should consume existing world-store layers, not a bespoke mosaic'), and it very likely inherits the SAME BLOCKER documented there: the world store's chart layer is z6/7/8 while a 1 m survey is L10, cube_bathymetry has NO cross-level fallback for chart (only reference gets the seedNewTile level-walk fallback, #115), and the offline tools report neither primed nor level-mismatched counts, so a level mismatch fails SILENTLY. Whether that specific gap binds the web-display path as it binds the gate path has NOT been verified - it is a flag for wrap-up, not a finding. Worth treating the two as one piece of work at wrap-up rather than two, since both want the same thing: an existing world-store layer consumed directly instead of a hand-flattened product.

**2026-08-27 09:24 -04:00** — PLACEHOLDER-DRAW FIX COMMITTED AND PUSHED - 0fc6408 'fix: draw coarse coverage only as a clipped placeholder for a missing tile' on camp jazzy, af5e378..0fc6408 to git@gitcloud:field/camp.git, tree clean and level with origin. 8 files, +959/-59. Implements the OPERATOR'S rule, not my scale rule: planDraw() resolves exactly two contributions - for each pending fine index the finest resident ancestor overview painted over THE FINE INDEX'S extent through the sub-rect of that ancestor it occupies, plus every resident fine tile whole. An overview is never painted over its own extent again and there is no scale term anywhere. 'Known to exist' = catalogued_fine_ (assigned wholesale from each catalog since a complete catalog is a snapshot, maintained even while disabled to match the camp#169 buffer) union evicted_fine_indices_, minus tiles_. VERIFIED THE RISKY PART MYSELF - the shared-renderer change is genuinely additive: RasterFieldItem gains u0/v0 defaulting to 0.0f and u1/v1 to 1.0f, i.e. the whole texture, and grep finds NO assignment of those fields in raster_layer.cpp or gggs_tile_layer.cpp, so the chart and GGGS callers keep today's behaviour by default. Agent reported reading all three call sites plus the renderer test helpers and finding every one default-constructs then assigns members with no aggregate initialisation, which is what makes added fields safe. Sub-rect window computed from GGGS index arithmetic with a row-to-v flip rather than floating-point extent ratios; a descendant that does not resolve inside its ancestor draws nothing rather than guessing. 323 tests 0 failures against a 317 baseline, and the agent reports MUTATION-CHECKING rather than assuming: 3 of 4 placeholder tests fail against the pre-fix draw list, 2 of 4 fail against an unclipped placeholder, and the renderer test fails against the pre-change texcoords. ADR-0010 D5 replaced and D1 restated - the protected apex is no longer DRAWN at zoom-out, it is the guaranteed resident SOURCE a zoomed-out view samples through the missing tiles' footprints. NOT LIVE: needs a camp restart.

**2026-08-27 09:25 -04:00** — UNEXPLAINED CAMP DEATH AND RESPAWN AT 08:47:49 - CAUSE NOT ESTABLISHED, surfacing rather than guessing. The camp the operator restarted at 08:33:04 (pid 771711) is GONE; a camp is running as pid 775563 started 08:47:54 whose PARENT IS 705460, the same long-running 'ros2 launch bizzyboat_project11 operator_ui_launch.py' - so this was the launch RESPAWN (respawn_delay=5), not a manual restart, and camp therefore died at about 08:47:49. What I checked: pid 771711's rosout log ends mid-stream at stamp 1787834869 (08:47:49) on a routine repeating NavSource::trySubscribe line with NO crash message, NO stack trace and NO clean-shutdown record - consistent with a hard kill or a crash that never reached the #217 handler. NOT OOM: no 'Killed process' or oom entries in syslog or dmesg around that time, and the box is comfortable at 22 GiB available of 31 GiB. No crash dump found under the camp app-data dir. So the cause is genuinely unknown and I am NOT attributing it. Candidates not distinguished: an external kill (note ListAgents shows a busy peer session project11-86 and an idle Remote Control session literally named 'Reset live sonar coverage', neither of which I have touched), or a silent crash. TIMELINE FACT THAT RULES OUT THE OBVIOUS SUSPECT: 08:47:49 PRECEDES the third agent's first build, so the agent's rebuilds did not cause it. SEPARATE HAZARD THE AGENT RAISED AND WORTH RECORDING ANYWAY - because --symlink-install is used, an in-session colcon build OVERWRITES the .so files a live camp has mapped. Pid 775563 survived all three of its builds, but building camp against a running camp is a live-ops hazard, not a safe no-op, and should be treated as one for the rest of this deployment.

**2026-08-27 16:07 -04:00** — TIED UP AT THE DOCK - operator called end of on-water ops and asked to get ready to wrap up. Sterile-cockpit relaxes from here; deeper analysis is now the expected work. Beginning field-side wrap-up PREPARATION only - the full /wrap-up-deployment skill runs from a DEV host (it verifies dev-side access, needs gh to find and close the deployment issue, and drives /import-field-changes), and pandy is field side with a gitcloud origin, so it cannot run here.

**2026-08-27 16:11 -04:00** — OPERATOR END-OF-DAY ASSESSMENT - 'Good survey day, fixes we did help with monitoring survey coverage'. Recorded as the operator's own words. FACTUAL CONTEXT I VERIFIED rather than inferred, since no log entries were written between 09:25 and 16:07 and the survey itself is otherwise unrecorded here: the camp binary was last built 09:20:35, which is the build carrying all three of today's fixes (532b6df catalog QoS, af5e378 pyramid prune, 0fc6408 clipped placeholder draw). Every camp instance that ran from about 09:26 onward therefore carried all three. So the operator's assessment is against the FULL fix set, not a partial one, and the coarse-placeholder rule they designed was live for the working day. NOT recording any mechanism beyond that - which specific fix helped most, or how the display behaved in detail, was not stated and is not being extrapolated.

**2026-08-27 16:12 -04:00** — CAMP DIED SILENTLY AT LEAST FIVE TIMES TODAY, SAME SIGNATURE EVERY TIME - TOP RCA CANDIDATE FOR WRAP-UP. The 08:47:49 death I flagged as unexplained was not a one-off. Reconstructed from the rosout log files, whose mtimes mark when each instance stopped writing: pid 771711 ended 08:47, 775563 ended 09:26, 799871 ended 11:33, 822198 ended 12:47, 834026 ended 14:19. Five deaths, intervals of roughly 1h14m to 2h46m, no fixed period. VERIFIED THE SIGNATURE IS IDENTICAL on three of them, spot-checked rather than assumed: each log's final line is the routine repeating 'pending_velocity_topic_: /bizzy/sensors/sbg/imu/velocity' from NavSource::trySubscribe, cut off mid-stream, and a case-insensitive grep for terminate, segmentation, signal, crash and abort returns ZERO hits in any of them. So camp is not shutting down cleanly and is not reaching the #217 crash handler either - it simply stops. Launch respawns it each time (respawn=True, respawn_delay=5), which is why the operator had a usable day and why this went unnoticed: the failure is INVISIBLE FROM THE COCKPIT except as a display that blanks and repopulates. Ruled out: OOM (no syslog or dmesg entries, 22 GiB of 31 GiB available). NOT ruled out and NOT being guessed at: a silent crash in camp itself, an external killer, or something in the GL or driver path. NOTE THIS IS INDEPENDENT OF TODAY'S THREE FIXES - the first death at 08:47:49 predates the af5e378 and 0fc6408 builds entirely, so the pattern is pre-existing and not a regression we introduced. ALSO OBSERVED, unexplained and worth a look: as of 16:11 there are TWO live camp processes, 847978 started 14:19:28 and still running, alongside 865649 started 16:11:11 - a respawn appears to have fired while the previous instance was still alive, which would leave two camps competing on the same topics and cache directory.
