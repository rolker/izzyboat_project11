# 2026-06-29 — gabby log (BizzyBoat deployment — #356)

Deployment issue: #356 <!-- backfilled at wrap-up; field-started issue-less (#533) -->
Host: gabby
Side: field
Started: 2026-06-29 10:14 -04:00

**2026-06-29 10:14 -04:00** — deployment mode activated on gabby (field side, issue-less start #533); repo on jazzy, clean tree

**2026-06-29 10:18 -04:00** — OBSERVATION: many segmentation false positives on costmap BEHIND boat (aft). Operator suspects recent switch to SBG IMU — possible misalignment vs FCU IMU (attitude bias would tilt the sea-surface projection).

**2026-06-29 10:23 -04:00** — CONFIRMING DATA — SBG ekf_quat vs FCU mavros/imu/data, 8 samples while underway/turning (yaw swept 127deg->27deg). Constant SBG-minus-FCU offset: roll +2.03deg (std 0.08), pitch -0.84deg (std 0.12), yaw +0.12deg (std 0.46). Tiny std despite active maneuvering => fixed mounting misalignment, mainly ~2deg roll. Heading well-aligned. Consistent with operator hypothesis: attitude source now ~2deg off the camera-extrinsic reference, tilting the sea_surface projection -> aft false positives. Real fix = ~2deg roll correction to SBG->base extrinsic (wrap-up).

**2026-06-29 10:31 -04:00** — ARCH FINDING (wrap-up candidate): mru_transform does NOT apply the IMU sensor->base_link URDF rotation to ATTITUDE. updateOrientation (mru_transform.cpp:145) broadcasts the sensor orientation quat directly as north_up_base_link->base_link. It DOES rotate angular velocity (:183-197) and linear velocity (:265-292) and position lever-arm (:68-88) via the static TF, but the attitude is passed through raw. => Putting a 2deg roll offset for the SBG in URDF would NOT fix the costmap false positives; the sea_surface projection uses the raw attitude. Fix routes: (1) correct SBG internal/driver alignment so output attitude is already base_link (node's design assumption), or (2) make mru_transform compose sensor->base rotation into attitude (code change + sim verify). NOT a live retune.

**2026-06-29 10:46 -04:00** — FCU IMU stability check (/bizzy/mavros/imu/data, 20s, 200 samples): rate 10.0Hz steady, max gap 101ms (no dropouts), 0 bad/non-unit quats, orient cov[0]=1.0 (valid), max consecutive RPY step 0.13-0.21deg (no glitches), gyro mean 0.97 max 2.51 deg/s (gentle window). Stream healthy -> good candidate to set FCU back as primary attitude source. Caveats: 10Hz (likely slower than SBG was feeding); window was low-dynamics, not stress-tested under hard maneuvering. Reverting to FCU should remove the ~2deg projection tilt causing aft costmap false positives (extrinsics effectively cal'd to FCU reference).

**2026-06-29 10:51 -04:00** — MRU SOURCE SWITCHING: sensor_names=[sbg,fcu] (SBG primary, FCU fallback), sensor_timeout=0.5s. NO live operator switch — sensor_names read once at construction (navigation_sensors.cpp:14-15), no service/param callback; ros2 param set won't re-read (and zenoh can drop it). Durable switch = reorder to [fcu,sbg] + RELAUNCH mru_transform. Automatic freshness failover IS on-the-fly: first FRESH source by priority owns output; SBG stale >0.5s -> auto-fail-over to FCU, auto-reclaims when SBG returns. Live active source on /bizzy/nav/active_sensor/orientation. Force-failover-by-starving-SBG is a hack, not stable.

**2026-06-29 10:51 -04:00** — RCA THREAD (wrap-up): bizzyboat.yaml:64-65 claims SBG verified vehicle-aligned 2026-06-27 (yaw matches FCU 0.01deg, roll/pitch match tie-up), but today measured constant +2deg roll offset (yaw still aligned 0.12deg). Either the 2026-06-27 check missed roll or mount/alignment shifted since. ALSO: my 2deg diff was on sensors/sbg/ekf_quat, but mru_transform consumes sensors/sbg/imu/data (bizzyboat.yaml:69) — almost certainly same EKF attitude but not verified identical; re-diff on imu/data to be airtight.

**2026-06-29 10:55 -04:00** — CONFIG CHANGE (uncommitted, not yet live): bizzyboat.yaml mru_transform sensor_names [sbg,fcu] -> [fcu,sbg] (FCU primary), comment updated with revert rationale. NOTE/CONSEQUENCE: sensor_names is ONE priority list governing position + orientation + velocity (navigation_sensors.cpp:17-23 builds all three per name). So this also makes FCU the primary POSITION (mavros/global_position/global) and VELOCITY source, displacing the SBG INS — likely a GPS/velocity accuracy downgrade. Flagged to operator. Needs mru_transform relaunch to take effect (sensor_names read once at construction).

**2026-06-29 10:57 -04:00** — DECISION: operator chose FULL switch — FCU primary for position+orientation+velocity (accepting SBG INS displaced for position/velocity). Config edit stands as-is. Pending: mru_transform relaunch to apply. Commit batched (not yet committed).

**2026-06-29 11:34 -04:00** — SWITCH VERIFIED after relaunch: /bizzy/nav/active_sensor/{orientation,position,velocity} all report 'fcu'. And /bizzy/odom attitude now == FCU mavros/imu/data exactly (mean diff 0.00 roll/pitch/yaw over 6 samples) — the prior steady +2deg roll is gone from the nav solution. mru_transform healthy. Next: confirm aft costmap false positives clear.

**2026-06-29 11:37 -04:00** — RESOLVED (operator-confirmed): aft costmap false positives are GONE after switching mru_transform to FCU primary. Confirms the ~2deg SBG roll misalignment, passed raw through mru_transform attitude into the sea_surface projection, was the cause. Full chain: observed aft FPs -> measured constant +2deg roll SBG-minus-FCU underway -> found mru attitude ignores URDF mounting rotation -> verified FCU stream stable -> reordered sensor_names [fcu,sbg] -> relaunched -> active source=fcu, odom==FCU (0deg), FPs cleared.

**2026-06-29 11:45 -04:00** — COMMITTED config fix 34bf84c on jazzy (field-mode, author Claude Code Agent): sensor_names [fcu,sbg] FCU primary. Pre-commit hooks passed. Not pushed.

**2026-06-29 12:16 -04:00** — DATA CAVEAT — garmin sidescan set to AUTO RANGE in the chartplotter over the weekend (operator-reported), to test auto-range behavior while surveying. So today's data so far has the sidescan auto-switching ranges (range_m varies through the run rather than fixed). Relevant to anyone analyzing today's sidescan bags / range_m field.

**2026-06-29 12:30 -04:00** — CUBE STATUS (M3): node m3/cube_bathymetry lifecycle active[3], fed by m3/soundings at ~28Hz (M3 multibeam live via kongsberg_em_bridge->detections_to_pointcloud). Generating GRIDS: m3/grid GridMap ~0.2Hz (every ~5s). Generating TILES: m3/cube_bathymetry/tiles GridMap ~0.2Hz (every ~5s). coverage_tiles NOT producing — request-driven, coverage_requests has 0 publishers (nothing requesting), expected not a fault. DISK: costmap bathymetry store ~/data/stores/bathymetry UNCHANGED since 2026-06-26 (chart prior + registry.json only) — live CUBE grids/tiles are on ROS topics but not landing in that store. Flag: confirm where this cube instance persists / expected persist timing.

**2026-06-29 12:41 -04:00** — CUBE PERSISTENCE: m3/cube_bathymetry draft_dir is EMPTY (''). Config bizzyboat.yaml:41-44 sets only map_frame=bizzy/map, cell_size=0.5 — draft_dir not set, defaults to ''. Live params: draft_dir='', save_interval=30.0, max_resident_tiles=64. => today's M3 CUBE grids/tiles are live on topics (~5s) but NOT persisted to disk (save_interval has no target). ~/data/stores/bathymetry is the separate static chart prior read by costmap bathymetry_layer. Consequences: no survey persistence across run/restart; only 64 tiles held resident (older coverage drops as you survey new ground). To persist, set draft_dir to a writable dir (config change). [Inferred from empty param + static store; can confirm exact empty-draft_dir behavior in cube source if needed.]

**2026-06-29 12:47 -04:00** — CAMP LIVE-TILE READINESS: CUBE serving side READY — coverage_catalog is RELIABLE/TRANSIENT_LOCAL (latched, CAMP gets it on subscribe; that's why hz showed nothing), coverage_requests (TileRequest) cube-subscribed and listening, coverage_tiles (SonarVisualizationTile, BEST_EFFORT) publisher ready, tiles generating ~5s. GAP = transport: none of coverage_requests/coverage_tiles/coverage_catalog appear anywhere in udp_bridge config (whole-config grep; only deltat_grid is bridged). CAMP reaches boat via udp_bridge 'operator' remote (port 4200, all_topics:false allowlist, ~300KB/s cap). So over the operator link CAMP CANNOT ask for or receive live tiles — request+catalog+tiles topics are not bridged. To enable: add coverage_requests (inbound CAMP->boat), coverage_catalog + coverage_tiles (outbound) to operator remote with bandwidth budgeting (tiles are GridMap-class, heavy vs cap). Exception: if CAMP joins boat zenoh net directly (dockside/high-bw), it sees topics natively, no bridge change.

**2026-06-29 13:07 -04:00** — ACTION: called udp_bridge/remote_subscribe (runtime, not persisted) to pull coverage_requests over VPN. remote=operator, connection_id=vpn (1.2MB/s cap; cell is 300KB/s), source_topic=destination_topic=/bizzy/sensors/m3/cube_bathymetry/coverage_requests, queue_size=10, period=0. Service accepted. Semantics verified in source (udp_bridge.cpp:1461): remote_subscribe tells the REMOTE (salmon) to advertise the topic to us = inbound CAMP->boat. Post-check: coverage_requests still pub 0 / no data — expected until CAMP actually publishes requests on salmon (bridge creates boat-side republisher on first msg). ASSUMPTION: CAMP publishes under the same FQ name on salmon; if not, re-call with correct source_topic. CAVEATS: (1) RETURN path not done — CAMP won't receive tiles until coverage_tiles+coverage_catalog are remote_advertise'd outbound (heavy vs cap, needs budgeting). (2) Runtime-only — lost on udp_bridge relaunch; add to config for durability.

**2026-06-29 13:11 -04:00** — RETURN PATH set up (runtime, not persisted) via udp_bridge/remote_advertise, remote=operator connection_id=vpn: coverage_catalog (queue 2, period 0 — small/latched) and coverage_tiles (queue 2, period 0.5 = max 2/s — heavy, rate-limited). Verified boat-side: both topics now Subscription count 1 (bridge consuming -> forwarding to operator). Note coverage_tiles is request-driven (silent until a coverage_request arrives), so no continuous bandwidth until CAMP asks; the continuous ~5s /tiles GridMap was NOT bridged. FULL LOOP now: in=coverage_requests (remote_subscribe), out=coverage_catalog+coverage_tiles (remote_advertise), all over vpn. Caveats unchanged: (1) request path needs CAMP publishing the matching FQ source_topic on salmon to flow; (2) all runtime-only, lost on udp_bridge relaunch -> add to config for durability; (3) watch vpn bytes when tiles actually flow (cap 1.2MB/s, already ~784KB/s tx); loosen/tighten period per real tile size.

**2026-06-29 13:24 -04:00** — SUCCESS (operator-confirmed): CAMP is receiving live CUBE coverage tiles over VPN via the runtime udp_bridge remote_subscribe (coverage_requests in) + remote_advertise (coverage_catalog + coverage_tiles out) set up this session. Boat-side note: a short hz sample right after caught no inbound requests / tile bursts and coverage_requests still showed pub 0 — consistent with request-driven traffic (only flows while operator interacts with CAMP map), not a fault; operator display is ground truth. vpn cap 1.2MB/s, prior tx ~784KB/s — watch headroom under active tile fetch.

**2026-06-29 13:31 -04:00** — PERSISTED (config edit, uncommitted, not yet live): added coverage_catalog + coverage_tiles to bizzyboat.yaml udp_bridge operator/vpn connection (topics_list + topics map), mirroring the working runtime remote_advertise params (catalog q2; tiles q2 + period 0.5). YAML parses. IMPORTANT ASYMMETRY found in udp_bridge source: the boat bridge config (topics_list loop, udp_bridge.cpp:399-414) only advertises OUTBOUND topics — there is NO inbound-subscribe config field. So the inbound coverage_requests (CAMP->boat) CANNOT be persisted boat-side; it must be persisted on salmon (salmon advertises coverage_requests -> bizzy over vpn) OR via a startup remote_subscribe hook. Documented in a config comment + flagged for wrap-up. Takes effect on next udp_bridge relaunch (which also drops the current runtime hookups, then reloads outbound from config).

**2026-06-29 13:36 -04:00** — INBOUND PERSISTED too — operator.yaml (name: operator, remote bizzy) is in the SAME repo and reachable. Added coverage_requests to its bizzy/vpn connection (topics_list + topics: {source: /bizzy/sensors/m3/cube_bathymetry/coverage_requests, queue_size 10}), advertising operator->bizzy. Confirms operator's point: sender-advertises, so the inbound-to-boat topic lives in the operator bridge config, not the boat's. Full loop now persisted as a PAIR: bizzyboat.yaml advertises coverage_catalog+coverage_tiles bizzy->operator; operator.yaml advertises coverage_requests operator->bizzy; all vpn. Updated bizzyboat.yaml cross-ref comment. Both YAMLs parse. Source FQ-name convention (/bizzy/...) matches existing command/joystick_helm entries — same reason the runtime same-name assumption worked. Uncommitted; takes effect on respective bridge relaunches (boat bridge for outbound, operator/salmon bridge for inbound).

**2026-06-29 14:50 -04:00** — MANDA COVERAGE: /bizzy/manda_coverage (manda_coverage_action_server, action /bizzy/compute_sonar_coverage_path) is active[3], action server up, odom live. BUT its soundings input is remapped to sensors/deltat/soundings (echoboat navigation_launch.py:234) which is SILENT (DeltaT off; pub advertised, 0 data in 8s). It is NOT subscribed to the live m3/soundings (~28Hz). So manda coverage is wired in but to the dead DeltaT source — coverage-path compute would run on empty soundings. Fix = repoint remap soundings -> sensors/m3/soundings (launch change + relaunch), operator's call. Distinct from the CUBE->CAMP coverage_tiles work today (manda is the coverage-PATH planner, not the tile server).

**2026-06-29 14:54 -04:00** — COMMITTED (field-mode jazzy, author Claude Code Agent, hooks passed): 0277a5b unh_echoboats — persist CAMP coverage-tile bridging (bizzyboat.yaml + operator.yaml). 03db1cc seafloor_echoboat — manda_coverage soundings remap deltat->m3 (navigation_launch.py). Both uncommitted->committed; not pushed. (earlier 34bf84c mru FCU-primary already committed.)

**2026-06-29 14:55 -04:00** — RUN-TASK BT TRACE (manda coverage usable?): YES. run_tasks.xml NavigatorSequence -> Switch5 case 'survey_area' -> SurveyAreaTask -> IfThenElse HasSubTasksCondition FALSE -> PipelineSequence(RunSurveyAreaSubTasks + SonarCoverageTask) -> SonarCoverage node server_name=compute_sonar_coverage_path. SonarCoverage/CancelSonarCoverage BT nodes registered in bt_register_nodes.cpp (bound to compute_sonar_coverage_path, BtActionNode<ComputeSonarCoveragePath>), plugin lib marine_nav_behavior_tree_bt_plugins is in bt_task_navigator plugin_lib_names (nav2_params.base.yaml:23). manda_coverage action server active. TRIGGER: dispatch a survey_area task (polygon) with NO pre-computed sub-task lines -> BT calls manda to generate lines (AddSubTask, run as survey_line). If lines pre-supplied, generator is SKIPPED (HasSubTasks true). CAVEAT: line spacing depends on manda soundings (just remapped deltat->m3 in 03db1cc; needs nav relaunch). So: wiring complete + server live; usability gated on (a) survey_area-without-lines dispatch and (b) the soundings relaunch for valid spacing.

**2026-06-29 14:59 -04:00** — MANDA TASK PAYLOAD CONTRACT (verified in source): no special value/flag needed. Requirements: (1) task type == 'survey_area' (Switch5 case); (2) task.poses ARE the area polygon boundary — SetPolygonFromTask copies poses[start..end] position.xyz into PolygonStamped (set_polygon_from_task.cpp), no special data key; (3) no sub-tasks attached (HasSubTasksCondition false is the ONLY gate selecting generate-coverage vs run-premade-lines). Action goal ComputeSonarCoveragePath = ONLY geometry_msgs/PolygonStamped survey_area (no spacing/overlap/mode). Line spacing/swath is decided INSIDE manda (node params + soundings/depth), not the payload — so payload is clean but spacing quality still depends on the soundings remap 03db1cc (pending nav relaunch).

**2026-06-29 15:15 -04:00** — MANDA RELAUNCH TARGET: restart nav_launch.py (bizzyboat_project11) — the boat's own nav group (NOT wrapped by core_launch.py; only sim_core includes it). Chain: nav_launch.py -> nav2_bringup_launch.py (model 240) -> navigation_launch.py (owns /bizzy/manda_coverage + the deltat->m3 soundings remap). Verified NO REBUILD needed: installed navigation_launch.py already in sync with commit 03db1cc (install copy has sensors/m3/soundings) — plain relaunch suffices. Caveats: full nav2-stack relaunch (controller/planner/bt_task_navigator/costmaps/lifecycle mgrs + manda all cycle together, not single-node); wipes runtime-set nav2 params; startup-race risk (verify lifecycle active + TF settles after); interrupts active navigation (do between runs); INDEPENDENT of udp_bridge so CAMP tile bridging stays up.

**2026-06-29 15:18 -04:00** — NAV RELAUNCH VERIFIED (operator restarted nav_launch.py, testing manda): manda_coverage now subscribes /bizzy/sensors/m3/soundings (remap 03db1cc took; deltat gone). m3/soundings live ~28Hz. manda lifecycle active[3]. nav2 stack all active[3] (bt_task_navigator, controller_server, planner_server, local_costmap) — clean startup, no race stall. Action /bizzy/compute_sonar_coverage_path available. manda now planning off live M3 depth -> survey_area task (polygon, no premade lines) should yield real depth-based coverage. Ready/under test.

**2026-06-29 15:22 -04:00** — MANDA COVERAGE VIZ: two outputs. (1) Action feedback compute_sonar_coverage_path: current_line (nav_msgs/Path per generated line) + line_number + percent_complete — always on during a goal, primary output. (2) Dedicated swath Marker (visualization_msgs/Marker LINE_STRIP, ns manda_coverage_swath, SurveyPath.cpp:81) — GATED by display_topic param which is currently EMPTY ('') on the node and not in any config, so it's DISABLED (publisher reset). To enable: set display_topic param (read at SurveyPath construction = per-goal, so set before next goal; runtime-only/verify-under-zenoh; add to a manda config block for durability). Also: generated lines run as survey_line sub-tasks -> visible on CAMP via existing received_global_plan/path_follower_visualization overlay (already bridged). Offered to enable display_topic + bridge marker to CAMP.

**2026-06-29 15:25 -04:00** — MANDA VIZ ENABLED + BRIDGED (runtime): (1) set param /bizzy/manda_coverage display_topic = /bizzy/manda_coverage/coverage_swath (verified, not dropped). (2) udp_bridge remote_advertise that marker -> operator/vpn (queue 2, period 1.0 since swath LINE_STRIP grows w/ coverage). Service accepted. Marker topic does NOT exist until first survey_area goal (manda creates publisher at SurveyPath construction, reads display_topic then) — bridge sub is registered + waiting, forwards once manda publishes. NOTE: display_topic read per-goal, so a goal already running used old ''; need a fresh survey_area goal. Both runtime-only: display_topic lost on nav relaunch, advertise lost on udp_bridge relaunch. TODO offered: persist display_topic (manda config block) + marker bridge (bizzyboat.yaml operator/vpn) + commit, mirroring coverage_tiles.

**2026-06-29 17:33 -04:00** — DOCK WRAP — persisted manda viz to config + committed 6cabd94 (nav2_overlay.yaml display_topic + bizzyboat.yaml manda_coverage_swath bridge). Committing this log next. Pushing all field commits to gitcloud origin: unh_echoboats (34bf84c mru, 0277a5b CAMP bridge, 6cabd94 manda viz, + log commit) and seafloor_echoboat (03db1cc manda remap).
