# 2026-08-26 — pandy log (BizzyBoat deployment — issue pending)

Deployment issue: pending (backfill from a dev host)
Host: pandy
Side: field
Started: 2026-08-26 08:43 -04:00

## 2026-08-26

**2026-08-26 08:43 -04:00** — DEPLOYMENT MODE ACTIVATED on pandy, field side (origin git@gitcloud:field/unh_echoboats_project11.git), repo on jazzy, clean tree. Issue-less start per ros2_agent_workspace#533 - git-bug lookup skipped because the boat was already live and on the water; header carries the canonical pending marker for dev-side backfill. Prior deployment #459 was closed out 2026-08-25 16:29 (TIED UP, committed 2c9387f, pushed fa6beec), so this is a new deployment rather than day 2 of that one.

**2026-08-26 08:43 -04:00** — MARINE_WEB_VIEW BROUGHT UP LIVE TO S3. make sync earlier today pulled unh_marine_autonomy 4b44412..ddf13c3 (PR #358 / issue #357), adding the ais_renderer node plus a 251-line AIS layer in web/index.html. Built marine_web_view in core_ws with --symlink-install. All three renderers (state, coverage, ais) now run against bizzy publishing to s3://unh-ccom-p11-live via profile p11-renderer.

**2026-08-26 08:43 -04:00** — FINDING - ais_renderer_launch.py as merged in #357 could not launch at all. ParameterValue rejects a bare python list as value_type; launch_ros coerces only against int/bool/float/str and their typing.List forms, so the file raised Unrecognized data type: <class list> and died before spawning any node, whatever ignore_mmsis was set to. Fixed to List[int] (commit d3b1642 on unh_marine_autonomy jazzy). Verified both directions: default empty list starts clean, and ignore_mmsis:=[366000001,367000002] logs never publishing MMSI 366000001, 367000002. NOT caught by the 423-test suite because test_launch_params.py parses the launch files as TEXT rather than executing them - a launch file can be syntactically fine, pass every param-forwarding assertion, and still be unable to run.

**2026-08-26 08:43 -04:00** — FINDING - sibling launch includes leak configurations into each other. Wrote web_view_launch.py to bring all three renderers up together (commit 7441896). First dry run showed ais_renderer reporting /bizzy/ais/contacts -> /tmp/position.geojson every 1.0s: it had inherited state_renderer local_path and interval, because the three included files declare overlapping argument names and DeclareLaunchArgument does not overwrite a configuration already set, so the first include to declare a name wins it for every later sibling. Live this also leaks key, and ais would have PUT its contact collection over live/position.geojson - taking the boat off its own map. Fixed with scoped=True GroupAction per include; re-verified each renderer keeps its own key, local_path and interval.

**2026-08-26 08:44 -04:00** — FINDING - AIS topic default was wrong for the operator station. web_view_launch.py originally derived contacts_topic from platform, giving /bizzy/ais/contacts. That topic is real but boat-side - it is what the recorded bags carry, because BizzyBoat has its own receiver. pandy runs its OWN shore chain (/ais/nmea_relay -> /ais/ais_parser -> /ais/ais_contact_tracker) publishing the global /ais/contacts, which is what the ais_renderer README documented all along. Derived, the renderer subscribed to a topic that does not exist here and would publish an empty collection forever - which reads on the page as a quiet river, not as a misconfiguration. Fixed in commit 22046b7; contacts_topic no longer derived from platform.

**2026-08-26 08:44 -04:00** — OPERATIONAL HAZARD - an aborted ros2 launch can leave already-spawned children running, reparented to init. My first web_view_launch dry run at 06:39:28 aborted on the ais ParameterValue error above; launch died but state_renderer (pid 586475) and coverage_renderer (pid 586476) had already spawned and survived with PPID 1 for 72 minutes. Both were dry_run=true so nothing reached S3, but the orphaned coverage_renderer kept publishing TileRequest every 5s at up to 256 tiles per request, so once the bridge came up at 07:12 the boat was serving DOUBLED tile traffic over the survey link alongside the operator stack. Also note I initially reported only one orphan: ros2 node list piped through head -15 truncated /state_renderer, which sorts after /rqt_diagnostics. Both killed at operator request; coverage_requests confirmed back to 0 publishers afterwards. Check pgrep -af marine_web_view/(state|coverage|ais)_renderer after any non-clean launch exit.

**2026-08-26 08:44 -04:00** — AIS PIPELINE VERIFIED WORKING END TO END. Measured over 30s on pandy: /ais/nmea 77 msgs, /ais/messages 76, /ais/contacts 44 (~1.5 Hz), /ais/atons 29. ais_renderer is 1 of 2 subscribers on /ais/contacts. Socket sampling shows a FRESH https connection to S3 per upload cycle (ais_renderer local ports 48252 -> 51454 -> 38526 across three samples), so PUTs are being sent and not erroring; the intermittent CLOSE-WAIT is S3 closing idle keep-alives between 10s ticks and never accumulates. state_renderer independently confirmed live by its rosout line: track hit the 1200 point cap, oldest fixes trimmed - it only reaches that after 1200 real position fixes. No credential or upload errors on /rosout across 45s. CAVEAT: this confirms PUTs are sent and not failing, NOT that the objects were read back - p11-renderer is scoped to s3:PutObject on live/* and returns AccessDenied on list_objects_v2 for both the root and live/ prefixes, so bucket contents cannot be verified from this host.

**2026-08-26 08:44 -04:00** — HEADLINE FINDING - the deployed web page predates the AIS feature, so nothing AIS shows on it despite the pipeline being healthy. Operator confirmed by inspection: no AIS row in the readout and no AIS traffic checkbox at all (as opposed to the layer being present and reading offline or no contacts, which would have meant a fetch or data fault instead). The AIS layer went into web/index.html at 2026-08-26 03:43 as part of #357 and reached pandy only in this morning make sync. NOTHING IN THE REPO PUBLISHES THE PAGE: refresh_chart_tiles.py covers chart tiles only, and the renderers upload only their own artifacts (live/position.geojson, live/track.geojson, live/ais.geojson, live/coverage/*). Building the package installs index.html into install/ but does not deploy it, so CloudFront still serves the pre-AIS page and live/ais.geojson is being written correctly with no page code to read it.

**2026-08-26 08:44 -04:00** — BLOCKED ON PAGE DEPLOY - two unknowns, both for the operator, deferred rather than guessed. (1) CREDENTIAL: index.html sits at the bucket ROOT (artifact paths in the page are relative, live/ais.geojson), but p11-renderer is scoped to s3:PutObject on live/* so it almost certainly cannot write the page; the other configured profile is ccom-jhc. Did not test-write against production to find out. (2) CACHE-CONTROL: freshness is per-object max-age and CloudFront invalidation is deliberately NOT used (README: billed per path, would dominate every other cost at any real update rate). A PUT replaces the object metadata, so deploying sets the page max-age, the current value cannot be read back from this host, and getting it wrong either pins the stale page at the edge for a long TTL or makes every viewer refetch constantly. Expect a delay after any deploy: without invalidation, edges serve the cached copy until the OLD object TTL expires.

**2026-08-26 08:44 -04:00** — GAP FOR WRAP-UP - marine_web_view has no page-deploy path at all: no script, no make target, no documented procedure anywhere in the workspace (grepped README, docs/ and scripts/ across all project repos). The README documents the renderers thoroughly but never says a web/index.html change needs publishing separately from them, which is exactly how a merged, built, running feature stayed invisible on the live page. Three commits sit on unh_marine_autonomy jazzy UNPUSHED (gitcloud origin, field mode): d3b1642 the launch fix, 7441896 the umbrella launch, 22046b7 the AIS topic default. Deferred to wrap-up per sterile cockpit: a page-deploy script or documented one-liner, a README note that the page deploys separately, and a test that EXECUTES each launch file rather than parsing it as text.

**2026-08-26 08:50 -04:00** — COMMITTED AND PUSHED at operator go-ahead. unh_echoboats_project11: today log committed 567692d on jazzy, pushed b16f45e..567692d to gitcloud, fast-forward, no divergence. unh_marine_autonomy: three commits pushed ddf13c3..22046b7 on jazzy to gitcloud (d3b1642 launch fix, 7441896 umbrella launch, 22046b7 AIS topic default). Both fetched first to check divergence - none either time, no rebase needed, no force push. SAME HOOKS CAVEAT AS 2026-08-25: no pre-commit hooks ran. --no-verify was NOT used and nothing was bypassed - pre-commit is simply not installed on pandy (not on PATH, not in the workspace .venv) and this repo has no .git/hooks/pre-commit wired. Unmet by absence, not evasion. Committed files are markdown and python/launch, and the launch files were separately checked clean with ament_flake8 and ament_pep257 before commit. An agent on the dev machine is deploying the web page.

**2026-08-26 08:50 -04:00** — UDP_BRIDGE PULLED AND BUILT. Was 63 commits behind; fast-forwarded ed372a8..93096b2 on jazzy (PR #77 / issue #52, the admission-control and AIMD work: connection_diagnostic.h, test_admission_field_replay, test_message_atomicity, test_connection_diagnostic; 25 files, +6549/-531). Built udp_bridge + udp_bridge_interfaces in core_ws with --symlink-install, 2 packages finished, 0 failures. The only stderr is pre-existing -Wpedantic flexible-array-member warnings in packet.h, unrelated to this change.

**2026-08-26 08:51 -04:00** — RESTART CAVEAT ON THE NEW UDP_BRIDGE - the operator udp_bridge is RUNNING and carrying the live link to bizzy right now. It keeps the code it loaded at startup, so the build just done changes nothing until the node is restarted, and restarting it mid-survey drops the operator link to the boat. Deliberately NOT restarted. Checked the one startup-blocking hazard before leaving it: #52 retires link_headroom_fraction and the new tripwire rejects on PRESENCE of the key in the parameter overrides (the parameter is no longer declared at all and kDefaultLinkHeadroomFraction is deleted), so a stale config carrying it would REFUSE to start rather than warn. Grepped every yaml/py/xml under layers/main/*/src outside udp_bridge itself: no live config sets link_headroom_fraction, so a restart is safe from that specific failure whenever the operator chooses one. Have NOT otherwise reviewed 63 commits of admission-control change against the current field config.

**2026-08-26 08:58 -04:00** — POST-UPDATE CHECK AFTER GABBY RESTARTED ON THE NEW UDP_BRIDGE - link healthy, and the one alarming diagnostic is PRE-EXISTING, not caused by the update. Live state on pandy: vpn level 0, tx ~3.0-3.3 kB/s, rx ~290-305 kB/s steady; publish queue 0 queued 0 dropped total; relay queue 0 queued 0 dropped total. WIRE FORMAT UNCHANGED by #52 - packet.h has zero diff across ed372a8..93096b2, no version or magic constant touched - so the old bridge on pandy and the new one on gabby interoperate, which the steady rx confirms.

**2026-08-26 08:58 -04:00** — ANOMALY (PRE-EXISTING, NOT A REGRESSION) - resend give-ups trip the ERROR threshold and the rate display flaps. Consecutive diagnostic ticks alternate level 0 'give-up rate 0.00/s (total N)' with level 2 'give-up rate 286.18/s exceeds error threshold', and the totals climb by exactly the quoted rate (1489657 -> +286 -> 1489943 -> +239 -> 1490182), so give-ups are genuinely running ~240-290/s while the operator sees OK on half the ticks. Checked against yesterday BEFORE any bridge update, from the 2026-08-25 operator bag /diagnostics: same pattern, 12385 samples over 199.3 min, total 0 -> 4665718, AVERAGE 390.2 give-ups/s, 3150 ERROR-level statuses. Today is ~236/s average over ~105 min uptime - LOWER than yesterday. So the gabby update neither caused nor worsened this.

**2026-08-26 09:46 -04:00** — CAMP CRASHED at 09:43:48, auto-respawned 09:43:53 (5s), no operator action needed. Kernel: CCOMAutonomousM[589286] segfault at 0, error 4, in libQt5Gui.so.5.15.13. RECOVERED AND HEALTHY - new pid 617136 confirmed subscribed to /bizzy/mavros/global_position/global, /bizzy/sensors/sbg/imu/nav_sat_fix, both IMUs, velocity_body and /marine/platforms.

**2026-08-26 09:46 -04:00** — CAMP CRASH ROOT CAUSE (first-order) - NOT a Qt bug and NOT from today udp_bridge update. The three lines immediately before the segfault: 'Unexpected condition: string capacity was zero for allocated data! Exiting.' then two 'Timers cannot be stopped from another thread'. That first message is emitted by /opt/ros/jazzy/lib/librosidl_runtime_c.so - the ROS message runtime hard-exits when it deserializes a string with zero capacity but allocated data. So camp was killed by a MALFORMED STRING IN AN INCOMING ROS MESSAGE; the Qt5Gui null deref is teardown debris after exit() ran static destructors on a non-GUI thread. Checked whether the gabby bridge update could be responsible since a reassembling bridge is exactly what could corrupt a string: NO - the same signature is in pandy ROS logs for 2026-08-20 (1), 08-21 (2), 08-24 (1), 08-25 (2) and today (1), so this is a longstanding ~1-2 per survey day camp crash that predates the update by nearly a week. Which message/publisher carries the bad string is NOT identified - deferred to wrap-up RCA.

**2026-08-26 10:15 -04:00** — PANDY-SIDE VIEW OF LIVE COVERAGE TILES, for the agent working the problem on gabby. Transport IS delivering to pandy: measured over 30s, coverage_catalog 5 msgs, coverage_tiles 6 msgs, coverage_requests 0. Endpoints on coverage_tiles: publisher is /operator/udp_bridge (relaying gabby), sole subscriber is /coverage_renderer (marine_web_view). STRUCTURAL POINT: CAMP has NO coverage subscriptions at all - ros2 node info /operator/camp lists no coverage_catalog, coverage_tiles, TileCatalog or SonarVisualizationTile. So the only live-tile consumer on pandy right now is the web-view renderer, and if the goal is coverage visible in CAMP, that is a pandy-side gap independent of whatever gabby is producing (camp#121 is the CAMP live-cache consumer; it appears not to be active in this session). Also note the earlier orphaned coverage_renderer that doubled TileRequest traffic was killed at 08:5x, so request counts seen from gabby should have halved around then.

**2026-08-26 10:22 -04:00** — CAMP LIVE COVERAGE RESOLVED BY AN OPERATOR RESTART - and the real defect is a DISCOVERY RACE, not a stale build. Operator restarted camp and the live-coverage layer appeared immediately; camp now subscribes to all three of /bizzy/sensors/m3/cube_bathymetry/{coverage_catalog,coverage_tiles,coverage_requests} and the catalog/tiles subscriber counts went 1 to 2 (camp alongside the web-view coverage_renderer). The tiles subscription means the layer is ENABLED, not merely spawned - sonar_live_cache_layer subscribes the catalog on construction ('availability is cheap') and the tile stream only on enableLiveCoverage().

**2026-08-26 10:22 -04:00** — CAMP DISCOVERY DEFECT (for wrap-up RCA) - SonarLiveCacheManager::updateTopics is edge-driven off TopicsManager::namesUpdated and gates each candidate on node->count_publishers(topic) == 0 -> continue. EVIDENCE IT MISSES A LATE SOURCE: the camp instance that respawned at 09:43:53 after the crash still had ZERO coverage subscriptions of any kind at 10:15, while coverage_tiles was measurably flowing at that same moment (6 msgs in 30s, publisher /operator/udp_bridge). A manual restart after tiles were flowing discovered it on the first try. So a source that starts publishing AFTER camp is already up is not reliably picked up, and the operator workaround is to restart camp once the boat tile producer is confirmed live. WHICH of the three silent-continue paths is responsible is NOT established - candidates are namesUpdated not firing for a publisher appearing on an already-known name, the count_publishers gate being evaluated only on name-list change, and topLevelLayers() returning null during construction. Note the same class of bug was just fixed in the web page for the AIS layer in #357 ('sync the AIS layer to its checkbox at startup, not just on change').

**2026-08-26 11:10 -04:00** — WEB VIEW - outlined the live-coverage footprint at operator request (operator saw coverage on the page but could not distinguish it from the background bathymetry). Cause is by design: coverage shares the basemap depth ramp because uma#345 made them match so one depth cannot read as two colours, so where new soundings agree with the chart there is no visible edge. Fixed by OUTLINING, not recolouring - recolouring would undo #345. drop-shadow follows the alpha channel and coverage tiles are transparent where the sonar has not covered ground, so it traces the real survey boundary. Filter is on a dedicated Leaflet pane, NOT the tile layer: per-tile it would outline all four sides of every tile and paint a grid over the coverage. Pane created once at map setup since the coverage layer is rebuilt on every manifest zoom change, and it carries the zIndex 350 the layer option used to set. Committed ea6bdce, pushed to gitcloud. 423 package tests still pass; JS structure checked balanced. NOT VISUALLY VERIFIED - cannot render the page from here, and the dev-side agent owns page deploys, so this needs their deploy plus a CloudFront TTL wait before the operator sees it. No test added for the pane wiring (test_page_layers.py parses index.html and could assert it) - deferred to wrap-up.

**2026-08-26 11:15 -04:00** — CAMP PULLED AND BUILT with the crash-reporting work. Fast-forwarded c97153c..a6863ac on jazzy, 43 commits, PR #218 / issue #217: new libcamp_crash library, per-thread alternate signal stacks, anti-hang watchdog on the terminate path, cmake/check_worker_alt_stacks.cmake guard. Built in layers/main/ui_ws with --symlink-install, 1 package, 0 failures, 1min24s; only stderr is pre-existing -Wunused-parameter/-Wsign-compare noise in surveyarea.cpp and group.cpp. Verified it landed: libcamp_crash.so built+installed and present in ldd of the executable, which now carries install_crash_handlers, install_thread_alt_stack and crash_log_path. Tests 309 passed, 0 failures, 1 skipped (the skip is elsewhere, not in the crash suite); the CrashHandler suite ran 12 tests 0 failures including CrashOnANonMainThreadIsStillReported - the exact shape of this morning 09:43 crash, which died during exit() teardown on a non-GUI thread. CRASH LOGS WILL LAND AT rclcpp::get_logging_directory() + /camp_crash_<pid>.log i.e. ~/.ros/log/camp_crash_<pid>.log, degrading to stderr-only if the log dir cannot be resolved. NOT YET IN EFFECT - the running camp has the old code mapped and the handler installs at startup, so it needs an operator restart; restarting while tiles are flowing also re-triggers live-coverage discovery, which is the known workaround for that race.

**2026-08-26 14:13 -04:00** — CAMP CRASHED AGAIN 14:11:09 - and the new crash handler CAUGHT IT, first real crash since the #217 build. Wrote ~/.ros/log/camp_crash_639096.log, 3426 bytes, 36-frame backtrace, and no bare kernel segfault line this time. Camp respawned and recovered clean: 3 coverage subscriptions and both nav sources back, so live coverage returned on the respawn without operator action. Signal SIGABRT si_code=-6.

**2026-08-26 14:13 -04:00** — CAMP CRASH 14:11 IS HEAP CORRUPTION, and it is NOT the same signature as the 09:43 one. glibc diagnostic at the same epoch (1787767869): 'malloc_consolidate(): unaligned fastbin chunk detected' - a corrupted fastbin free-list entry. Backtrace: main -> QCoreApplication::exec -> glib event loop -> sendPostedEvents -> QWidget::event -> QWidgetPrivate::drawWidget -> QPainter::begin -> QRasterPaintEngine::createState -> operator new -> malloc -> libc internals -> abort. CRITICAL READING NOTE: that stack is where corruption was DETECTED, not where it was CAUSED - glibc validates the free list on the next allocation, so the Qt paint path is just the unlucky next caller and chasing QRasterPaintEngine would be a dead end. Separately, a 'munmap_chunk(): invalid pointer' at 10:20:33 belongs to the operator manual restart around then, NOT to this crash - checked by epoch, it is a different event.

**2026-08-26 14:13 -04:00** — HYPOTHESIS FOR WRAP-UP RCA - camp has ONE heap-corruption bug presenting three ways today: 09:43 'string capacity was zero for allocated data! Exiting.' from librosidl_runtime_c (rosidl rejecting a nonsense string header - exactly what a smashed heap header looks like from its side), 10:20 'munmap_chunk(): invalid pointer', 14:11 'malloc_consolidate(): unaligned fastbin chunk detected'. All three are consistent with an out-of-bounds write or an invalid free somewhere in camp or a library it uses. This is a HYPOTHESIS, not established - the three could be independent. It does reframe the RCA from 'which Qt call is buggy' to 'what is writing out of bounds', and finding the writer needs an ASAN or valgrind build, which must NOT be run against the live operator console. Two crashes today (09:43, 14:11), consistent with the 1-2 per survey day pattern seen 08-20 through 08-25.

**2026-08-26 14:51 -04:00** — WEB VIEW COVERAGE STALENESS FIXED - operator reported live coverage on the page not always updating, needing a manual browser refresh. CAUSE: coverage_renderer cache_control (20) EQUALS render_interval (20.0). At equality a redraw issued the moment new tiles land is answered from the browser cache because the held tile is still inside its max-age, and CloudFront can serve an already-stale copy on top (~40s combined). Compounding it, the page redraws ONLY when the manifest rendered_tiles counter advances (a deliberate gate - without it a docked boat re-requested every viewport tile every 20s per viewer), so a stale frame is not corrected an interval later, it waits for the NEXT advance, which on a slow survey line is minutes. That is the intermittency. NOT the errorTileUrl 404 path - uncovered water answers 4xx with no cache headers so those are not sticky.

**2026-08-26 14:51 -04:00** — WEB VIEW RESTARTED with cache_control=10. cache_control is bound once at coverage_renderer __init__ so ros2 param set does not move it; needed a restart. web_view_launch.py did not expose the argument, so added a passthrough defaulting to 10 (strictly below render_interval, so every next-pass redraw is guaranteed to miss cache); committed 20f7b0a and pushed to gitcloud. Restart was CLEAN: SIGINT to launch pid 646455 reaped all three renderers with zero orphans (checked explicitly given this morning orphan incident). Relaunched detached under setsid so it survives the agent session; launch pid 670688, renderers 670772/3/4, kill -INT 670688 to stop. Verified live: cache_control=10, render_interval=20.0, dry_run=False, rendered 1 tile at z15, chart datum -28.03 m in bizzy/map, both nav sources subscribed. Operator will not see the change instantly - CloudFront still serves tiles stamped max-age=20 from before the restart.

**2026-08-26 14:51 -04:00** — TWO OBSERVATIONS FROM THE RESTART. (1) NEW QoS WARNINGS, not present in the operator 12:06 launch (count 0 there): twice at startup, on /tf_static and on .../coverage_catalog - 'offering incompatible QoS. No messages will be received from it. Last incompatible policy: DURABILITY'. Fired twice at startup only, has not recurred, and the renderer is demonstrably receiving (it rendered a tile and resolved the chart datum), so the read is a DDS discovery race during the restart with a stale endpoint from the dying process still cached. Benign on the evidence but NEW - if coverage goes quiet, restart the web view first. (2) The #357 AIS privacy filter is working on real traffic: 'MMSI 369493067 withheld from the public artifact -- search and rescue / law enforcement'. That position never reached S3 or the CDN.

**2026-08-26 16:00 -04:00** — ROOT CAUSE OF DEGRADED LIVE COVERAGE - QoS DURABILITY MISMATCH ON coverage_catalog, breaking BOTH consumers. /operator/udp_bridge publishes .../coverage_catalog as VOLATILE; both subscribers - coverage_renderer AND /operator/camp - request TRANSIENT_LOCAL, which cannot match a VOLATILE publisher, so NEITHER receives the catalog. Evidence: 204 render passes in the renderer log with ZERO 'catalog: N entries -> request R, prune P' reconcile lines, 3 tiles cached, and only the tiles pushed on coverage_tiles being painted. coverage_tiles itself is fine (publisher VOLATILE, renderer subscriber VOLATILE/BEST_EFFORT = compatible), which is why tiles arrive but the historical mosaic can never be requested. Catalog messages ARE on the wire and healthy: ros2 topic echo with default VOLATILE QoS receives them fine. CAMP IS AFFECTED THE SAME WAY - same TRANSIENT_LOCAL subscription.

**2026-08-26 16:00 -04:00** — CORRECTION TO MY EARLIER CALL - I logged the startup QoS warnings at 14:45 as 'benign on the evidence' because the renderer had rendered a tile and resolved the chart datum. That inference was WRONG: it was receiving TILES, not the CATALOG, and the catalog is what drives requesting everything the renderer does not already hold. The warning named coverage_catalog explicitly and said 'No messages will be received from it'; it meant exactly that. NOT CAUSED BY MY 14:45 RESTART either - the restart only surfaced it. CAMP received the catalog fine at 10:22 when the operator restarted it and live coverage appeared, so the publisher durability changed to VOLATILE sometime AFTER 10:22, which is the window the gabby-side agent has been working in. Also note my 08:58 'no weirdness from the gabby udp_bridge update' covered link health and throughput only - it did NOT cover QoS propagation, so it does not rule the #52 update out as the cause here.

**2026-08-26 16:00 -04:00** — FIX BELONGS ON THE PUBLISHER/BRIDGE SIDE, not the consumers. TRANSIENT_LOCAL is the CORRECT request for a latched catalog - it is what lets a late-joining consumer get current state instead of waiting for the next publish - so downgrading camp and coverage_renderer to VOLATILE would paper over it and lose late-join semantics (the catalog does republish about every 6s, 5 msgs in 30s measured, so VOLATILE consumers would limp, but that is a workaround not a fix). What is needed: the udp_bridge relay must preserve the producer TRANSIENT_LOCAL durability when republishing coverage_catalog on the operator side rather than downgrading it to VOLATILE. Open question for the gabby agent: did the cube_bathymetry producer restart with different QoS, or did the bridge QoS propagation change with the #52 update? NO CONSUMER-SIDE CHANGE MADE - this needs the gabby/bridge end.

**2026-08-26 16:12 -04:00** — WHY CAMP LOOKS FINE AND THE WEB VIEW DOES NOT - operator reports camp coverage fine while the web page shows only a sliver, despite BOTH having the same unmatched TRANSIENT_LOCAL catalog subscription. Resolved: camp keeps a DISK CACHE (LiveTileCache/cache_dir, writeTileToCache, loadFromGeoTiff, 'warm-load the disk cache BEFORE subscribing', ADR-0006/ADR-0008 D5) so it warm-loads its whole mosaic at startup and keeps painting new tiles, which still arrive because coverage_tiles QoS IS compatible. It never needs the catalog because it is not missing anything. The web renderer is explicitly the opposite - README: 'There is no warm load and no disk cache' - so it starts empty and depends entirely on the catalog round to rebuild. The catalog outage is invisible to a warm consumer and total for a cold one.

**2026-08-26 16:12 -04:00** — MY RESTART IS WHY THIS IS VISIBLE - owning this plainly. The 14:45 restart I did emptied the ONE component with no cache, at a time when the catalog round it needs to rebuild was already broken by the QoS mismatch. The QoS defect is not mine and predates the restart, but I should have known coverage_renderer is memory-only (its own README says so) and flagged that restarting it means rebuilding the whole mosaic from the catalog - and checked the catalog was actually reaching it - BEFORE restarting for a cache_control change. Nothing is lost: the S3 PNGs are intact (un-publish is guarded by 'if (x,y) not in self._published: return skipped'), and the authoritative product comes from the bags regardless.

**2026-08-26 16:12 -04:00** — PRODUCER CONTRACT CONFIRMS THE BRIDGE IS THE DEFECT - cube_bathymetry_node.cpp:515 publishes the catalog as rclcpp::QoS(1).transient_local().reliable(), with the comment at :459 'periodic ~/coverage_catalog (transient_local for late joiners)'. So TRANSIENT_LOCAL is the designed contract, camp and coverage_renderer are CORRECT to request it, and /operator/udp_bridge republishing it VOLATILE on the operator side is a QoS-propagation defect in the relay. Note this is PANDY's bridge, which is still the pre-#52 build - so the #52 update on gabby is not implicated by this line of evidence, though what changed after 10:22 is still unexplained. TWO WAYS FORWARD, operator's call: (a) fix the relay - correct, but needs a pandy bridge restart which drops the live link to the boat; (b) stopgap - drop the renderer catalog subscription to VOLATILE, one line in coverage_renderer.py plus a renderer restart, no bridge touch and no link drop; the catalog republishes about every 6s so late-join durability is not load-bearing during an active survey. (b) papers over a real bug and must be marked a workaround with the relay fix tracked for wrap-up.

**2026-08-26 16:19 -04:00** — WORKAROUND APPLIED AND WORKING - coverage_renderer catalog subscription dropped from TRANSIENT_LOCAL to VOLATILE to match the relay. Committed 1ffc459 and pushed to gitcloud, with a REVERT-ME block in the source naming the real fix. VERIFIED AGAINST THE LIVE GRAPH: the coverage_catalog incompatible-QoS warning is gone (count 0), and the reconciler now runs - 'catalog: 71 entries -> request 71, prune 0', draining 71 to 66 over about 70s with tiles cached climbing 3,4,5. The producer holds 71 GGGS tiles. Rebuild is BANDWIDTH-LIMITED over the bridge at roughly one tile per 15s, so the full mosaic returns to the page in about 15 minutes, progressively rather than at once. Current pids: launch 684347, renderers 684350/1/2, all correctly parented to the launch; kill -INT 684347 to stop.

**2026-08-26 16:19 -04:00** — ORPHAN HAZARD RECURRED, second time today - when I went to stop the 14:45 web view, the launch process was already gone but all three renderers were still running, reparented (their real parent was 684347-era pid 670769, not the 670688 I had recorded from pgrep). Killed them directly, verified clean, then relaunched. LESSON, worth carrying into the wrap-up write-up: pgrep for 'ros2 launch' can return a wrapper pid rather than the process that actually owns the nodes - take the parent from ps -o ppid= on a RENDERER, which is what I did this time (684350/1/2 -> 684347). And always verify with pgrep -af after any stop, never assume the launch reaped its children.

**2026-08-26 16:31 -04:00** — OPERATOR OBSERVATION - after the VOLATILE workaround, CAMP shows the hi-res live-coverage tiles as before PLUS a huge low-res tile covering UNSURVEYED area, which the operator says is not helpful. Operator judgement: fine for now, note it and move on - so NOT chased further under sterile cockpit. Measurements taken before standing down: the catalog advertises 71 entries and every one is level 11; every tile message on coverage_tiles is also level 11; no mixed levels anywhere, so this is NOT an overview-pyramid level mismatch as first suspected. An L11 GGGS tile covers a large ground area, so a tile holding only a few genuinely ensonified cells still paints as a big coarse patch over mostly-unsurveyed water.

**2026-08-26 16:31 -04:00** — LIKELY CAUSE AND THE GENERAL LESSON - CROSS-CONSUMER INTERFERENCE ON THE SHARED ADR-0008 REQUEST/RESPONSE PROTOCOL. Before the workaround, coverage_renderer sent ZERO requests (measured 0 msgs in 30s on coverage_requests) because it never received a catalog. After it, the renderer requests all 71 catalog entries. The producer answers on the SHARED coverage_tiles topic, and CAMP subscribes to that topic with compatible QoS - so CAMP now ingests and paints tiles for the whole 71-tile footprint, including tiles that are almost entirely unsurveyed, which it was never being sent before. In other words ONE CONSUMER'S REQUESTS CHANGE WHAT ANOTHER CONSUMER DISPLAYS. That is a property of the protocol, not a bug I introduced, but my workaround is what exposed it, and it is worth writing up: any consumer joining or restarting can alter every other consumer's view. Wrap-up should consider whether CAMP should filter incoming tiles by its own view/interest rather than painting everything that arrives.

**2026-08-26 17:13 -04:00** — BACK AT THE DOCK - operator reports the boat is alongside. End of live on-water ops for the day. Sterile-cockpit restriction relaxes from here, and this is the natural breakpoint for batching commits per the urgency contract.

**2026-08-26 17:30 -04:00** — CLOSE-OUT SUMMARY. Seven commits landed today, all on jazzy and all pushed to gitcloud. unh_marine_autonomy: d3b1642 ais_renderer_launch could not launch at all (ParameterValue rejected a bare list as value_type), 7441896 web_view_launch.py bringing all three renderers up together, 22046b7 AIS topic default corrected to the station receiver, ea6bdce outline the live-coverage footprint so it reads against the shared basemap ramp, 20f7b0a cache_control 10 so the display stops sitting on cached tiles, 1ffc459 the FIELD WORKAROUND dropping the catalog subscription to VOLATILE. unh_echoboats_project11: 1bdab4f this log. Left running at dock: three web-view renderers under launch 684347, camp, rosbag2 recorder. Working tree clean across the workspace and all 44 project repos - nothing uncommitted, nothing unpushed.

**2026-08-26 17:30 -04:00** — CARRY-FORWARD FOR WRAP-UP, in rough priority order. (1) RELAY QoS DEFECT - /operator/udp_bridge republishes the transient_local catalog as volatile, breaking both consumers; 1ffc459 is a MARKED workaround that must be reverted once the relay preserves producer durability, and it costs late-join if the producer restarts. (2) CAMP HEAP CORRUPTION - three signatures today, 1-2 crashes per day for a week; needs an ASAN or valgrind build to find the out-of-bounds writer, and must not be run against a live operator console. (3) CAMP LIVE-COVERAGE DISCOVERY RACE - a source that starts publishing after camp is up is not picked up; restart is the workaround; same class as the #357 'sync at startup, not just on change' fix. (4) NO PAGE-DEPLOY PATH in marine_web_view - no script, no make target, nothing documented; a merged, built, running feature stayed invisible on the live page because index.html deploys separately from the renderers. (5) LAUNCH-ABORT ORPHAN HAZARD - hit twice today; an aborted or wrapper-detached ros2 launch leaves spawned nodes reparented to init, and an orphaned coverage_renderer keeps requesting tiles over the survey link. (6) CROSS-CONSUMER INTERFERENCE on the shared ADR-0008 tile protocol - one consumer's requests change what every other consumer paints. (7) SMALLER: no test executes the launch files (test_launch_params.py parses them as text, which is why d3b1642 shipped broken); no test for the coverage pane wiring; camp NavSource::trySubscribe logs ~4 lines/second with empty pending topics.

**2026-08-26 17:30 -04:00** — CORRECTIONS I MADE DURING THE DAY, collected so a wrap-up reader does not re-derive them from a wrong premise. (a) I said camp was a week-stale build needing a rebuild - WRONG, twice over: I read the mtime of the --symlink-install SYMLINK rather than its target, and I checked the executable for symbols that live in libcamp_map_ros.so. Camp was current and the live-coverage code was compiled in; the real defect was the discovery race. (b) I logged the startup QoS warnings as 'benign on the evidence' because a tile had rendered - WRONG: it was receiving tiles, not the catalog, and the warning named coverage_catalog and meant exactly what it said. That cost time before I caught it. (c) I reported only ONE orphaned renderer this morning when there were two - ros2 node list piped through head -15 truncated /state_renderer, which sorts after /rqt_diagnostics. (d) My 08:58 'no weirdness from the gabby udp_bridge update' covered link health and throughput only, NOT QoS propagation, so it never ruled the update out for the catalog durability problem. (e) I restarted the memory-only coverage_renderer for a cache_control change without first confirming the catalog was reaching it - that is why the public page went sparse mid-survey; its own README says there is no warm load and no disk cache.

**2026-08-26 17:30 -04:00** — STATE A WRAP-UP READER NEEDS. This deployment was started ISSUE-LESS per ros2_agent_workspace#533 - the header still carries 'Deployment issue: pending (backfill from a dev host)' and a dev host must create and link the issue, anchored on this log's filename date 2026-08-26, before /wrap-up-deployment can discover it via gh issue list. No pre-flight section: field side skips tides/currents/weather by contract. HOOKS CAVEAT unchanged from 2026-08-25 - no pre-commit hooks ran on any commit today. --no-verify was NOT used and nothing was bypassed; pre-commit is simply not installed on pandy, neither on PATH nor in the workspace .venv, and neither repo has .git/hooks/pre-commit wired. Unmet by absence, not evasion. What WAS run instead: ament_flake8 and ament_pep257 clean on the launch files, 423 marine_web_view tests passing after every change, 309 camp tests passing after the #217 build, and a structural balance check on the edited index.html JavaScript. Today's raw data is on gabby, not pandy; the crash log worth keeping is ~/.ros/log/camp_crash_639096.log.

**2026-08-26 18:07 -04:00** — WHAT IT TOOK TO GET A REFERENCE LAYER FOR SHOALS - three manual steps, none of them supported by existing tooling, because build_bathy_store.sh accepts exactly ONE --reference-tif and no Shoals chart prior existed. (1) EXPORT: ros2 run s57_to_geotiff s57_to_geotiff ~/data/world/charts/ENC_ROOT ~/data/world/chart_prior --geoid ~/data/world/datum/geoid/us_noaa_g2018u0.tif --vdatum-dir ~/data/world/datum/vdatum. All 13 cells exported, VDatum resolving (US4NH1BD alone 3.6M valid cells), output is 13 two-band GeoTIFFs at GGGS levels 6, 7 and 8 - one per cell, at the level each cell compilation scale warrants. (2) MOSAIC: no gdal CLI on pandy (same as salmon), so via python GDAL 3.8.4 - gdal.BuildVRT(resolution='highest', srcNodata='nan', VRTNodata='nan') then gdal.Translate to ~/data/world/shoals_chart_prior.tif, 18432x9220, 2 bands Float64, finest pixel 0.0000326 deg (~3.6 m), 0.09 GB DEFLATE. Sanity checked: band 1 -110.3..-26.5 m ellipsoidal, which against the -28.03 m chart datum the coverage renderer reported is roughly 0..82 m of water; band 2 sigma 0.5..27.5 m, 0.5 being CATZOC A1 and no 1000 m D/U zones present. (3) IMPORT via build_bathy_store.sh --reference-tif - which is where it went wrong, see the next entry.

**2026-08-26 18:07 -04:00** — THE MOSAIC STEP IS THE SMELL, AND IT IS LOSSY. Flattening 13 cells that live at THREE DIFFERENT GGGS LEVELS (6, 7, 8 - assigned by each cell compilation scale, with coarse cells already clipped by finer cells M_COVR coverage so they do not overlap) into ONE single-resolution raster throws away exactly the multi-level structure the store's own model exists to represent. resolution='highest' was chosen so the fine cells are not degraded, but that upsamples the level-6 cells ~4x for nothing and produces a 170 Mpx raster where the source was 13 right-sized ones. The store then resamples that back down to its own 1 m grid at import. Two resamplings and a level-structure loss to move data that was already in the right shape.

**2026-08-26 18:08 -04:00** — BUG THAT CORRUPTED THE REFERENCE, run aborted at 4% - build_bathy_store.sh passes the MASSABESIC LAKE DATUM CONSTANTS UNCONDITIONALLY to the reference import: --depth-scale $CHART_DEPTH_SCALE (-1) --depth-offset $CHART_DEPTH_OFFSET (48.88, the lake full-pool ellipsoidal height), at lines 328-330, with no branch on band count. Those exist to convert NH GRANIT positive-down LAKE depths into ellipsoidal height. An s57_to_geotiff product needs NO conversion - its own README says band 1 is already WGS84 ellipsoidal height and is 'exactly the band semantics import_geotiff expects, so no --depth-scale / --depth-offset conversion is needed at import'. Applied anyway, z = -1*band1 + 48.88 turned the seafloor into a plateau ~130 m too high. VERIFIED IN A WRITTEN TILE, not inferred: reference/10_17779_13971.tif band 1 reads +75.86..+79.96 m where it should read about -56 m. The gate would have compared every real sounding against a surface 130 m out - worse than no gate, because it looks like it is working. Only UNC_ARGS is conditional on band count, so the 2-band path is half-implemented: it knows to skip --uncertainty for a 2-band product but still applies the 1-band lake conversion. CHART_DEPTH_SCALE/OFFSET/PLATFORM/SENSOR/SURVEY are plain assignments, not ${VAR:-default}, so none of this is overridable from the environment - fixing it means editing the script.

**2026-08-26 18:08 -04:00** — TWO MORE DEFECTS THE SAME BLOCK CAUSED. (a) WRONG PROVENANCE - the reference import stamps the store registry.json with --platform nh-granit --sensor edp-bathymetry-lakes --survey massabesic-2026 from the same hardcoded block, so a NOAA ENC Shoals prior is recorded as NH GRANIT Massabesic lake bathymetry. (b) SELF-REJECTING STORE - marine_bathymetry_store migrates a legacy survey/ layer to processed/ BY DESIGN (tile_io.cpp:315, pinned by test_tile_io.cpp:503), but build_bathy_store.sh's own preflight refuses any store containing chart/draft/processed as 'pre-#96 legacy content'. So the store this script produces is one the same script will refuse to touch on the next run. Those two are independent of the datum bug and would survive fixing it.

**2026-08-26 18:08 -04:00** — OPERATOR DIRECTION - THIS SHOULD CONSUME EXISTING WORLD-STORE LAYERS, NOT A BESPOKE MOSAIC. The prior I hand-built already exists on this host in the right shape: ~/data/world/store/chart/ holds 38 ENC-derived GeoTIFF tiles at z6/7/8 plus editions.json, regenerated wholesale by enc_updater from the same 13-cell ENC_ROOT and kept current (last download and regen 2026-08-21T00:22Z, last_error null, per .updater_health.json). ~/data/world/datum/ holds the MENHMAgome23 VDatum grids and GEOID18 that the export needs. So the workspace ALREADY maintains, on a cron, exactly the product build_bathy_store.sh made me rebuild by hand - and it maintains it multi-level, which is the form the store wants. The right shape is for the store build to point at a world-store LAYER (chart, or any other existing bathy layer such as a prior survey's processed/ output or an S-102 import) and prime the gate from it directly, instead of taking a single --reference-tif that a human had to flatten. That also removes the datum bug's whole class: a store layer already carries its own datum and provenance, so there is nothing to convert and nothing to mis-stamp.

**2026-08-26 18:08 -04:00** — THE CROSS-LEVEL CATCH THAT MAKES THIS NON-TRIVIAL, and it is the same one from this morning's first question. cube_bathymetry primes the gate from BOTH prior layers in SourceLayer order - chart/ first, then reference/ overwrites where they overlap - but the two behave differently: a coarser REFERENCE tile is picked up by the seedNewTile level-walk fallback (#115) that resamples the finest available coarser tile onto the fine survey cells, while CHART has NO cross-level fallback at all ('only an exact survey-level chart tile is primed; coarse-chart resampling is deferred'). The world store's chart layer is z6/7/8 and a 1 m survey is L10, so pointing the gate at chart/ as it stands today would prime ZERO tiles and gate nothing - silently, since the offline tools print only 'Reference-prior seeding from <dir>' and never report primed or level-mismatched counts (unlike the live node, which warns explicitly at cube_bathymetry_node.cpp:254). So consuming the world chart layer needs the deferred coarse-chart resampling implemented first, OR the world store gaining a reference/ layer alongside chart/. Filed as the top wrap-up item for the store pipeline.

**2026-08-26 18:08 -04:00** — STORE IMPORT STATE LEFT AT END OF DAY - NOT BUILT, deliberately. The aborted run's store at ~/data/stores/shoals/bathymetry holds a CORRUPTED reference layer (+78 m plateau) and an empty processed/, and must be DELETED rather than resumed - the bad prior is already written and a resume would gate against it. Nothing else is lost. What IS on disk and good: 56 GB of survey bags pulled from salmon (30 GB bizzyboat_sonar + 26 GB bizzyboat, 18 sessions each from 2026-08-20, mirroring salmon's layout so BAG_ROOT needs no override), the 13 per-cell ENC exports at ~/data/world/chart_prior/, and the mosaicked 2-band prior at ~/data/world/shoals_chart_prior.tif whose VALUES ARE CORRECT - it was the import that corrupted them, not the export. A dry run selected 16 bags (2 skipped, no detections) and the command it would issue is recorded in the build log. So the rebuild needs only: delete the store, fix the datum flags, re-run. Estimated a few hours from the observed rate (4 percent in about 6 minutes, 673k pings).

**2026-08-26 18:10 -04:00** — BAD STORE DELETED at operator instruction - rm -rf ~/data/stores/shoals, removing the 3.9 GB aborted build (corrupted +78 m reference layer and an empty processed/). Checked before deleting: ~/data/stores contained ONLY that shoals tree, created by me today, and nothing was running against it. The empty ~/data/stores parent is kept. NOTHING ELSE TOUCHED - the 56 GB of raw survey bags are intact (30 GB bizzyboat_sonar + 26 GB bizzyboat), as are the 13 per-cell ENC exports in ~/data/world/chart_prior/ and the 88 MB mosaicked 2-band prior ~/data/world/shoals_chart_prior.tif, whose values are correct. 321 GB free. The store rebuild, once the datum flags are fixed, needs no re-pull and no re-export.

**2026-08-26 18:10 -04:00** — END OF DAY. Deployment mode stays active for this session; the deployment remains OPEN and issue-less, header still 'Deployment issue: pending (backfill from a dev host)' - a dev host must create and link the issue anchored on this log's date 2026-08-26 before /wrap-up-deployment can find it. Left running: three web-view renderers under launch 684347 (state and ais publishing to S3, coverage rebuilding through the VOLATILE catalog workaround), camp with the #217 crash handler installed, and the rosbag2 recorder. All repos clean and pushed. Wrap-up carry-forward is recorded in the 17:30 entry, with the store-pipeline items added at 18:07-18:08: prime the gate from an existing world-store layer rather than a hand-flattened tif, which needs coarse-chart cross-level resampling or a reference/ layer in the world store first.

**2026-08-26 18:14 -04:00** — WEB VIEW RENDERERS STOPPED at operator instruction - SIGINT to launch 684347, which reaped all three (state 684350, coverage 684351, ais 684352). Verified clean: no marine_web_view processes, no renderer nodes in the ROS graph, no orphans. Note this leaves camp as the only live-coverage consumer, and camp still cannot receive the catalog (its TRANSIENT_LOCAL subscription vs the relay's VOLATILE) - it keeps painting from its disk cache and from tiles pushed to it, which is fine with the boat docked.

**2026-08-26 18:14 -04:00** — ACTION ITEM - THE WEB VIEW RENDERERS SHOULD BE IN THE STANDARD OPERATOR LAUNCH, not started by hand. Every time they ran today they were launched manually, and every problem that cost time flowed from that: they were absent at the start of the survey, restarting them by hand emptied the memory-only coverage renderer at the worst moment, and two separate launch aborts left orphaned renderers reparented to init still requesting tiles over the survey link. The right home is bizzyboat_project11/launch/operator_core_launch.py, which already includes ais_launch.py, publish_state_launch.py, udp_bridge_launch.py, bag_recorder_operator_launch.py and network_monitor_operator_launch.py. ais_launch.py in particular is the very chain that PRODUCES /ais/contacts, so the renderer that publishes it belongs alongside. web_view_launch.py (committed today, 7441896) is already the single entry point that brings all three up with the right platform namespaces and frames, so the change is one IncludeLaunchDescription plus an enable argument so a station that should not publish to S3 can opt out. Doing that also makes the renderers start and stop with the rest of the operator stack, which is what removes the orphan class entirely.
