# 2026-08-26 — pandy log (BizzyBoat deployment — issue pending)

Deployment issue: pending (backfill from a dev host)
Host: pandy
Side: field
Started: 2026-08-26 08:43 -04:00

## 2026-08-26

**2026-08-26 08:43 -04:00** — DEPLOYMENT MODE ACTIVATED on pandy, field side (origin git@gitcloud:field/unh_echoboats_project11.git), repo on jazzy, clean tree. Issue-less start per ros2_agent_workspace#533 - git-bug lookup skipped because the boat was already live and on the water; header carries the canonical pending marker for dev-side backfill. Prior deployment #459 was closed out 2026-08-25 16:29 (TIED UP, committed 2c9387f, pushed fa6beec), so this is a new deployment rather than day 2 of that one.

**2026-08-26 08:43 -04:00** — MARINE_WEB_VIEW BROUGHT UP LIVE TO S3. make sync earlier today pulled unh_marine_autonomy 4b44412..ddf13c3 (PR #358 / issue #357), adding the ais_renderer node plus a 251-line AIS layer in web/index.html. Built marine_web_view in core_ws with --symlink-install. All three renderers (state, coverage, ais) now run against bizzy publishing to s3://unh-ccom-p11-live via profile p11-renderer.

**2026-08-26 08:43 -04:00** — FINDING - ais_renderer_launch.py as merged in #357 could not launch at all. ParameterValue rejects a bare python list as value_type; launch_ros coerces only against int/bool/float/str and their typing.List forms, so the file raised Unrecognized data type: <class list> and died before spawning any node, whatever ignore_mmsis was set to. Fixed to List[int] (commit d3b1642 on unh_marine_autonomy jazzy). Verified both directions: default empty list starts clean, and ignore_mmsis:=[366000001,367000002] logs never publishing MMSI 366000001, 367000002. NOT caught by the 423-test suite because test_launch_params.py parses the launch files as TEXT rather than executing them - a launch file can be syntactically fine, pass every param-forwarding assertion, and still be unable to run.

**2026-08-26 08:43 -04:00** — FINDING - sibling launch includes leak configurations into each other. Wrote web_view_launch.py to bring all three renderers up together (commit 7441896). First dry run showed ais_renderer reporting /bizzy/ais/contacts -> /tmp/position.geojson every 1.0s: it had inherited state_renderer local_path and interval, because the three included files declare overlapping argument names and DeclareLaunchArgument does not overwrite a configuration already set, so the first include to declare a name wins it for every later sibling. Live this also leaks key, and ais would have PUT its contact collection over live/position.geojson - taking the boat off its own map. Fixed with scoped=True GroupAction per include; re-verified each renderer keeps its own key, local_path and interval.

**2026-08-26 08:44 -04:00** — FINDING - AIS topic default was wrong for the operator station. web_view_launch.py originally derived contacts_topic from platform, giving /bizzy/ais/contacts. That topic is real but boat-side - it is what the recorded bags carry, because BizzyBoat has its own receiver. pandy runs its OWN shore chain (/ais/nmea_relay -> /ais/ais_parser -> /ais/ais_contact_tracker) publishing the global /ais/contacts, which is what the ais_renderer README documented all along. Derived, the renderer subscribed to a topic that does not exist here and would publish an empty collection forever - which reads on the page as a quiet river, not as a misconfiguration. Fixed in commit 22046b7; contacts_topic no longer derived from platform.

**2026-08-26 08:44 -04:00** — OPERATIONAL HAZARD - an aborted ros2 launch can leave already-spawned children running, reparented to init. My first web_view_launch dry run at 06:39:28 aborted on the ais ParameterValue error above; launch died but state_renderer (pid 586475) and coverage_renderer (pid 586476) had already spawned and survived with PPID 1 for 72 minutes. Both were dry_run=true so nothing reached S3, but the orphaned coverage_renderer kept publishing TileRequest every 5s at up to 256 tiles per request, so once the bridge came up at 07:12 the boat was serving DOUBLED tile traffic over the survey link alongside the operator stack. Also note I initially reported only one orphan: ros2 node list piped through head -15 truncated /state_renderer, which sorts after /rqt_diagnostics. Both killed at operator request; coverage_requests confirmed back to 0 publishers afterwards. Check pgrep -af marine_web_view/(state|coverage|ais)_renderer after any non-clean launch exit.

**2026-08-26 08:44 -04:00** — AIS PIPELINE VERIFIED WORKING END TO END. Measured over 30s on pandy: /ais/nmea 77 msgs, /ais/messages 76, /ais/contacts 44 (~1.5 Hz), /ais/atons 29. ais_renderer is 1 of 2 subscribers on /ais/contacts. Socket sampling shows a FRESH https connection to S3 per upload cycle (ais_renderer local ports 48252 -> 51454 -> 38526 across three samples), so PUTs are being sent and not erroring; the intermittent CLOSE-WAIT is S3 closing idle keep-alives between 10s ticks and never accumulates. state_renderer independently confirmed live by its rosout line: track hit the 1200 point cap, oldest fixes trimmed - it only reaches that after 1200 real position fixes. No credential or upload errors on /rosout across 45s. CAVEAT: this confirms PUTs are sent and not failing, NOT that the objects were read back - p11-renderer is scoped to s3:PutObject on live/* and returns AccessDenied on list_objects_v2 for both the root and live/ prefixes, so bucket contents cannot be verified from this host.

**2026-08-26 08:44 -04:00** — HEADLINE FINDING - the deployed web page predates the AIS feature, so nothing AIS shows on it despite the pipeline being healthy. Operator confirmed by inspection: no AIS row in the readout and no AIS traffic checkbox at all (as opposed to the layer being present and reading offline or no contacts, which would have meant a fetch or data fault instead). The AIS layer went into web/index.html at 2026-08-26 03:43 as part of #357 and reached pandy only in this morning make sync. NOTHING IN THE REPO PUBLISHES THE PAGE: refresh_chart_tiles.py covers chart tiles only, and the renderers upload only their own artifacts (live/position.geojson, live/track.geojson, live/ais.geojson, live/coverage/*). Building the package installs index.html into install/ but does not deploy it, so CloudFront still serves the pre-AIS page and live/ais.geojson is being written correctly with no page code to read it.

**2026-08-26 08:44 -04:00** — BLOCKED ON PAGE DEPLOY - two unknowns, both for the operator, deferred rather than guessed. (1) CREDENTIAL: index.html sits at the bucket ROOT (artifact paths in the page are relative, live/ais.geojson), but p11-renderer is scoped to s3:PutObject on live/* so it almost certainly cannot write the page; the other configured profile is ccom-jhc. Did not test-write against production to find out. (2) CACHE-CONTROL: freshness is per-object max-age and CloudFront invalidation is deliberately NOT used (README: billed per path, would dominate every other cost at any real update rate). A PUT replaces the object metadata, so deploying sets the page max-age, the current value cannot be read back from this host, and getting it wrong either pins the stale page at the edge for a long TTL or makes every viewer refetch constantly. Expect a delay after any deploy: without invalidation, edges serve the cached copy until the OLD object TTL expires.

**2026-08-26 08:44 -04:00** — GAP FOR WRAP-UP - marine_web_view has no page-deploy path at all: no script, no make target, no documented procedure anywhere in the workspace (grepped README, docs/ and scripts/ across all project repos). The README documents the renderers thoroughly but never says a web/index.html change needs publishing separately from them, which is exactly how a merged, built, running feature stayed invisible on the live page. Three commits sit on unh_marine_autonomy jazzy UNPUSHED (gitcloud origin, field mode): d3b1642 the launch fix, 7441896 the umbrella launch, 22046b7 the AIS topic default. Deferred to wrap-up per sterile cockpit: a page-deploy script or documented one-liner, a README note that the page deploys separately, and a test that EXECUTES each launch file rather than parsing it as text.
