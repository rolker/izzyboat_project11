# 2026-06-16 — gabby log (BizzyBoat deployment — issue TBD)

Deployment issue: TBD — no `deployment`-labeled issue exists for the 2026-06-16 Lake Massabesic standard survey. Field hosts have no GitHub access to create one; two stale open deployment issues (`0f2da64` shakedown, `a429b20` 06-10 sensor) predate today and were not adopted.
Host: gabby
Side: field
Started: 2026-06-16 10:32 -04:00

> **Issue not yet created.** Standard Lake Massabesic survey day; no
> deployment issue exists for it and field side cannot create issues. Create
> the deployment issue from a dev host (`/start-deployment` on deadpool, dev
> side), run `issue_sync.dev_push`, then `field_pull` here and stamp the issue
> ID into this header and add `- [gabby log](docs/logs/2026/2026-06-16_gabby_logs.md)`
> under the issue's `## Logs` section.
>
> **Site note:** Lake Massabesic is freshwater — no tidal/current signal.
> Pre-flight tides/currents are skipped for this deployment (dev-side only
> regardless).

## 2026-06-16

**2026-06-16 10:32 -04:00** — Deployment mode activated on gabby (session start) — standard Lake Massabesic survey day. No deployment issue (field side can't create one); issue creation deferred to a dev host. Urgency contract loaded for this session. Repo on `jazzy`, tree clean. Today's `make sync` + `make build` already run (all 5 layers green); sync pulled a `bizzyboat.yaml` retune, `massabesic_datum_polygons.yaml`, and launch/URDF updates — relaunch required for anything still running on pre-sync code.

**2026-06-16 10:32 -04:00** — Operator reports **off the dock**, starting sidescan pinging. Survey getting underway.

**2026-06-16 10:34 -04:00** — Operator: sidescan rqt/control-panel controls "not wired up"; asked agent to start transmit directly. Called `set_transmit` service (`std_srvs/SetBool {data: true}`) on `/bizzy/sensors/sidescan/garmin_sidescan` → response `success=True, message='transmitting'` (the node only reports success when the GCV TCP transmit-on frame actually sends). Verified flow via the node `~/status` string: `tx=ON`, pings(port/stbd/down)=656/656/656 — imagery decoding on all three channels. Control-panel wiring (udp_bridge `~/change_state` / `~/state` path) is the open item to chase at wrap-up; transmit itself is good.

**2026-06-16 10:36 -04:00** — Operator: set sidescan range to 8 m. Set via `ros2 param set …/garmin_sidescan range_m 8.0` → "Set parameter successful"; verified with `param get` → `8.0`. The node sends the GCV range command inside the param-set callback and rejects the set if the hardware send fails, so the accepted value means the device received it.

**2026-06-16 10:38 -04:00** — Operator: set sidescan range to 30 m. `param set range_m 30.0` → success; `param get` → `30.0`. Device received it (same accept-after-send guarantee).

**2026-06-16 10:41 -04:00** — Operator: **running the first survey line.** Sidescan confirmed live at line start — `~/status`: `tx=ON`, range 30 m, pings(port/stbd/down)=19757/19757/19757 (all three channels decoding).

**2026-06-16 11:15 -04:00** — Operator: record 5 min of video. Ran `record_camera_topics.sh 300` (4 OAK cameras ffmpeg + segmentation raw/compressed/camera_info + TF + diagnostics + robot_description + local costmap). Recorder subscribed to all topics and is recording → `~/data/logs/bizzy_images/bag_2026-06-16T11.15.47_ffmpeg_seg`. Self-terminates at 300 s (~11:20:47).

**2026-06-16 11:20 -04:00** — Recording complete (exit 0, clean rosbag2 flush). Bag: `~/data/logs/bizzy_images/bag_2026-06-16T11.15.47_ffmpeg_seg/` — 221 MB mcap + metadata.yaml.

**2026-06-16 11:25 -04:00** — Operator asked whether the M3 `.all` recording (new in `kongsberg_em_bridge`, marine_tools sync) can be disabled on the fly. **Finding: no runtime disable exists.** `save_all_dir` is read once in `__init__` with no `set_parameters` callback and no service, so `ros2 param set save_all_dir ''` would report success but keep recording (silent false-success). Only a write failure or node shutdown stops it. Current file `m3_20260616_141738.all` = **1.1 GB**, growing ~1 GB/hr; disk has 1.7 TB free (7% used) — not a space concern. Decision pending; recommended leaving it (the `.all` is the georeferenceable M3 record) and disabling via `save_all_dir:=''` at the next relaunch if wanted. **Wrap-up item:** add a runtime toggle (param callback or `set_recording` service) to `kongsberg_em_bridge` so `.all` capture can be stopped without a node restart.

**2026-06-16 11:28 -04:00** — Operator asked about modifying the node to **break the `.all` file up by time or size**. Agreed it's the better fix than an on/off toggle, but it's sterile-cockpit / out-of-scope mid-survey (code + rebuild + relaunch) — deferred to dev/wrap-up. **Wrap-up item (marine_tools, `kongsberg_em_bridge`):** add `save_all_max_seconds` and `save_all_max_bytes` params (0/empty = off); roll the file in `_record` under `_save_lock` on a datagram boundary into a fresh `m3_<UTC>.all`. **Critical correctness detail:** cache and re-emit the most-recent Installation-Parameters-Start (I/73) + runtime/SVP datagrams at the head of each new segment, or split files won't load/georeference in Caris/Qimera/MB-System. Needs a test fixture. Likely supersedes/absorbs the runtime-toggle item above.

**2026-06-16 11:34 -04:00** — Operator directed: build the time/size rollover **now as a prototype**, deferring the I/73 re-emit question ("not sure the M3 even sends those"). Implemented in `marine_tools/kongsberg_em_bridge/kongsberg_em_bridge/node.py` (field-mode edit on `jazzy`, tree was clean):
  - New params `save_all_max_seconds` (float) + `save_all_max_bytes` (int), default 0 = off, armed independently; exposed in `launch/kongsberg_em_bridge.launch.py`.
  - Rollover runs in `_record` **after** a successful write (segment ends on a whole-datagram boundary). New segment = fresh timestamped `m3_<UTC>.all` via collision-safe `_unique_all_path` (`_NN` suffix on same-second roll so `'wb'` never truncates).
  - Pure `save_rollover_due()` decision fn extracted for testability; added `test/test_save_rollover.py` (7 tests).
  - Validation: 18/18 unit tests pass (7 new + 11 em_datagrams); repo ament flake8 + pep257 pass. Compiles.
  - **Deferred (per operator):** split segments do NOT re-emit install/SVP (I/73) datagrams at their head; mid-stream files may not load/georeference in Caris/Qimera until that's added. First segment always complete.
  - **NOT yet activated or committed.** Activation needs a `kongsberg_em_bridge` relaunch (interrupts M3 bridging + closes the live 1.1 GB file) — to be done at a between-lines break, not mid-line, on operator's call. Commit to `jazzy` pending.

**2026-06-16 11:41 -04:00** — Established the M3 bridge is **not** `respawn` (perception_launch.py M3 block had only `emulate_tty=True`), unlike the boat's other UDP/serial drivers (cameras/SBG/sidescan/ntrip/sound_speed all `respawn=True`). Also, the launch block didn't set the new rollover params, so a relaunch alone wouldn't arm rollover. Operator directed: edit the launch file now + add respawn. Done in `bizzyboat_project11/launch/perception_launch.py` M3 block:
  - Added `save_all_max_bytes: 1000000000` (~1 GB ≈ hourly segments; size chosen by agent, easily changed) + explicit `save_all_max_seconds: 0.0`.
  - Added `respawn=True, respawn_delay=5.0` (matches the other drivers; a respawn opens a fresh timestamped `.all` segment, never overwrites).
  - Compiles; rebuilt `bizzyboat_project11` (--symlink-install) to stage the launch file; verified the installed copy has both edits. Running nodes untouched.
  - **Activation still pending a perception-group relaunch** (between lines). Both repos' changes (marine_tools node + bizzy launch) uncommitted; both are field-mode (gitcloud).

**2026-06-16 11:44 -04:00** — Operator: rotate at 200 MB. Changed `save_all_max_bytes` 1000000000 → `200000000` (~12 min/segment at current rate) in perception_launch.py; recompiled, rebuilt bizzyboat_project11, verified installed copy. Still pending relaunch to activate.

**2026-06-16 11:46 -04:00** — Operator: reduce M3 node info output. In `kongsberg_em_bridge/node.py` `_publish`, replaced the per-100-ping info log (fires every few seconds at survey rates) with a time-throttled heartbeat (`throttle_duration_sec=30.0`). Compiles; 20/20 tests + ament flake8/pep257 pass. Python/symlink-installed, so it takes effect on the same pending perception-group relaunch as the rollover/respawn changes.

**2026-06-16 11:52 -04:00** — **INCIDENT: boat drifted off the survey line.** Operator reported drift while line-following; took **manual control** during triage. Read-only findings at time of drift:
  - FCU healthy: `mavros/state` connected, armed, **GUIDED**, mode GUIDED. `controller_server` lifecycle **active**.
  - Controller output `/bizzy/cmd_vel_nav` publishing at 5 Hz but commanding **x=0.0, z=0.0** — autonomy itself commanded zero motion (→ boat drifted with wind/current). Gated `/bizzy/cmd_vel` silent/zero (consistent).
  - `mavros/global_position/raw/fix` **status: 0** (basic fix, NOT RTK-augmented). RTK fix_type (`gpsstatus/gps1/rtk`) not captured before manual takeover.
  - Helm gate node `/bizzy/ca_safety` present; not implicated (zero originates upstream at the controller, not the gate).
  - **Not** a mode dropout and **not** a thrust failure — autonomy commanded zero. Leading hypothesis: **localization degradation** (RTK→plain fix can collapse the CrabbingPathFollower solution toward zero); alt: controller lost path / goal-reached. **RCA deferred to wrap-up** (needs path/odom/goal-state + RTK history; manual control has mitigated). Capture-a-bag offered.

**2026-06-16 11:58 -04:00** — **Root cause found (cmd_vel chain traced).** Chain is intact — symptom is inside `marine_nav_crabbing_path_follower::CrabbingPathFollower`, not a broken link:
  - Verified: `controller_server` active, `received_global_plan` @5 Hz (controller HAS a path), `FollowPath.default_speed`=1.54 m/s (wants to drive), no `/bizzy/speed_limit` publisher, no controller error spam. Chain "anomalies" were red herrings: `/bizzy/cmd_vel` 0-subs = `docking_server` orphan (docking inactive); `cmd_vel_nav` 7 publishers = controller_server + 6 behavior_server plugins (normal). My `map→base_link` TF check was malformed (frames are `bizzy/...`, not `map`) — no TF fault claimed.
  - **Mechanism** (`crabbing_path_follower.cpp` setPlan ~L340-378 + L369 comment): cursor `current_segment_` == `segment_count` is the **"done" sentinel → zero cmd_vel by design** (stop at line end). A *same-goal* re-plan **deliberately preserves the cursor** (anti-PID-kick, root-caused 2026-06-04). Therefore:
    - Re-sending the **same mission** kept the cursor at "done" → no motion (matches operator: "resending the mission didn't make the boat move").
    - A **goto override** = new goal → cursor resets → drove to it → "done" again → drifted (matches: "goto did but then drifts").
    - A **new trackline** = goal moved > `new_plan_goal_tolerance_` → `new_line=true` → cursor resets to 0 → follows. **Resolved — fine now.**
  - The drift itself = **no station-keeping in the "done" state**; boat is pushed by wind/current. `max_deviation=6.0` was NOT the trigger.
  - **Wrap-up items:** (1) safety — boat should `Hover`/station-keep at line completion instead of drifting freely (the `marine_nav_behaviors::Hover` BT plugin exists but didn't engage; investigate BT transition); (2) UX footgun — re-sending the identical line won't restart it (cursor stays "done"); operator must send a new line/goto, should be documented or given a restart path; (3) open RCA — did the line genuinely complete or hit "done" early? needs a bag (path + cursor/odom). Resolved operationally via new trackline.

**2026-06-16 12:26 -04:00** — `.all` data status check (operator): **healthy.** Rollover prototype went live with the ~11:52 relaunch (during drift handling) and is rotating cleanly at 200 MB — `m3_all/` has the pre-relaunch 1.48 GB monolith + new 200 MB segments + a growing one (~0.95 GB/hr). M3 pinging (500 kHz, SV 1491 m/s), detections flowing on `/bizzy/sensors/m3/detections` (BEST_EFFORT — needs matching QoS to echo; `ros2 topic hz` default RELIABLE shows nothing, not a stall), `detections_to_pointcloud` + `cube_bathymetry` both active. Disk 1.7 TB free. Respawn + quiet-logging changes also confirmed live.

**2026-06-16 12:28 -04:00** — Operator: **CA false-positiving a lot.** `/bizzy/ca_safety` (helm gate) subscribes `/bizzy/collision_monitor/pointcloud` (reflex cloud from OAK sea-surface segmentation, frame base_link_level), gates `cmd_vel_smoothed`. **Root vulnerability: no minimum-points / clustering threshold** — params are purely zone geometry (stop 5 m×4 m; slowdown 5–20 m×6 m), reverse, TTC, timeouts. A *single* phantom point in a zone triggers it. Input cloud had 129 points → high chance of glint/wake/chop returns in the large 20 m×6 m slowdown corridor. Options presented: (1) runtime relief — shrink slowdown corridor (`slowdown_max_length`20→10, `slowdown_width`6→4), keep stop zone (reversible, not durable, verify-after-set); (2) upstream — reduce phantom output from `segments_to_pointcloud_reflex`; (3) code — add min-points/cluster gate to `ca_safety`. Safety-critical → awaiting operator decision before any CA change. **Wrap-up items: #2 source tuning + #3 min-points filter in ca_safety.**

**2026-06-16 12:34 -04:00** — Operator decision: **set CA ranges to zero — "we are watching vigilantly"** (operator risk-acceptance, manual oversight). Literal zero **rejected** by ca_safety (`<param> must be finite and > 0` — node has a live validating param callback). Honored intent via epsilon: set `stop_length`=0.1, `slowdown_min_length`=0.1, `slowdown_max_length`=0.2, `reverse_distance`=0.1 (all "Set parameter successful", verified via `param get`). Sub-meter zones → CA effectively disabled (nothing realistic falls inside). **NOT durable** — a nav/perception relaunch restores 5/5/20/3. **Originals to restore: stop_length=5.0, slowdown_min_length=5.0, slowdown_max_length=20.0, reverse_distance=3.0.** ⚠️ CA is OFF — re-enable before any unattended autonomy.

**2026-06-16 12:35 -04:00** — Operator asked for the "front USB camera" usb-bridge settings. Clarified: the OAK forward nav camera is **PoE** (`oak_forward.yaml` i_ip 192.168.20.10), not USB; all 4 OAKs are PoE (.10/.9/.12/.11). The actual USB cam is `/bizzy/sensors/cameras/usb/usb_camera` (ROS `usb_cam`, `usb_camera_launch.py` + bizzyboat.yaml): /dev/video0, 640×480 @5 fps, pixel_format mjpeg2rgb, av_device_format YUV422P, io_method mmap, frame bizzy/usb_camera_optical, uncalibrated; transport jpeg @~800 kbps, keyframe 64.

**2026-06-16 13:14 -04:00** — **Second drift event — root cause: RC override.** Operator-reported: boat started drifting again; someone then noticed it **was not in GUIDED** — the **RC controller, at the fringe of its range, kicked the FCU into a different mode**, dropping autonomy control → drift. Mitigation: **RC controller turned off.** Factual context: distinct from the ~11:52 drift (which was measured in GUIDED with controller_server active but cmd_vel_nav=0 → CrabbingPathFollower "done" cursor). This event is an FCU mode change out of GUIDED, not a controller-internal zero — a different mechanism. (Whether the first drift also had an intermittent RC mode-flip that had recovered to GUIDED before I sampled mavros/state is unconfirmed.) **Wrap-up item:** review RC/mode-switch + RC failsafe behavior (ArduRover) so a fringe-of-range RC signal can't silently knock the boat out of GUIDED mid-mission — e.g., RC-override/mode-channel config or FS_OPTIONS so loss/degradation holds autonomy rather than swapping mode.

**2026-06-16 13:18 -04:00** — Operator: CA-off worked but sea-surface false positives now reach the **costmap**, bending survey lines → "make the costmap less sensitive." Architecture: local `sea_surface_layer` (`SeaSurfaceLayer`) fuses 4 OAK segmentation streams → probabilistic evidence grid → publishes `/bizzy/sea_surface/lethal_grid`; global `sea_surface_relay` (`SeaSurfaceRelayLayer`) relays that grid into the planner costmap. All tuning is on the **local** layer (feeds both). Reduced sensitivity on `/bizzy/local_costmap/local_costmap` (all set successful, `param get`-verified): `sea_surface_layer.max_evidence_step` 0.85→**0.4** (one frame can't mark; needs persistence), `lethal_threshold` 0.9→**0.95**, `decay_half_life_s` 60→**20** (phantom marks clear ~3× faster). **Originals: max_evidence_step=0.85, lethal_threshold=0.9, decay_half_life_s=60.0.** NOT durable across relaunch. Untouched next levers: `maximum_range` 150→~50, `obstacle_prob_min` 0.7→higher. Runtime-vs-startup application by the custom layer unconfirmed — watch if lines straighten. **Wrap-up:** durable FP rejection belongs in segmentation/SeaSurfaceLayer config, not runtime epsilon.

**2026-06-16 13:20 -04:00** — Operator: surveying at 3.5 kt (was 3 kt earlier), **boat oscillating** (line-follow hunting). Diagnosed (read-only, no change applied): `controller_server` FollowPath `default_speed` bumped **1.543→1.800 m/s** (3.0→3.5 kt); cmd_vel_nav x≈1.87, odom x≈1.74 m/s. **Cause:** lookahead not velocity-scaled — `lookahead_time=0.0`, `lookahead_distance=0.0` → effective lookahead = `lookahead_min_distance`=1.0 m fixed (~0.55 s ahead at 1.8 m/s); with `pid.p=-20` (stiff), over-corrects → hunting. Tolerable at 3 kt, under-damped at 3.5 kt. **Fix options (not applied — held):** set `lookahead_time≈1.0–1.5 s` (velocity-scaled, preferred), soften `pid.p`, or revert to 3 kt. **Wrap-up:** lookahead should scale with commanded speed by default.

**2026-06-16 13:20 -04:00** — Operator: **CAMP crashed** (topside display/control app). Boat-side unaffected — autonomy, nav stack, sensors, and all recording (camera bags + M3 .all) continue on gabby. Control sets repopulate from the boat's heartbeated topics on CAMP reconnect; restart is operator-station-side (nothing actionable from gabby). ⚠️ Reminded operator: CA off + CAMP down = reduced avoidance + visibility at 3.5 kt; suggested easing speed / holding until CAMP back (operator's call). CA restore values on hand: 5/5/20/3.

**2026-06-16 13:24 -04:00** — Operator-reported: killing CAMP dropped the **camera streams**; CAMP respawned and streams resumed ~1–2 min later. Suggests camera publishing is **consumer-coupled** (lazy/on-demand to the topside subscriber) and recovery waits on OAK respawn + zenoh re-discovery. My `ros2 topic hz --qos-reliability best_effort` confirm was inconclusive (the flag isn't honored by `hz`; not evidence of a fault). **Wrap-up item:** camera streaming shouldn't depend on a topside consumer being alive / recovery shouldn't take minutes — review lazy-publish + zenoh discovery behavior.

**2026-06-16 13:26 -04:00** — Operator: data-driven PID tweak for line following. Captured 12 s of `/bizzy/FollowPath/pid/pid_state` (~55 samples @5 Hz). **Cross-track `p_error` = sustained limit cycle: ~±1 m amplitude (peaks +1.15 / troughs −0.78, mean +0.1), ~10 s period (~18 m wavelength @1.8 m/s), non-decaying → under-damped.** `output` (yaw) swings −23..+15. Gains P=−20, I=−0.5, D=−0.6 → |D/P|≈0.03 = almost no damping; worsened by 3→3.5 kt with fixed 1 m lookahead (shorter in time). **Recommended small tweak: `pid.d` −0.6 → −1.2** (add damping; reversible, not durable), then re-measure; alternatives `pid.p` −20→−15, or root fix `lookahead_time` 0→~1.0 s (velocity-scaled). Awaiting operator go before applying.

**2026-06-16 13:30 -04:00** — Operator: "try it." Applied `FollowPath.pid.d` −0.6 → **−1.2** (set successful, `param get`-verified; `pid_state` `d_term: -1.2` confirms it reached the **live** controller — not a startup-only no-op). 14 s re-capture: cross-track span ~1.97 m (peaks +1.00 / −0.97, mean −0.11) vs before ~1.93 m (peaks +1.15 / −0.78, mean +0.10) — positive overshoot down slightly, trough deepened, **net amplitude ~unchanged in the short window**. **Operator reports it did improve** (eyes-on, longer span = better judge than my 14 s snapshot). Offered longer (30–60 s) capture to quantify steady-state and/or push further (`pid.d`→−1.8, `lookahead_time`→~1.0 s). Not durable across relaunch. **Wrap-up:** if the tuned gains hold up, fold into nav2 YAML (durable); the ~10 s limit cycle root cause (fixed 1 m lookahead, not velocity-scaled) still merits the `lookahead_time` fix.

**2026-06-16 13:36 -04:00** — Boat still weaving after pid.d tweak + line change. Tried `FollowPath.lookahead_time` 0→**2.0** (velocity-scaled lookahead, ~3.6 m @1.8 m/s): cross-track got **worse** — span 2.49 m (±1.27) vs ~1.97 m. So the weave is **not** a simple pure-pursuit lookahead limit cycle; more lookahead amplified it. **Reverted lookahead_time → 0** (kept `pid.d` −1.2 per operator preference). Iterative live gain-tuning not converging — handed back per time-box; proper retune (incl. why more lookahead worsens it) deferred to wrap-up with bag analysis.

**2026-06-16 13:38 -04:00** — Operator: set speed limit to 3 kt. Reduced `FollowPath.default_speed` 1.800554 → **1.543332 m/s (3.0 kt)** (original value; set successful, `param get`-verified). Boat slowing: cmd_vel_nav→1.54 m/s, odom 1.71→1.60 settling toward 3 kt. This is the controller's commanded survey speed (effective 3-kt cap); the weave was tolerable at 3 kt earlier, so this is the reliable mitigation independent of gain tuning. Current live controller state: default_speed 1.543 (3 kt), pid.d −1.2, lookahead_time 0. NOT durable across relaunch (would revert to 3.5 kt). **Wrap-up:** if 3 kt + pid.d −1.2 is the chosen config, fold into nav2 YAML; investigate the lookahead-worsens-weave behavior (non-standard for the CrabbingPathFollower).

**2026-06-16 13:44 -04:00** — Steady at 3 kt. Captured pid_state for the clean comparison: **3.0 kt → cross-track RMS 0.05 m, span 0.17 m, output ±3.4** (dead steady); vs **3.5 kt → ±1 m limit cycle, output −23..+15**. A 17% speed rise → ~12× cross-track error + ~7× output = the loop crossing its stability margin. **Diagnosis: speed-dependent loop-gain instability, not under-damping.** Cross-track loop gain scales with boat speed (ẋ_cross ≈ v·ψ_err); controller is near margin at 3 kt, over the edge at 3.5 kt. Explains the trials: D barely helped (not damping-limited), more lookahead worsened it (added lag). Output at 3.5 kt is P-dominated (`pid.p`=−20 × ±1 m ≈ ±20 of ±23). **PID hint = reduce `pid.p`** (e.g. −20→−13, restore margin at speed, looser tracking) — **proper fix = gain-schedule / speed-normalize the cross-track gain** (controller already speed-scales lookahead; gain should too) so 3.5 kt tracks like 3 kt. Offered: live test (3.5 kt + pid.p −13) or bank 3 kt + do gain-schedule at wrap-up. **Wrap-up:** speed-normalized PID gains in CrabbingPathFollower.

**2026-06-16 13:48 -04:00** — Operator: set `pid.p` −13, hold speed at 3 kt. Applied `FollowPath.pid.p` −20→**−13** (verified; `pid_state` p_term −13.0 confirms live). At 3 kt: **stable, no weave**, but looser — cross-track RMS 0.05→**0.24 m**, span 0.30 m, ~0.2–0.3 m steady offset (not oscillation; lower P → larger steady-state error vs ambient set/drift). Expected P-reduction trade-off; reinforces gain-schedule case (−20 tight@3kt but unstable@3.5kt; −13 stable but loose@3kt). 3.5-kt test deferred to operator (will show if −13 bounds the weave). Current live gains: p −13, i −0.5, d −1.2; default_speed 1.543 (3 kt); lookahead_time 0. None durable across relaunch. Fallback offered: intermediate p −16, or restore −20.

**2026-06-16 13:52 -04:00** — Operator: set speed 3.2 kt. `default_speed` 1.543→**1.646222** (3.2 kt; verified, odom settled ~1.65). Captured 16 s @3.2 kt with p−13: **bounded, no limit cycle** — cross-track span 0.52 m (±0.3, RMS 0.18, mean ~0, centered), output **−4.6..+2.3** (vs −23..+15 for the 3.5 kt/p−20 weave). Confirms diagnosis: reduced P restored margin; +0.2 kt recovered with no weave. Residual ~±0.3 m slow undulation (survey-acceptable; room to nudge p back to ~−15 for tighter tracking since well clear of limit cycle). Current live: p −13, i −0.5, d −1.2, default_speed 1.646 (3.2 kt), lookahead_time 0. Not durable across relaunch. Options offered: hold 3.2, push toward 3.5, or p−15 to tighten.

**2026-06-16 13:56 -04:00** — Operator: set speed 3.5 kt. `default_speed` 1.646→**1.800554** (3.5 kt; odom settled ~1.80). Captured 18 s @3.5 kt with p−13: **WEAVE FIXED** — cross-track span 0.50 m (±0.25, RMS 0.16, mean ~0), output **−4.5..+2.2** — vs the p−20 baseline at 3.5 kt of ±1 m limit cycle / output −23..+15. Bounded gentle undulation, ~10× calmer yaw, tracking ≈ 3.2 kt quality. **Diagnosis confirmed: P too hot for speed-scaled loop gain; pid.p −20→−13 restores margin and recovers 3.5 kt with no weave.** Current live tune: pid.p −13, i −0.5, d −1.2, default_speed 1.80 (3.5 kt), lookahead_time 0 — **NOT durable across relaunch.** **Action item:** fold pid.p −13 (+ d −1.2) into nav2 YAML (field-mode edit) to survive relaunch; longer-term wrap-up = speed-normalize the cross-track gain so tracking stays tight at all speeds. Residual ±0.25 m survey-acceptable; p−15 available if tighter wanted.

**2026-06-16 14:00 -04:00** — Operator: try 3.75 kt. `default_speed` 1.800→**1.929167** (3.75 kt; odom ~1.90). Captured 18 s @3.75 kt with p−13: still **bounded, no weave** — span 0.575 m (±0.3, RMS 0.158, mean ~0), output −4.4..+2.9. Excursion grew slightly vs 3.5 kt (span 0.50→0.58) but RMS unchanged. p−13 RMS trend across sweep: 3.0=0.24, 3.2=0.18, 3.5=0.16, 3.75=0.16 — consistent ~±0.3 m, all stable → clean 3.0–3.75 kt operating range on p−13. Current live: pid.p −13, i −0.5, d −1.2, default_speed 1.929 (3.75 kt), lookahead_time 0. Not durable across relaunch (YAML-bank still pending).

**2026-06-16 14:04 -04:00** — Operator: try 4.0 kt. `default_speed` 1.929→**2.057778** (4.0 kt cmd). Two findings: (1) **tracking still clean** with p−13 — span 0.50 m, RMS 0.157, output −1.5..+5.0, no weave (margin still holds). (2) **Boat speed-saturating ~1.98 m/s (≈3.85 kt)** — does NOT reach the 4.0 kt command; cmd-vs-achieved gap widened (3.75 cmd→1.90, 4.0 cmd→1.98), i.e. thrust/speed ceiling ~3.85 kt. Recommended settling at **3.75 kt** (fully achieved, clean, below saturation knee); 4.0 kt effectively unreachable on this hull/load today. Awaiting operator: revert to 3.75 or hold. **Wrap-up note:** platform max speed ~3.85 kt at current load/conditions.

**2026-06-16 14:10 -04:00** — Operator: line change handled stable (capture missed most of the turn but operator confirms stable); "lock in the pid value." **Banked the PID tune durably** in `bizzyboat_project11/config/nav2_overlay.yaml`: added a `controller_server.ros__parameters.FollowPath.pid` override (`p: -13.0`, `d: -1.2`, plus base-matching i/clamps/publisher) — overrides the base nav2_params.base.yaml `p:-20/d:-0.6` for BizzyBoat only, with a comment documenting the speed-dependent limit-cycle rationale. YAML parse-validated, bizzyboat_project11 rebuilt (--symlink-install), installed copy verified. Survives nav relaunch now; running stack unaffected (keeps live -13). **NOT yet committed** (field-mode/gitcloud). **`default_speed` deliberately NOT locked** — still runtime-only (4.0 cmd/~3.85 actual); operator to decide durable survey speed separately. **Outstanding uncommitted field-mode work (2 repos):** marine_tools (M3 .all rollover + respawn-relevant node + tests), unh_echoboats_project11 (perception_launch respawn/rollover/200MB, nav2_overlay PID, host log). Commit at a breakpoint.

**2026-06-16 14:13 -04:00** — Operator: set speed 2.0 (kt, per session convention). `default_speed` 2.058→**1.028889 m/s (2.0 kt)**; verified, boat decelerating (odom 1.75→1.55→1.36 settling toward 1.03). Runtime-only (speed not locked in YAML). Reason not stated; not assumed.

**2026-06-16 14:18 -04:00** — Battery check (operator): **25.63 V** live from `/bizzy/mavros/battery` (current ~0.01 A and percentage −1% read as unconfigured on this FCU — voltage is the only trustworthy field). **Finding (from FCU baseline .param): no battery voltage failsafe configured** — `BATT_LOW_VOLT`/`BATT_CRT_VOLT`=0, `BATT_FS_LOW_ACT`/`BATT_FS_CRT_ACT`=0 (BATT_MONITOR=4, CAPACITY=16000). Boat won't auto-act on low battery → manual watch. Not confirmed against live FCU params (baseline file only). SoC not estimated (pack chemistry/cell-count unverified). **Wrap-up item:** consider configuring battery low/critical voltage + failsafe (or confirm intentional manual-management).

**2026-06-16 14:21 -04:00** — Operator: set sidescan range to 20 m. `ros2 param set …/garmin_sidescan range_m 20.0` → success; `param get` → 20.0 (node accepts only after the GCV range command sends, so device has it).

**2026-06-16 14:26 -04:00** — Operator: sound speed no longer updating. **Diagnosis: SVS sensor input stopped, not software.** `/bizzy/sensors/sound_speed/sound_speed_bridge` ALIVE with publisher up but **0 messages out** (both QoS) → starved. Source = **AML SVS on gabby `/dev/ttyS0`** (9600, RX-only); bridge also fans out Valeport UDP to M3 (mercat:20003). `/dev/ttyS0` device node present → port fine, **sensor stopped streaming** (power/cable/sensor hang — eyes-on check). Knock-on: M3 sound_speed **frozen at 1493.2 m/s** (was 1491.4). Impact: M3 bathy applying stale SV — small refraction error in freshwater unless temp drifts; `.all` records SV used → re-correctable in post with a cast. Did NOT grab the serial port or restart (won't help a dead sensor; bridge auto-recovers when SVS streams again; bridge is respawn-enabled if a node restart is wanted after sensor confirmed alive). **No hardware commands fired (operator has ground truth on the physical SVS).**

**2026-06-16 14:30 -04:00** — Operator asked if restarting the node resets the serial line. Checked bridge diagnostic: **"No reading for 2032.1s"** (~34 min, stopped ~13:52), NOT reconnect errors → port open/healthy, `ser.read` returning empty (no bytes), bridge already auto-reopens on real serial faults (reconnect loop, `with serial.Serial(...)`) and is NOT cycling. **Restart analysis:** reopening /dev/ttyS0 does reset host-side (flush input buffer, re-init termios, toggle DTR/RTS via pyserial default) — but won't wake an RX-only SVS that's silent (no power/command path; line already clean, no wedge, no port error). Only helps in the unlikely case the SVS reset/wake is wired to DTR/RTS. Recommendation: check sensor power/cable first; node restart is a free long-shot only if SVS looks alive but stays silent. Awaiting operator.

**2026-06-16 14:55 -04:00** — Operator: **quick stop at the dock to power-cycle the sound speed probe** (the AML SVS that went silent ~13:52). Survey paused for the hardware fix. Bridge port is open/reading-empty, so no node restart needed — it should auto-resume publishing once the SVS streams again; will confirm sound_speed + M3 SV recover post-power-cycle.

**2026-06-16 15:00 -04:00** — SVS power-cycled (operator) → checked: **still 0 messages**, diagnostic "No reading for 2390s" (climbing), M3 SV still 1493.2. Power-cycle alone didn't restore it. Bridge had held /dev/ttyS0 open continuously ~40 min (only reopens on serial *error*; empty reads don't throw) → restart now indicated to reopen/re-sync to the freshly-powered sensor.

**2026-06-16 15:03 -04:00** — Confirmed `sound_speed_bridge` is `respawn=True` (respawn_delay 2.0) on the node itself (launch lines 51–53 / 74–75). Operator: restart. SIGINT'd PID 230744 → **respawned cleanly (new PID 320338)**, fresh /dev/ttyS0 open. **Still 0 messages; diagnostic "No reading for inf s"** (zero bytes since fresh boot). **Software/host side fully ruled out** — node, port, ROS chain healthy; SVS is not putting bytes on the wire despite power-cycle + clean port reopen. **Handed to hardware:** re-seat serial cable both ends (SVS↔gabby /dev/ttyS0), confirm SVS actually in streaming mode post-power-up (indicator/display), possible SVS fault. M3 continues on frozen SV 1493.2 (re-correctable in post w/ a cast). **Wrap-up:** SVS reliability — it died mid-survey ~13:52 and survived neither power-cycle nor port reopen → suspect cable/connector or sensor.

**2026-06-16 15:08 -04:00** — 2nd power-cycle + 2nd node restart (SIGINT 320338 → respawn PID 321342): **still 0 messages, "No reading for inf s."** Two power-cycles + two clean restarts, zero bytes → **boat-computer side conclusively ruled out**; fault is the physical serial link or the SVS unit (power-cycle can't fix a broken/loose data wire). Recommendation: stop restarting (ineffective ×2); re-seat/inspect serial cable both ends + check SVS came up streaming. Offered raw-port sniff (stop bridge, read /dev/ttyS0 raw) to definitively split cable-vs-sensor, on operator say-so. M3 continues on frozen SV 1493.2.

**2026-06-16 15:06 -04:00** — Operator: **brought down the stack** (intentional full shutdown, not a fault — no collapse diagnosis). (Note: the three entries above timestamped 15:00–15:08 were typed estimates and ran slightly ahead of wall clock; this one is `date`-stamped.) **Relaunch revert/restore checklist** — durable (survive relaunch): nav PID tune (pid.p −13 / d −1.2, nav2_overlay.yaml); M3 .all rollover 200 MB + respawn + quiet logging (perception_launch.py/node, rebuilt) — now active from launch. **Runtime-only, will revert to YAML — redo if wanted:** CA ranges → back to 5/5/20/3 (CA ON again); costmap sea_surface sensitivity → back to sensitive (FP may return); default_speed → YAML value (was 2.0 kt runtime); sidescan → transmit OFF on startup, re-run set_transmit true + range 20. SVS: fresh launch restarts the bridge identically — recovers only if cable/sensor fixed, not from the relaunch. Offered to re-apply the runtime set + re-verify SVS once stack is back up.

**2026-06-16 15:12 -04:00** — Raw-port sniff of SVS on /dev/ttyS0 (stack down, port free). **Data IS arriving** (3328 B/6s) → cable carries signal, sensor transmitting — but **heavily corrupted**: ~41% null bytes + repeating garbage, only occasional clean `AML,SVM,1493.07`; the `$` prefix mangled → bridge regex (`\$AML,SVM,...`) almost never matched (explains "0 readings" despite an alive sensor). **Baud sweep (2400–115200): 9600 is the only baud yielding real `$AML,SVM` sentences** (6 with `$`, 11 loose; sane values 1490–1493 matching frozen M3 1493.2). So: **baud correct (9600), sensor fine, but the physical serial line is marginal** — null-heavy framing corruption = loose/partial connector, damaged conductor, or bad ground/shield. Worked clean earlier today, degraded mid-run → physical shift. **Fix: re-seat + inspect serial cable both ends (esp. ground/shield), swap cable if needed.** Power-cycle/node-restart can't fix a data-line fault (consistent with both failing earlier). Bridge will auto-recover at 9600 once line is clean — no config change needed. **Resolves the cable-vs-sensor question: it's the cable/connection, not the SVS.**

**2026-06-16 15:37 -04:00** — **SVS root cause found by operator: loose wire at the connector** (being fixed). Confirms the data diagnosis exactly — intermittent contact → null-heavy framing corruption at correct baud, mangled `$`, immune to power-cycle/restart. A Prolific USB-serial adapter was plugged in as a workaround option (`/dev/ttyUSB0`, by-id `usb-Prolific_Technology_Inc._USB-Serial_Controller-if00-port0`) but the fix is the wire. **On reconnect:** if back to `/dev/ttyS0` (wire fixed) → no config change. If they switch to the USB adapter → bridge `device` (currently `/dev/ttyS0`, set via sound_speed_launch.py `device` launch arg default) must be repointed before relaunch — recommend the stable by-id path, not `ttyUSB0` (renumbers). Will re-verify clean `$AML,SVM` + low nulls once reconnected.

**2026-06-16 15:42 -04:00** — Operator: need to finish with a **static sound speed**; requested a quick 1 Hz sender for 1493.3. Wrote `bizzyboat_project11/scripts/static_sound_speed.py` (field stopgap, uncommitted): emits Valeport-format UDP to M3 (mercat:20003, ` 1493300\r\n` — byte-exact to bridge `format_valeport`, verified) + ROS `marine_interfaces/SoundSpeed` on `/bizzy/sensors/sound_speed/sound_speed`; defaults 1493.3 m/s @1 Hz; `--value/--rate/--host/--port/--no-ros/--no-udp`; UDP-only fallback if ROS unsourced. Compiles, executable, frame verified. **NOT run yet** (operator fixing wire; M3/stack down). ⚠️ Must be stopped when real SVS restored (else live+static interleave at M3/topic); safe alongside a starved bridge (sends nothing). **Wrap-up:** if static-SV fallback is wanted permanently, harden + add a proper `--static`/`fixed_sound_speed` mode to sound_speed_bridge itself rather than a side script.

**2026-06-16 15:48 -04:00** — Static SV script **working, confirmed end-to-end**: operator running it; M3 `detections.sound_speed` now **1493.300** (was frozen 1493.2) → Valeport UDP reaching the M3 via mercat:20003. Survey finishing on static 1493.3. Stack was relaunched to run it → runtime-only tweaks reverted (PID −13/d−1.2 persists via nav2_overlay.yaml; CA back ON @5/5/20/3, costmap back to sensitive, default_speed→YAML, sidescan transmit OFF). Re-apply offered, pending operator. ⚠️ Static script must be stopped if/when real SVS restored.

**2026-06-16 15:52 -04:00** — Operator: start sidescan transmit (was OFF after relaunch). `set_transmit {data:true}` → success/'transmitting'; confirmed via fresh status tick **tx=ON, pings 395/395/395 climbing** (initial tx=OFF read was a stale latched ~/status sample). Range reverted to startup default on relaunch (was 20 m); offered to re-set to 20.

**2026-06-16 15:57 -04:00** — Operator: compute freshwater SV from Garmin water temp (cross-check the static value). Garmin `~/water_temperature` = **23.04 °C** (steady ×3). **Marczak 1997 freshwater SV @23.04 °C = 1491.35 m/s** → static 1493.3 is **+1.95 m/s high** (~0.13%, ~1.3 cm/10 m on depth). Garmin temp trustworthy (validated vs CTD+SVS, issue #37). Marczak=distilled; lake trace solids → true ~1491.4–1491.6, still ~2 m/s under 1493.3. Gap vs dead SVS (1493.2) = real cooling over ~2 h and/or SVS-vs-Marczak offset. **Recommended switching static feed to 1491.4 m/s** (better live estimate); awaiting operator. Calc method noted for wrap-up (could auto-derive static SV from Garmin temp via Marczak as a future fallback mode).

**2026-06-16 16:04 -04:00** — Operator: compare recorded SVS sound speed vs Garmin temp (proper calibration). Extracted bag `bizzyboat/2026-06-16T15-54-20+00-00` (11:54–13:52 EDT, SVS alive): SVS 204k msgs mean **1491.79 m/s** (std 0.89, range 1483.5–1493.6); Garmin temp 5993 msgs mean **22.88 °C** (std 0.21). Marczak(mean temp)=1490.87. **Offset SVS−Marczak = +0.96 m/s** (paired, std 0.65) — consistent, = lake dissolved-solids + minor calibration above distilled Marczak. **Applied to current temp 23.04 °C → Marczak 1491.35 + 0.96 = 1492.3 m/s = calibrated best static.** Reconciles: 1493.3 (last SVS) ~1 m/s high (water cooled ~0.15 °C since; was near SVS max 1493.6); 1491.4 (raw Marczak) ~0.9 low. **Recommended static = 1492.3 m/s**; awaiting operator to restart static_sound_speed.py --value 1492.3.

**2026-06-16 16:10 -04:00** — Operator: script to compute SV from temp with the found correction. Wrote `bizzyboat_project11/scripts/temp_sound_speed.py` (field stopgap): subscribes Garmin `water_temperature` (BEST_EFFORT), computes **SV = Marczak1997(T) + 0.96** (bag-measured offset), feeds M3 Valeport UDP (mercat:20003) + ROS `SoundSpeed` @1 Hz; `--offset/--rate/--fallback 1492.3/--host/--port/--no-ros/--no-udp`; warns if temp stale >30s; needs ROS sourced + Garmin transmitting. Validated: T=22.88 °C (bag mean) → 1491.85 ≈ recorded SVS mean 1491.79 (offset reproduces real SVS ✓); T=23.04 °C → 1492.30. Compiles/executable, frames verified. Supersedes the fixed static feed (don't run both — both target mercat:20003). NOT run yet; offered to swap static→temp-tracking. ⚠️ Stop on real SVS restore. **Wrap-up:** fold this temp+offset mode into sound_speed_bridge as a proper SVS-failover.

**2026-06-16 16:16 -04:00** — Operator started temp_sound_speed.py, reported "not making it to mercat." Diagnosed: script running (PID 334707), no static conflict, mercat resolves (192.168.20.8) + pings (0.2 ms). **Actually working:** temp script publishing ROS `sound_speed`=1492.45 (8/5s, timer firing, computing from live temp); **M3 detections.sound_speed=1492.40** = temp-derived value reaching the sonar (was 1493.3 under static → updated). Likely operator checked in the first 1-2 s before the first Garmin temp arrived (fallback) + M3 picked up the new stream. Confirmed flowing boat-side; if operator was watching mercat directly that's a separate check. M3 now tracking Garmin temp at ~1492.4.

**2026-06-16 16:22 -04:00** — Operator: M3 *software* still shows the old static SV though the script console shows changing SV. Snapshot proves the M3 **datagram IS tracking**: temp 23.04→23.24 °C, script 1492.45→1492.83, **M3 detections.sound_speed 1492.40→1492.80** (follows temp). So the M3's applied/recorded SV (datagram → .all) is correct/live; the discrepancy is the **M3 vendor software display** (cached or a manual-SV entry not following the external Valeport feed). Guidance to operator (M3-app-side): check M3 software sound-speed SOURCE = external/Valeport vs manual; refresh/re-select source. **Caveat logged:** can confirm datagram SV from boat side, but if the M3 app does its own range calc off a manually-set SV that path would still be static — operator to confirm which SV the M3 app actually applies.

**2026-06-16 16:30 -04:00** — Operator asked if I'm on live M3 data. **Verified live**: rclpy capture = 169 detections/6 s (~28 Hz), header.stamp age 0.16 s. Corrected my earlier over-read (two lucky single-shots): sustained data shows the M3 datagram IS tracking, just slowly — **1492.40→1492.80→1493.10** as Garmin temp rose **23.04→23.24→23.45 °C**; the warming has pushed the temp-derived SV back up near the old static 1493.3, which is why it *looks* stuck on static (coincidence, not latch). Proof it's not latched: when the temp script took over, the M3 dropped OFF 1493.3 to 1492.4. Bridge still starved (`No reading for inf`) → no SVS conflict; 2 topic publishers = script + silent bridge. **Limit:** reading M3 datagram, not the vendor software the operator sees — offered a definitive test (briefly send a distinctly-offset value, watch if M3 app display jumps) to separate live-feed-tracking from a manual SV entry in the M3 app.

**2026-06-16 16:34 -04:00** — **RESOLVED**: operator confirms the SV in the M3 detections *header* agrees with the calculated (temp-derived) value → the M3 is applying the correct live SV and the recorded `.all` soundings are good. The **M3 vendor-software display is just stale/cosmetic** — does not affect what the M3 computes or records. No blip test needed; no data-path fix. Survey finishing on the temp-tracking feed (`temp_sound_speed.py`, Marczak+0.96). **Wrap-up (cosmetic):** M3 app SV display refresh — non-critical.

**2026-06-16 16:42 -04:00** — Operator: network issues (yesterday's, seemed resolved this AM) returned since stack restart. **Diagnosed via ping_monitor (6 targets, direct-vs-VPN split):** salmon_direct + router_op_direct = **100% loss**; salmon_vpn 64.6 ms + router_op_vpn 65.7 ms = OK; internet (8.8.8.8/1.1.1.1) 19–26 ms OK; Starlink 0% drop/19 ms healthy. **Root: the direct boat↔shore link is DOWN — operator side reachable only over VPN** (the constrained path that caused yesterday's saturation + zenoh discovery instability). Prime suspect: **Mikrotik OmniTIK 5 ac = "Not running"** (direct-link radio/AP down, or out of LOS range surveying). Aggravators: **CAKE/AQM uplink shaping absent** (`tc qdisc` shows none — morning fix didn't persist a reboot); **OAK bitrate reverted to 800 kbps** ×4 (re-saturates VPN). Yesterday's RCA (06-15 dev log): zenoh discovery instability + Starlink saturation; morning mitigations = CAKE/AQM + lower OAK bitrate. **Recommendation: restore direct link (OmniTIK — operator/net-side); if staying on VPN, re-lower OAK bitrate (ROS param) + re-apply CAKE/AQM (OS tc).** Offered to re-lower OAK bitrate + re-apply CAKE on operator OK; OmniTIK is operator's check. No network/radio commands fired.

**2026-06-16 16:50 -04:00** — Operator corrected: morning fix = **udp_bridge queue-size tweak** (not CAKE/OAK), and wifi in/out all day is normal (direct-link drop NOT the issue). **Verified the queue fix did NOT revert:** committed `b431b3b config(bizzyboat): size udp_bridge queue_size for fail-fresh under link saturation`, pulled in via today's `make sync` (43cd4f0..1a24b14), running values match exactly (ffmpeg=5, segmentation=2, sidescan/watercolumn=25), no uncommitted drift. udp_bridge diag NOW: `queued 0, 0 dropped` (queue fix working — fail-fresh, no overflow), **give-up rate 0.00/s but total 33,501** (bursty delivery failures during lossy/wifi-out periods, not continuous), `no rx for ~18s` from operator. So queue tuning intact & correct — NOT the regression. Asked operator for the specific symptom (CAMP drops→zenoh discovery instability re-triggered by restart; topic lag→bridge give-ups under saturation; commands not landing→the no-rx return path) to localize. Earlier OmniTIK/Starlink/CAKE/OAK theory retracted.

**2026-06-16 16:54 -04:00** — **CAMP crashed again** → this is the recurring **zenoh discovery instability** (yesterday's RCA), i.e. the actual "network issue back since restart" — the stack relaunch re-armed it. **Separate from the udp_bridge queue tweak** (confirmed intact) — queue sizes won't fix CAMP/zenoh crashes. Recovery (documented 06-15): restart CAMP alone + wait ~30–60 s for zenoh re-discovery; control sets repopulate from boat heartbeat. Boat-side unaffected (udp_bridge relay up; all boat topics live). **Open wrap-up (carried from 06-15): trigger RCA for zenoh discovery instability / CAMP recurrence (leak vs discovery).**

**2026-06-16 16:58 -04:00** — **CAMP recovered** (restart + wait), operator: everything working again → confirms zenoh-discovery diagnosis + the documented recovery loop. udp_bridge diag: most connections back to give-up 0.00/s; a few at **18–32/s (warn threshold)**; total give-ups climbed 33.5k→~39.9k during the CAMP-down gap (expected). Elevated give-up is most likely the **direct/wifi-path topics during a wifi-out window** (the normal cycling) — VPN path carries essentials (CAMP fine); queue tweak fail-freshing correctly through it. No action needed; offered to enumerate which connections are at 18–32/s to confirm direct-vs-VPN. **Net network conclusion for the day: recurring issue = zenoh/CAMP discovery instability (restart-CAMP recovery), NOT the udp_bridge queue tuning (intact); durable fix = open zenoh-discovery RCA.**

**2026-06-16 17:04 -04:00** — **Back at the dock.** Survey ops winding down; direct wifi link should re-establish in range (network give-ups expected to clear dock-side). Still running: temp_sound_speed.py (temp-derived SV feed, Marczak+0.96). Ready for wrap-up — pending: commit the field-mode changes (marine_tools M3 .all rollover + tests; unh_echoboats_project11 perception_launch respawn/rollover/200MB, nav2_overlay pid −13/d−1.2, the two SV stopgap scripts, this host log), and the wrap-up item list (SVS loose-wire/USB-adapter follow-up, static-SV→bridge failover mode, ca_safety min-points filter, sea-surface FP source tuning, crabbing speed-normalized gains, end-of-line hover, RC failsafe, zenoh-discovery RCA, M3 .all I/73 re-emit).

**2026-06-16 17:06 -04:00** — **Stack down (intentional, end of survey).** SV stopgap scripts already stopped (none running) — clean, nothing dangling. At wrap-up point. **Time-sensitive: uncommitted field-mode work not yet committed** — marine_tools (`kongsberg_em_bridge` .all rollover params + collision-safe path + pure roll fn + `test/test_save_rollover.py`) and unh_echoboats_project11 (`perception_launch.py` M3 respawn + rollover 200 MB; `nav2_overlay.yaml` pid −13/d −1.2; `scripts/static_sound_speed.py`; `scripts/temp_sound_speed.py`; this host log). Recommend committing before cleanup.
