# Field log — gabby — 2026-06-02

**Host**: gabby
**Operator**: Roland + Claude Code Agent (Claude Opus 4.8, 1M context)
**Mode**: field (gitcloud origin)
**Deployment issue**: git-bug `de8cdad` — "Deployment 2026-06-02: UNH pier shakedown — survey lines + sensor/sonar readiness + OTH (pre-class)"
**Started**: 2026-06-02 13:03 -04:00

## Timeline

**2026-06-02 13:03 -04:00** — Deployment mode activated on gabby via `/start-deployment` (field side, gitcloud origin). First activation of git-bug issue `de8cdad`. Stale git-bug cache was masking state (showed yesterday's closed `f1fb95e` as open, hid today's `de8cdad`); refreshed via plain `git fetch 'refs/bugs/*' 'refs/identities/*'` + `rm -rf .git/git-bug/cache` (git-bug's own SSH-agent transport unavailable: `SSH_AUTH_SOCK` unset). Issue title already canonical; `## Logs` section present (dev log only). **TODO (dev):** stamp `- [gabby log](docs/logs/2026/2026-06-02_gabby_logs.md)` under the deployment issue's `## Logs` section next time `/start-deployment` runs on dev (field side can't edit the issue — no `gh`, git-bug has no edit verb here).

**2026-06-02 13:08 -04:00** — ✅ **Segmentation timestamp fix (perception #28) CONFIRMED on the dock.** Stack up, images flowing. Captured ~5 s of `header.stamp` concurrently from `oak_forward/image_raw/ffmpeg` (raw) and `oak_forward/segmentation` (seg), both running ~5 Hz; aligned each seg frame to its nearest raw frame. **17/18 seg frames match a raw frame stamp exactly (0.00 ms); median offset +0.00 ms.** This is `seg_stamp − ff_stamp ≈ 0` — the device-capture-time equality the fix intends (was a constant ~+123.5 ms when seg used `now()`). The lone −200 ms sample is a window-edge artifact (first seg frame's matching raw frame fell just before the capture window → matched the adjacent frame one 200 ms interval away), not a real offset. The cheap no-water gate passes; on-water δ-sweep (`sea_surface_tuner` best-align at δ≈0) still to confirm during the run.

**2026-06-02 13:14 -04:00** — Pre-launch readiness checks 1–3 (dock, stack up):
1. **velocity_smoother re-enabled (#36)** — `/bizzy/velocity_smoother` lifecycle **active [3]**, in the path. `/bizzy/cmd_vel_nav` and `/bizzy/cmd_vel_smoothed` both silent — **expected at dock** (no autonomy command → nothing to smooth). Smoother is wired + active, but cmd_vel pass-through (the "controls smoothly with smoother back" gate) can't be validated without a live command → **validate on first teleop nudge / first auto goal.**
2. **Sensor data flowing (Goal 2)** — `topic hz`, not just node presence: **SVS `/bizzy/sensors/sound_speed/sound_speed` 25.5 Hz ✅**; **MAVROS `/bizzy/mavros/global_position/global` (FCU/SBG nav) 10.0 Hz ✅**. **M3 sonar: no ROS topic on gabby** — exports via UDP→QINSy, so M3 ping-health / 1PPS lock is a **mercat-side check** (not verifiable from gabby). SBG nav healthy via mavros at 10 Hz. (Watch SVS for #163 dropouts over the run.)
3. **Nav startup race (#157)** — all nav nodes **active [3]** (local_costmap, global_costmap, controller, planner, bt_task_navigator, velocity_smoother); **`/bizzy/local_costmap/costmap` producing at 1.36 Hz** → `map_tide` TF chain resolved, goals will be accepted. **Race did NOT bite this launch. ✅**
- **Open before/at launch:** (a) smoother pass-through on first command; (b) M3 ping health + 1PPS on mercat; (c) recording pipeline armed deliberately (M3/QINSy + boat-side bags) before the first line, not after (#201 lesson).

**2026-06-02 13:15 -04:00** — Pre-launch checks cleared by operator ("we are good"). Operator going to put the boat in the water. Phase: prep → **launch**.

**2026-06-02 13:24 -04:00** — Boat in the water (operator-reported). Phase: launch → **in-water**.

**2026-06-02 13:28 -04:00** — Controls check + first autonomy behavior, all good. Operator: teleoped close to a buoy with the USB controller, then issued a **hover** — "it seems to be working, and the costmap is showing the buoy."
- **Teleop control** (USB controller) responsive ✅.
- **Hover** engaged and station-keeping ✅.
- **Costmap marking** ✅ — close buoy showing on the costmap (on-water positive for the marking pipeline; consistent with the dock-confirmed seg-timestamp fix #28 / perception #26).
- **Confirming data point — velocity_smoother pass-through (#36) CONFIRMED on water:** with hover active, `/bizzy/cmd_vel_nav` 3.69 Hz (controller output) → `/bizzy/cmd_vel_smoothed` **10.0 Hz** (smoother steady republish). Smoother is receiving autonomy commands and passing traffic — closes the dock open-item. Boat held station smoothly (no operator-reported jerk).

**2026-06-02 13:36 -04:00** — Operator: starting a survey pattern, boat "keeps veering off the transit path." Diagnosed live (mitigate path: raise speed):
- **NOT the avoider** — `/bizzy/survey_obstacle_avoidance` overlay silent (no data 5 s) → avoider not deviating.
- **NOT the smoother (#36)** — `cmd_vel_nav` angular.z ≈ **−0.49 rad/s** = `cmd_vel_smoothed` angular.z ≈ **−0.49 rad/s** (identical, n≈36 each); linear.x 0.232 = 0.232. velocity_smoother is a transparent 1:1 pass-through, not clamping yaw/accel.
- **Root: current set vs. low transit speed.** Controller commanding a near-constant −0.49 rad/s turn at only **0.232 m/s** (≈0.45 kn) forward, while **ground speed ≈ 0.87 m/s** (gps_vel vₓ −0.53, v_y 0.69). ~0.6–0.8 m/s of unaccounted motion = tidal current set (Portsmouth Harbor / Fort Point, ACT0731). Boat has no steerage authority against the set at 0.45 kn → swept off the transit line; controller crabs continuously but can't catch it.
- **Mitigation handed to operator:** raise transit/survey speed above the current (≥1 m/s / 2+ kn) for steerage, or run nearer slack tide. **Wrap-up note:** consider a min-transit-speed floor / current-aware speed for the survey behavior so low commanded speeds don't get overpowered.

**2026-06-02 13:48 -04:00** — Operator raised speed; boat now "hunting to get on the line and always overshooting." Operator hunch: avoider is implemented as a controller decorator — did the PID config still get applied? **Investigated; hunch ruled out — PID config IS applied correctly:**
- Controller plugin = `marine_nav_avoidance_controller::AvoidanceController` (wrapper); `primary_controller` = `marine_nav_crabbing_path_follower::CrabbingPathFollower`. Single PID namespace `FollowPath.pid.*` (no `FollowPath.primary_controller.pid.*`).
- **Live == base YAML** (`seafloor .../nav2_params.base.yaml`): `pid.p −20.0`, `pid.i −0.5`, `pid.d −0.6` all match. `default_speed` live 0.7717 m/s (= 1.5 kn, runtime-set) vs base 1.5 m/s.
- **Source proof** (`avoidance_controller.cpp:166`): `primary_->configure(parent, name_, …)` with `name_ == "FollowPath"` — wrapper passes the SAME namespace through, so CrabbingPathFollower reads `FollowPath.pid.*` from base YAML. Namespace not shifted by the decorator. (Contrast yesterday's avoider-param bug, which WAS a wrong-node/namespace issue — this one is clean.)
- **Therefore hunting/overshoot is genuine controller tuning, not plumbing:** `p −20` vs `d −0.6` (≈33:1), `tracking_time_constant 0`, `chatter_damping_weight 0` → underdamped; 1.5 kn worsens spatial overshoot. These ARE live-settable on `controller_server` (lifecycle node, externally param-serviced). Proposed mitigation (awaiting operator go): add damping — `pid.d −0.6 → −2.0` first, then trim `pid.p` / raise `chatter_damping_weight` if needed. Not durable across nav relaunch → bake to YAML if it proves out. **Wrap-up:** capture as project memory (avoidance wrapper passes FollowPath namespace through — PID config plumbing is correct).

**2026-06-02 13:56 -04:00** — Operator: confirm today's PID values are the same ones that worked yesterday — check yesterday's bag. **Done — gains are IDENTICAL, and they worked yesterday.** Source: yesterday's boat-side bag `~/data/logs/bizzyboat/2026-06-01T17-09-07+00-00` (mcap), topic `/bizzy/FollowPath/pid/pid_state` (control_msgs/PidState, 42k msgs).
- **Gains recovered:** each PID term clamps at its gain magnitude (`saturation:True`) → `max|p_term|=20.0`, `max|i_term|=0.5`, `max|d_term|=0.6` = **p −20 / i −0.5 / d −0.6, exactly today's live values.** (Naive term/error ratios are meaningless here because p_term pegs at 20 for any |XTE|≳1 m — the proportional term is bang-bang above ~1 m error.)
- **They worked:** yesterday's cross-track |error| median **0.467 m** (= the issue's documented 0.47 m clean-stretch figure), p90 3.2 m. Clean line-following on these exact gains.
- **Speed comparable:** yesterday cmd_vel_nav median (moving) 0.665 m/s vs today 0.77 — not the variable. Yesterday median |yaw| 0.206 rad/s vs today's sustained ~0.49 during the hunt.
- **Conclusion: do NOT detune the gains** (retracts my earlier d→−2.0 suggestion). The changed variable is **avoidance moved INTO the control loop** this run (`AvoidanceController` wraps `CrabbingPathFollower`); yesterday the controller was the **bare** crabbing follower + a separate BT-node avoider. `obstacle_avoidance_weight` is now **1.0** (1:1 with line-following) and the wrapper reshapes/resamples the followed path every cycle — prime suspect for the hunting, even with the overlay quiet.
- **Mitigation plan (avoidance does NOT gate this run):** (1) live test — set `FollowPath.obstacle_avoidance_weight → 0` on `controller_server` (live-settable per overlay design; verify with `get`) to neutralize the wrapper's path influence without a relaunch; if hunting stops, wrapper confirmed. (2) If needed, definitive known-good revert — `FollowPath.plugin` back to bare `CrabbingPathFollower` in nav2_overlay.yaml + nav relaunch (plugin type not live-swappable). Awaiting operator go.

**2026-06-02 14:00 -04:00** — Smoother is **new this run** (confirmed: `/bizzy/cmd_vel_smoothed` absent from yesterday's bag; logger record list adds it for today). Verified the **full cmd_vel chain is logged in today's bag** (`~/data/logs/bizzyboat/2026-06-02T17-04-14+00-00`, actively rolling 5-min splits). collision_monitor `cmd_vel_in_topic=cmd_vel_smoothed`, `cmd_vel_out_topic=piloting_mode/autonomous/cmd_vel`. Chain: controller→`cmd_vel_nav`→smoother→`cmd_vel_smoothed`→collision_monitor→`piloting_mode/autonomous/cmd_vel`→`mavros/setpoint_velocity/cmd_vel`→FCU. All four links in the record list (bizzyboat.yaml 328–331) and each shows the logger as a live subscriber. `/bizzy/cmd_vel` (geometry_msgs/Twist) has **0 subscribers** — dead-end legacy topic, not in the path, correctly not logged. Smoother live: `velocity_smoother` active [3], `cmd_vel_smoothed` 10 Hz. **No logging gap — both new-variable signal paths (smoother I/O + controller pid_state/plan) are captured for offline analysis.**

**2026-06-02 14:01 -04:00** — ⏱️ Transit start (operator-marked).

**2026-06-02 14:06 -04:00** — Operator: boat "going crazy for a bit, but kind of following the line now," then cleared the mission → return to **hover**. Phase: brief survey/transit attempt → hover (stable).

**2026-06-02 14:08 -04:00** — Bag analysis of transit window (split `_11`, 14:01:00–14:04:14; cmd_vel chain + pid_state). **Finding: the velocity_smoother is fighting the direct-tuned PID → oscillation.**
- `cmd_vel_nav` angular.z violent: std 0.86, range **±3.0 rad/s** (yesterday median 0.21 / p90 0.73); PID `output` saturating at ±90. 
- **Smoother rate-limiting yaw to ~0.5 rad/s²** while controller slews far faster → **max|Δ angular.z nav→smoothed| = 3.5 rad/s**. Controller commands hard correction → smoother ramps it slowly → boat responds late → PID winds up/saturates → hunts. Actuator-lag-induced instability; the smoother is the new lag.
- Cross-track |error| median **0.94 m** / max **4.76 m** (≈2× yesterday's 0.47 m).
- 30 s evolution matches operator: 14:01:00 calm (XTE 0.44, ~yesterday) → 14:01:30 explodes (yaw std 1.8, ±3.1, XTE 4.8) → oscillates → settles by 14:03:30 (yaw std 0.01).
- **Root cause hypothesis:** PID was tuned to drive the FCU directly (yesterday, no smoother). Inserting velocity_smoother with a tight ~0.5 rad/s² yaw-accel limit adds lag the PID isn't tuned for → windup + hunting. Smoother is the prime suspect over the avoidance wrapper (quantified lag in the loop). **Mitigation to test:** loosen velocity_smoother yaw accel limit (`max_accel`/`max_decel` theta), or run with smoother bypassed (yesterday's known-good path) — verify which params are live on `/bizzy/velocity_smoother`. Avoidance-weight→0 test still pending too; smoother now ranks ahead of it. Both new-this-run variables captured in bag for replay.

**2026-06-02 14:14 -04:00** — Mitigation applied (operator: "do 1" — loosen smoother yaw accel). Read `/bizzy/velocity_smoother`: `max_accel [0.8, 0.0, 0.5]`, `max_decel [-0.45, 0.0, -0.5]`, `max_velocity [2.75, 0.0, 1.0]` — yaw *velocity* cap 1.0 rad/s (correct, = #124 helm cap) but yaw *accel* only 0.5 rad/s² (= the measured lag; 2 s to reach the cap). **Set theta accel 0.5 → 3.0, decel −0.5 → −3.0** (reaches yaw cap in ~0.3 s); linear limits + velocity cap unchanged. **Verified live via `get`** (set not dropped) — velocity_smoother accepts dynamic accel reconfig, no relaunch. Reversible (revert to 0.5/−0.5). **Not durable across nav relaunch** → bake to YAML if it proves out. **Next: re-engage a line/transit and watch for the hunting to clear; the cmd_vel chain + pid_state are still logging for a before/after replay comparison vs split `_11`.**

**2026-06-02 14:15 -04:00** — ⏱️ New line start (operator), first run with loosened smoother yaw accel (3.0 rad/s²). Watching for hunting to clear vs the 14:01 attempt.

**2026-06-02 14:16 -04:00** — ✅ **Smoother yaw-accel fix CONFIRMED — hunting cleared.** Operator: "looking much better so far." Live sample (loosened smoother) vs the 14:01 attempt baseline:
- `cmd_vel_nav` angular.z std **0.25** (was 0.86), range −0.94…−0.08 — **no more ±3.0 rad/s saturation**, stays within the 1.0 cap.
- `cmd_vel_smoothed` now tracks nav closely (std 0.21 vs nav 0.25) — the controller↔smoother fight is gone.
- pid cross-track |err| median **0.454 m** / max 1.39 m — **back to yesterday's clean level (0.467 m)**; was 0.94 m / 4.76 m at 14:01.
- **Conclusion: the velocity_smoother's tight yaw-accel (0.5 rad/s²) was the root cause of the hunting, not the avoidance wrapper.** Raising it to 3.0 rad/s² restored clean line-following. The `obstacle_avoidance_weight→0` test was not needed. **Wrap-up:** bake `max_accel`/`max_decel` theta = ±3.0 into `nav2_overlay.yaml` (currently live-only, lost on relaunch); capture as the #36 lesson — re-enabling velocity_smoother needs its yaw-accel matched to the direct-tuned CrabbingPathFollower PID or the PID winds up against the ramp.

**2026-06-02 14:19 -04:00** — Operator: reduce obstacle avoidance weight to 0.5. **Set `FollowPath.obstacle_avoidance_weight` 1.0 → 0.5 on controller_server, verified live** (get returned 0.5). Now 0.5 obstacle : 1.0 line-following (2:1 toward the line) → gentler deviation around costmap marks while still avoiding. Live-only, not durable across nav relaunch → bake to `nav2_overlay.yaml` if it proves out. Other avoider tunables unchanged (max_deviation 6.0, max_lateral_change 1.0, chatter_damping_weight 0.0, smoothness_weight 2.0, along_track_spacing 2.0, lateral_resolution 0.5, anchor_behind_distance 4.0, avoid_speed 0.0).

**2026-06-02 14:20 -04:00** — Started 1-hour camera recording (operator): `record_camera_topics.sh 3600` → `/home/field/data/logs/bizzy_images/bag_2026-06-02T14.20.17_ffmpeg_seg` (background, runs to ~15:20). Capturing 4 OAK cams (forward/aft/port/starboard: segmentation + image_raw/ffmpeg + camera_info), `/tf`, `/tf_static`, `local_costmap/costmap`, `robot_description`. Covers the loosened-smoother line runs + seg-timestamp fix (#28) on-water verification window.

**2026-06-02 14:23 -04:00** — Operator: try some chatter damping. `chatter_damping_weight` → `params_.w_temporal` (source: "higher = steadier tick-to-tick"; penalizes change from previous tick's per-station offsets; same weight family as line 1.0 / obstacle 0.5 / smoothness 2.0). **Set `FollowPath.chatter_damping_weight` 0.0 → 0.5, verified live.** Modest first value — can raise toward 1–2 for steadier paths if needed. Live-only, bake to YAML if it proves out. Avoider weights now: obstacle 0.5, line 1.0, smoothness 2.0, chatter 0.5.

**2026-06-02 14:25 -04:00** — 🎉 **First on-water autonomous avoidance — BizzyBoat went around a moored sailboat** (operator, observed ~14:24–14:25). Confirmed the avoider was the mechanism: `/bizzy/survey_obstacle_avoidance` overlay publishing **4.75 Hz** (AvoidanceController actively solving + deviating, not a manual dodge). Validates the full pipeline on water: sea-surface costmap marked the sailboat → AvoidanceController bent the path around it → loosened-smoother yaw passed the maneuver cleanly. Tuning in effect: obstacle_avoidance_weight **0.5**, chatter_damping_weight **0.5**, line 1.0, smoothness 2.0; smoother yaw accel 3.0. **Captured in both bags for replay:** main boat-side bag (`survey_obstacle_avoidance`, `/plan`, cmd_vel chain, pid_state, `local_costmap/costmap`) + the 1-hr camera bag `bag_2026-06-02T14.20.17_ffmpeg_seg` (4-cam segmentation + costmap + TF). Real obstacle, not gating the run per the deployment scope, but a clean demonstration of avoidance #26/#59 working on water.

**2026-06-02 14:30 -04:00** — Operator: collision monitor slowdown interfering — boat slowed by CA before the avoider's turn completes; want the turn to lead. Read zones: **CollisionSlowdown** 20 m × ±3 m box, `slowdown_ratio 0.3` (slows to 30% — at ~0.77 m/s that brakes ~26 s out, during the avoider's route-around); **CollisionStop** 5 m × ±2 m, full stop. Polygon *size* is configure-time (not live, confirmed 2026-06-01); only `slowdown_ratio` is live.
- **LIVE (applied now):** `CollisionSlowdown.slowdown_ratio` 0.3 → **0.7**, verified — gentle slowdown so any trigger no longer fights the turn. 5 m hard-stop intact.
- **DRAFTED in `nav2_overlay.yaml` (staged, NOT yet applied — needs nav relaunch; running stack unaffected):** CollisionSlowdown box 20 m → **8 m** forward (±3 m kept) so the avoider turns away before the obstacle reaches the zone; `slowdown_ratio` baked to 0.7; CollisionStop width ±2.0 → **±1.5 m** (length 5 m kept). Rationale fits the existing config comment (which named the upstream avoider as "the real fix") — now that AvoidanceController (#59/#60) is deployed + validated on water, shrinking the box realizes that strategy rather than weakening safety. Not committed yet (batch at breakpoint). **Apply on next relaunch; re-validate the slowdown still backstops before trusting.**

**2026-06-02 14:35 -04:00** — Operator: "boat looks drunk again." Confirmed all live params intact (no relaunch / no zenoh drop): smoother `max_accel`/`max_decel` theta still 3.0/−3.0; obstacle_avoidance_weight 0.5; chatter_damping_weight 0.5; slowdown_ratio 0.7. **So NOT the smoother** — and the signature differed: `cmd_vel_nav` yaw **calm** (std 0.16, range −0.16…+0.43, vs 14:01's std 0.86/±3.0), smoother tracking nav closely, but cross-track |err| elevated (median **0.89 m** vs 0.45 clean) with the avoider overlay **active 4.5 Hz**. I.e. the boat was *smoothly* following an avoider-deviated path ~0.9 m off-line — avoider weaving, not controller hunting. Candidates: stale costmap ghosts (yesterday's persistence bake: clamp 15 / decay 60 s / clear_floor −0.5 / prob_min 0.8 → marks linger ~60 s) vs chatter-damping holding deviations.

**2026-06-02 14:36 -04:00** — Operator: "doing better now" — **self-resolved with NO param change.** That clears chatter_damping (unchanged at 0.5) and points at **transient costmap ghosts** decaying out (the ~60 s persistence). Wrap-up candidate: revisit sea_surface_layer persistence (decay_half_life_s 60 / clear_floor −0.5) — good for single-sighting marks but lets passed/phantom marks linger and weave the avoider. Chatter 0.5 + obstacle 0.5 retained; behaving.

**2026-06-02 14:45 -04:00** — Operator: save today's live params to config so a relaunch keeps them. Audited each:
- ✅ **Saved to `nav2_overlay.yaml`** (BizzyBoat overlay): `FollowPath.obstacle_avoidance_weight` 1.0 → **0.5**, `chatter_damping_weight` 0.0 → **0.5** (comment updated, water-validated). Collision-monitor box (20→8 m), `slowdown_ratio` 0.3→0.7, CollisionStop ±2→±1.5 m already written earlier.
- ⚠️ **Smoother yaw accel NOT yet saved — flagged for decision.** Lives in shared `seafloor .../nav2_params.240.yaml` (240-hull), currently `max_accel/max_decel` theta ±0.5. The comment there shows ±0.5 was a **deliberate #36 choice to spread the yaw-moment buildup that caused the #197 snap-roll (±30° hull roll)**. Raising to ±3.0 (today's live fix) reintroduces that roll risk and would change all 240-hull boats. Today at 3.0 no roll was observed (avoider deviations smooth, line-following yaw calm std 0.16–0.25), but no deliberate sharp-yaw-step/roll test was done. Awaiting operator call: bake 3.0 as-tested / a gentler compromise (e.g. 1.5) / BizzyBoat-only override in overlay vs editing shared 240.yaml / leave live-only. Until decided, a nav relaunch reverts smoother yaw accel to 0.5 → hunting returns.

**2026-06-02 14:47 -04:00** — Operator chose option 1: bake 3.0 as-tested. **Saved `nav2_params.240.yaml`** (seafloor repo): `max_accel`/`max_decel` theta ±0.5 → **±3.0**, comment updated to record the field tune + the retained ⚠️ #197 snap-roll risk + "SIM-validate roll response; back off toward ±1.5 if a sharp reversal rolls the hull." Live value already 3.0, so running stack unchanged; config now matches → survives relaunch. **All today's tuning now persisted.** Uncommitted changes across TWO repos (both field/gitcloud): `unh_echoboats_project11` (nav2_overlay.yaml: FollowPath weights + collision-monitor box/ratio/stop; this log) and `seafloor_echoboat_project11` (nav2_params.240.yaml: smoother yaw accel). Batch-commit both at a breakpoint. **Wrap-up:** sim-validate the 3.0 yaw-accel roll response (#197 regression check) before it ships beyond this deployment.

**2026-06-02 14:49 -04:00** — Operator restarting the nav stack (hovering at end of survey). First relaunch since all tuning was baked to YAML — relaunch reloads from config, so the runtime params persist (the point of saving). **Collision-monitor geometry changes (8 m slowdown box, ±1.5 m stop) take effect for the first time on this relaunch** (configure-time). Post-relaunch checks queued: (1) verify all baked params reloaded to intended values (smoother yaw 3.0, obstacle 0.5, chatter 0.5, slowdown_ratio 0.7, new polygon sizes); (2) #157 map_tide startup race (local_costmap producing → goals accepted); (3) re-validate CA backstops at the new box sizes.

**2026-06-02 14:50 -04:00** — Nav stack back up; **post-relaunch verification all ✅**. Every baked param reloaded to intended value: smoother `max_accel [0.8,0,3.0]`/`max_decel [-0.45,0,-3.0]`, `obstacle_avoidance_weight 0.5`, `chatter_damping_weight 0.5`, `slowdown_ratio 0.7`, **slowdown box now 8 m** `[[8,3],[8,-3],[0,-3],[0,3]]`, **stop box now ±1.5 m** `[[5,1.5]…]`. Confirms YAML edits landed in the right files (240.yaml + overlay) with valid syntax/merge. Lifecycle: velocity_smoother / controller_server / local_costmap / collision_monitor all **active [3]**; `local_costmap/costmap` producing **1.41 Hz** → map_tide TF resolved, **#157 race did NOT bite**. Collision-monitor box shrink (8 m / ±1.5 m) now live for the first time. Operator restarting survey to re-test with the full baked config + turn-before-slowdown geometry.

**2026-06-02 14:59 -04:00** — ✅ **Turn-before-slowdown CONFIRMED on water.** Operator: "doing a better job avoiding the sailboat this time" (post-relaunch, 8 m slowdown box + obstacle_weight 0.5 + chatter 0.5 + smoother yaw 3.0). Live data while passing the sailboat: avoider overlay active **4.37 Hz** (deviating), `cmd_vel_nav` speed **held 0.795 m/s** (default 0.77, range 0.77–0.85 — NOT slowed; slowdown would drop it to ~0.54 @ratio 0.7), `collision_monitor_state` published nothing (no slowdown/stop action firing). I.e. with the box shrunk 20→8 m the obstacle never enters the slowdown zone — the avoider turns the boat away first, so it routes around at full survey speed instead of braking. The box-shrink + gentler avoider weight worked as designed; the prior comment's "turn before the obstacle enters this zone" strategy realized end-to-end (costmap mark → avoider deviation → no CA brake). Footprint also confirmed publishing 2.78 Hz + bridged to operator (config + live).

**2026-06-02 15:01 -04:00** — ⏱️ Operator issued an over-the-horizon goto to watch planning + avoidance at range. Touches Goal 3 (OTH #130 — Starlink-only topic-budget/saturation envelope) and the planner on a distant goal (watch #23 TF-extrapolation on long goals; #157 goal acceptance). Watching.

**2026-06-02 15:05 -04:00** — OTH test PAUSED — operator + salmon-side agent troubleshooting display issues (operator-station / CAMP rendering). Boat-side (gabby) is steady; this is receive/render side, salmon's domain. Earlier-confirmed boat-side send is healthy (footprint 2.78 Hz + bridged both profiles; local_costmap producing 1.41 Hz) — available to the salmon agent for send-vs-receive isolation if asked. Standing by, holding boat-side state.

**2026-06-02 15:12 -04:00** — OTH deferred; operator starting a **larger, denser survey at default speed (0.77 m/s)** to encounter more obstacles — stress-test of the avoidance tuning (obstacle 0.5 / chatter 0.5 / 8 m slowdown box) + cross-track (#164) + apron turning limits (#124 §2) across many lines/turns. Watch items: (a) avoider handling multiple/successive obstacles without eating the run; (b) ⚠️ dense lines — if spacing < ~5 m, mid-line resume can match the WRONG line (RobotOnPath 5 m XTE tolerance, run_tasks.xml) — watch for resume snapping to an adjacent line; (c) stale-ghost weaving (decay 60 s) more likely when passing marks frequently; (d) collision_monitor still backstopping at the new 8 m/±1.5 m sizes. Watching; will pull bag XTE/avoidance numbers after a split closes.

**2026-06-02 15:17 -04:00** — ⏱️ Dense survey start (operator-marked) — default speed 0.77 m/s, larger/denser pattern. Bag window start for the avoidance/XTE stress-test analysis.

**2026-06-02 15:21 -04:00** — Operator: a survey line started just before 15:17 and "turned a lot before settling" — check PID data, is it still smoother-related. Analyzed split `_26` (15:14:14–15:19:14). **Verdict: NOT a smoother regression — the 3.0 fix held.**
- Smoothed yaw **max-slew = 3.00 rad/s²** (passing at the new cap, not the old 0.5 — identical to the clean 14:16 run).
- nav↔smoothed divergence **2.35 rad/s** vs **3.5** at the 14:01 smoother-fight — lag ~halved; smoother ramping, not throttling.
- But `cmd_vel_nav` yaw still violent (±2.3 rad/s, near-instant steps, PID `output` saturating ±90) — the PID is the dominant driver.
- Timeline: big initial correction at line acquisition → overshoot → **settles by ~15:18** (yaw std 0.21, XTE 0.22 m). XTE spikes 6.95/7.13 m at 15:16:54/15:17:14 look like avoidance / line re-acquisition artifacts (dense pattern, obstacles) — exclude per #164 guidance, not line-following quality.
- **Conclusion: line-acquisition transient + dense-survey/avoidance corrections, not the smoother.** Smoother behaving as on the clean run. Raising its cap further chases residual lag but re-opens #197 roll risk — not recommended (clean run already tracked at 3.0). Crisper acquisition = PID-saturation / approach-angle topic, deferred to wrap-up/sim.
- **1-hr camera recording COMPLETED** (`bag_2026-06-02T14.20.17_ffmpeg_seg`, exit 0) — covers the loosened-smoother line runs, sailboat avoidance, and seg-timestamp (#28) on-water window.

**2026-06-02 15:27 -04:00** — ⚠️ **NEAR-MISS / MANUAL TAKEOVER (safety event).** At default survey speed (0.77 m/s) in the dense pattern, the **operator took manual control to avoid hitting the sailboat.** Per operator: the avoider commanded a turn, but the boat didn't respond quick enough, and the collision avoidance (CollisionStop) **disabled the boat** (full stop). Fallback (manual override) used correctly — avoidance does NOT gate this run.
- **Root cause (mechanism):** the turn-before-slowdown tuning we validated earlier was validated **at low speed**, where the boat can out-turn the obstacle. At default speed the boat can't: turn authority is capped (smoother yaw accel 3.0 → 0.33 s to reach the **1.0 rad/s yaw velocity cap**, #124 helm limit), so at 0.77 m/s it covers too much ground before the heading change clears the obstacle. We had deliberately **reduced the slowdown safety margin** (box 20→8 m, slowdown_ratio 0.3→0.7) so it wouldn't interfere with the turn — which is safe ONLY when the turn succeeds. At speed the turn didn't complete, and with the margin gone the boat carried speed to the 5 m CollisionStop → emergency stop → manual takeover.
- **Key finding:** turn-before-slowdown is **speed-limited** — it needs the boat to out-turn the obstacle. At default speed the boat can't, so the slowdown braking is NOT optional there: it buys the avoider time (slow-and-turn). Our tuning removed exactly that margin.
- **Mitigation options (operator's call):** (a) **restore slowdown braking margin** — revert `slowdown_ratio` 0.7→0.3 live now + slowdown box back toward 20 m on next relaunch (configure-time); (b) run avoidance only at **reduced speed** (the validated-safe regime); (c) both. Recommend restoring CA margin before any further autonomous avoidance at default speed.

**2026-06-02 15:46 -04:00** — Boat recovered (out of water). Phase → **recovery**. Commit breakpoint. Uncommitted: `unh_echoboats_project11` nav2_overlay.yaml (FollowPath weights + collision-monitor box/ratio/stop) + this log (untracked); `seafloor_echoboat_project11` nav2_params.240.yaml (smoother yaw accel). **Config disposition pending operator decision before commit:**
- ✅ KEEP/commit — smoother yaw accel ±3.0 (240.yaml): validated, fixed the hunting.
- ✅ KEEP/commit — FollowPath obstacle_avoidance_weight 0.5 + chatter_damping_weight 0.5: validated at low-speed sailboat passes (not implicated in the near-miss).
- ⚠️ REVERT recommended — collision-monitor box 20→8 m, slowdown_ratio 0.3→0.7, stop ±2→±1.5 m: the 15:27 near-miss showed these remove safety margin needed at default speed. Recommend reverting nav2_overlay.yaml collision_monitor block to the original 20 m / 0.3 / ±2 m before committing, so an unsafe-at-speed CA config is NOT persisted. (Live slowdown_ratio still 0.7 but moot — boat recovered.)

**2026-06-02 16:11 -04:00** — Operator chose option 1 (revert CA block, commit smoother + avoider weights) and to **park the reverted experimental values for post-deployment review.**

**⏸️ PARKED — collision-monitor "turn-before-slowdown" experiment (reverted, revisit post-deployment):**
- `CollisionSlowdown.points`: **8 m × ±3 m** (was/restored 20 m × ±3 m)
- `CollisionSlowdown.slowdown_ratio`: **0.7** (was/restored 0.3)
- `CollisionStop.points`: **±1.5 m** width (was/restored ±2.0 m), 5 m length
- *Intent:* let the avoider steer clear before the slowdown brakes (turn-before-slowdown). *Result:* worked at low speed (sailboat passes), **failed at default speed 0.77 m/s** — boat couldn't out-turn (yaw cap 1.0 rad/s) and the removed margin let it reach CollisionStop → emergency stop → manual takeover (15:27 near-miss). *Reverted.*
- *Post-deployment directions to consider:* (a) **speed-scaled CA zones** (bigger/earlier slowdown at higher speed); (b) **faster turn authority** (raise yaw cap above 1.0 rad/s and/or smoother accel — weigh vs #197 roll); (c) **cap avoidance speed** so the boat can always out-turn within the slowdown margin; (d) keep avoidance as low-speed-only + manual fallback at survey speed (today's de-facto outcome). Full event analysis in the 15:27 entry.

**Reverted in `nav2_overlay.yaml`** to original 20 m / 0.3 / ±2 m (comment records the experiment + near-miss). **Committing now:** smoother yaw accel ±3.0 (240.yaml) + avoider weights obstacle 0.5 / chatter 0.5 (overlay); CA block back to original.

**2026-06-02 16:13 -04:00** — Committed (field mode, jazzy, both gitcloud). SHAs for dock reconciliation:
- `seafloor_echoboat_project11`: **`d35a795`** — nav2(240) velocity_smoother yaw accel/decel ±0.5 → ±3.0 (with #197 sim-validate caveat in message + comment).
- `unh_echoboats_project11`: **`0dc719d`** — nav2 avoider weights obstacle 0.5 / chatter 0.5; collision-monitor experiment reverted to original 20 m / 0.3 / ±2 m.
Pre-commit hooks passed. **Both commits are LOCAL — not pushed to gitcloud yet** (awaiting operator go on push). This gabby log held living/untracked through wrap-up. Parked CA experiment (8 m / 0.7 / ±1.5 m) documented above for post-deployment.