# 2026-06-09 — salmon log (BizzyBoat outing)

Host: salmon
Side: field (operator station)

## Timeline


**2026-06-09 ~12:00 -04:00** — Boat launched (in the water). _(Approximate — operator reported "sometime around noon"; backfilled.)_

**2026-06-09 ~12:31 -04:00** — _(event time from system `uptime` at first probe; entry backfilled/written ~16:00.)_ Stale-data incident on salmon. Symptom: CAMP UI interactive but all boat data and camera views stale. ROS graph/discovery healthy (all nodes present, topic/node list instant), but NO subscription data delivered on any topic — bridge_info, topic_statistics, and /bizzy/marine/heartbeat all echoed empty. rmw_zenoh_cpp flooding 'Received liveliness token with invalid qos keyexpr' / 'service implementation invalid'. Firefox renderer (RDD Process) pegging ~293% CPU, load avg ~6 (separate desktop-sluggishness factor, not the data outage). Fix sequence: restarting the Zenoh router (rmw_zenohd) did NOT clear it; restarting 'core' DID — /bizzy/marine/heartbeat recovered to steady ~1 Hz. Takeaway: the zenoh QoS/liveliness error flood was a symptom, not the root cause; the wedged component was the core bringup, not the router.

**2026-06-09 16:35 -04:00** — Command-path incident (after running fine for a while): operator reported commands not reaching the boat. Downlink healthy throughout (/bizzy/marine/heartbeat steady ~1 Hz). Uplink transport healthy too — manual helm /bizzy/piloting_mode/manual/helm flowing to boat at ~31 msg-bytes/s, ~195 success-bytes/s, 0 failed/dropped. Problem isolated to the marine command relay: /bizzy/command_bridge_sender (subscribes /bizzy/marine/send_command -> publishes /bizzy/marine/command) was wedged, repeatedly re-emitting ONE stale frame with a frozen embedded timestamp (2026-06-09T20:04:18.984: 'mission_manager override goto 43.0729601 -70.7117142' + 'piloting_mode autonomous') instead of forwarding fresh commands. send_command showed no new traffic in watch windows. Same failure shape as earlier core wedge — operator opted to restart core. Note for next time: a frozen embedded timestamp on /bizzy/marine/command is the tell that the relay is stuck, not the link. Boat has been recovered (out of the water).

**2026-06-09 16:43 -04:00** — SESSION TAKEAWAY — two independent 'core wedge' incidents in one outing (~12:31 and ~16:04 EDT), both cleared only by restarting core (Zenoh-router restart did nothing the first time). Distinct symptoms, same root component: (1) core stops delivering ANY subscription data while discovery stays healthy; (2) core's command relay /bizzy/command_bridge_sender freezes, replaying its last command frame with a stale embedded timestamp. Diagnostic tells: discovery-OK-but-no-data points at core, not the link; a frozen embedded timestamp on a relayed topic means the relay is stuck, not the link. Throughout both, the UDP bridge and link were healthy (heartbeat ~1 Hz, helm success-bytes nonzero). FOLLOW-UP (off-water): file an issue on core-bringup robustness — why core wedges mid-run and whether it can self-heal or expose a health/liveness signal so we don't rediscover via stale data. Also worth a watchdog on command_bridge_sender (stale-frame / no-fresh-input detection).
