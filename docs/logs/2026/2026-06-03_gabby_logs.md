# Field log — gabby — 2026-06-03

**Host**: gabby
**Operator**: Roland + Claude Code Agent (Claude Opus 4.8, 1M context)
**Mode**: field (gitcloud origin)
**Deployment issue**: git-bug `0cba862` — "Deployment 2026-06-03: pier — clean-survey validation + OTH (final pre-class)"
**Started**: 2026-06-03 13:02 -04:00

## Timeline

**2026-06-03 13:02 -04:00** — Deployment mode activated on gabby via `/start-deployment` (field side, gitcloud origin). First activation of git-bug issue `0cba862`. git-bug cache refreshed earlier this session via plain `git fetch 'refs/bugs/*' 'refs/identities/*'` + `rm -rf .git/git-bug/cache` (git-bug's own SSH-agent transport unavailable on this host). Issue title already canonical; `## Logs` section present (dev log only). **TODO (dev):** stamp `- [gabby log](docs/logs/2026/2026-06-03_gabby_logs.md)` under the deployment issue's `## Logs` section next time `/start-deployment` runs on dev (field side can't edit the issue — no `gh`, git-bug has no edit verb here).

**2026-06-03 13:09 -04:00** — Operator bringing up the stack.

**2026-06-03 13:12 -04:00** — Switched log-append method to the Edit tool (from Bash `printf >>`) to avoid per-entry permission prompts during live ops.

**2026-06-03 13:14 -04:00** — **Preferred log-append method = Edit tool** (timestamp still from the allowlisted `date` call). Confirmed prompt-free this session, whereas Bash `printf >>` prompts every entry (no matching allowlist rule). **Wrap-up TODO (workspace skill):** `start-deployment` SKILL.md rule 6 promotes a `printf`-based `dlog()` helper, which contradicts the same rule's "use the file tools over heredocs" guidance and causes a permission prompt per entry. Update the skill to recommend the Edit/Write file tools for log appends instead of (or alongside) the `dlog` shell helper. Operator was unaware the skill promoted the `printf` method.

**2026-06-03 13:21 -04:00** — Post-bringup health check (operator reported controls checks done, stack looking good). Mixed result from my end: `/bizzy/odom` publishing healthy; **`/bizzy/lifecycle_manager_navigation` → "Node not found"** and **`map_tide→base_link` TF not resolving (3s timeout)**. That pair matches the documented mru→/global Nav2 startup race (lifecycle loses the `map_tide` TF startup race). Flagged to operator; mitigation if nav group is up = full launch-group relaunch (not single-node restart). Open question to operator: is the nav stack actually launched yet, or are these expected because it isn't up.

**2026-06-03 13:25 -04:00** — Operator relaunched the nav group, reports "things look good, costmap in CAMP." Costmap (map-frame) confirmed up operator-side. But from my probes the **tide-datum chain is still dark**: `map_tide→base_link` TF not resolving, and `/bizzy/chart_datum/diagnostics` "does not appear to be published yet." Two independent signals point at the chart_datum/tide chain (not the nav stack — costmap on `map` is fine). Most likely benign: `map_tide` needs a GPS fix + tide lookup, which at the pier pre-launch may not be up until on-water with a fix. Alt: zenoh discovery miss on latched/low-rate topics. Flagged to operator that line-tracking (map-frame) is unaffected, but tide-corrected depth referencing would need `map_tide` alive. Awaiting operator on whether map_tide is expected up at the pier yet.

**2026-06-03 13:27 -04:00** — Tide-validity theory ruled out by chart_datum node log (`chart_datum_node_10471`, from the 13:08 launch): valid datum already computed dockside at (43.0720, -70.7117) — MLLW -28.013 m, MHHW -25.149 m, **tidal range 2.863 m**. So tides ARE estimate-valid on the pier. No newer chart_datum log after the 13:22 nav restart → chart_datum is in a group that was NOT restarted; the `map_tide→base_link` TF gap is most likely a TF-chain desync from the partial (nav-only) relaunch, not a tide-validity issue. Re-check map_tide once underway; full launch-group relaunch would force a clean handshake if it persists.

**2026-06-03 13:27 -04:00** — **Boat deploying** (operator launching into the water). Phase: prep → launch.

**2026-06-03 13:42 -04:00** — In water; `map_tide→base_link` TF still not resolving (persistent across pier→water + restart; tide-validity ruled out — see 13:27 entry). Tide-datum TF publisher not re-handshaking after the nav-only restart; full launch-group relaunch would force it. Not a blocker for map-frame line-following.

**2026-06-03 13:42 -04:00** — Operator observed "tides are high now"; requested NOAA comparison. NOAA Fort Point (8423898), MLLW metric, today: H 01:35 2.771 m / L 08:01 0.136 m / **H 14:19 2.401 m** / L 20:01 0.502 m. Predicted height now (13:42) = **2.352 m**, ~37 min before afternoon high, within ~5 cm of the 2.40 m high → operator observation matches prediction (near top of flood). Caveat: 8423898 is predictions-only (no real-time gauge), so no measured-vs-predicted delta available. Cross-check: chart_datum's tidal range 2.863 m vs NOAA today's range ~2.64 m agree to ~0.2 m. (Field-host tide fetch was operator-requested in-situ, not pre-flight.)

**2026-06-03 13:45 -04:00** — Operator reports onboard **predictor reading 2.97 m** vs NOAA predicted **2.35 m** now (high 2.40 m @ 14:19, morning high 2.77 m). **Δ +0.62 m** — a clean offset above even the morning high, characteristic of a datum/station mismatch rather than a real water-level difference. Predictor = `mru_transform` (publishes `map_tide`); node pid 10469 from the 13:08 launch (un-restarted group). Could not pull predictor's configured datum/station mid-ops: `mru_transform` log empty, tide config not in `bizzyboat.yaml`. **Operationally moot today** — `map_tide` TF is down, so the predictor value isn't applied to the TF tree/survey data; this is a calibration/config concern, not a live hazard. Asked operator which display/topic + datum the 2.97 reading is from. **Wrap-up TODO:** reconcile mru_transform tide predictor datum/station against NOAA Fort Point (8423898, MLLW); cross-check whether the +0.62 m is a datum offset. Deferred — do not chase mid-deployment (sterile cockpit).

**2026-06-03 13:46 -04:00** — Operator starting **avoider test: survey line close to the pier** (nav#63 validation). Avoider ON; tests whether seafloor PR #41's 3 m/1.0 local-costmap buffer (vs the old 150 m/0.05 chart-inflation halo) stops the pier-edge weaving. 13:22 nav relaunch would have picked up PR #41 if built in. Success criteria (per issue): line tracks straight near the charted pier (no AvoidanceController weaving) yet still deviates for a discrete obstacle. Watching for weaving on operator report; cmd_vel/controller-rate checks ready (`/bizzy/cmd_vel`, `/bizzy/controller_server/cmd_vel` allowlisted).

**2026-06-03 13:51 -04:00** — Avoider test result, two distinct systems: (1) **AvoidanceController weaving = much better** (operator-reported) — PR #41's 3 m/1.0 local buffer working. (2) **Boat still getting stuck = `collision_monitor`**, a SEPARATE reflex system reading `segments_to_pointcloud_reflex` (NOT the costmap), so PR #41 has no effect on it. Confirmed via collision_monitor log (pid 12816, 13:22 restart): in ~30 s around 13:46:09–13:46:43 the node logged **2× "Robot to stop due to CollisionStop polygon"** (13:46:22, 13:46:42) interleaved with ~15× **"slowdown for 30 percents due to CollisionSlowdown polygon"** (= nav#64 speed-limited CA). Both my `cmd_vel` hz probes returned nothing — consistent with the boat being halted by CollisionStop, not a discovery miss. The charted pier is inside the CollisionStop polygon → repeated full stops. Lever is the **collision_monitor polygon sizes** (`CollisionStop`/`CollisionSlowdown`), a safety-margin trade-off = operator's call: (a) shrink CollisionStop to run closer, (b) keep lines further off the pier, (c) manual override past the pier segment. Caveat: zenoh `param set` can silently drop (verify w/ get); runtime polygon changes not durable across relaunch (push to YAML). Reinforces issue note: autonomous CA is fallback-only with students. Validates memory [[ca-independent-of-seasurfacelayer]] (CA reads reflex pointcloud, not costmap).

**2026-06-03 13:57 -04:00** — Started 1-hour camera recording via `bizzyboat_project11/scripts/record_camera_topics.sh 3600` (bg task bq9jc59wa). Confirmed up: subscribed to 4 OAK cameras (ffmpeg + segmentation raw/compressed/camera_info), /tf, /tf_static, /diagnostics, /bizzy/robot_description, /bizzy/local_costmap/costmap. Bag: `~/data/logs/bizzy_images/bag_2026-06-03T13.57.27_ffmpeg_seg` (mcap zstd_fast). Runs to ~14:57.

**2026-06-03 13:57 -04:00** — Operator suspects avoider viz overloading CAMP via udp_bridge. Findings (from source): avoider publishes `survey_obstacle_avoidance` (`visualization_msgs/MarkerArray`, QoS depth 1, lifetime 2 s) inside `computeVelocityCommands` → **every control cycle = 5 Hz** (`controller_server.controller_frequency: 5.0` in seafloor nav2_params.base.yaml; the 50.0 is `docking_server`, unrelated). Marker payload = 3 markers (nominal/adjusted LINE_STRIP + deviation LINE_LIST) with point count ∝ survey-line length (1 pt/path-pose-or-station, 24 B/pt). **Bridge `maximum_packet_size: 1000`** (ccomjhc_project11/configuration/dev_bridge.yaml; remote = salmonz:4200 over zerotier) → a long survey line's MarkerArray exceeds 1 KB and fragments into many UDP packets/publish; effective bridge packet rate ≈ 5 × ⌈msg_bytes/1000⌉. So overload mechanism = payload×fragmentation, not the 5 Hz itself. Offered live `topic bw` to confirm. Mitigation (operator's call): (1) stop bridging survey_obstacle_avoidance to CAMP (pure viz), (2) throttle bridged copy to ~1 Hz, (3) decouple viz publish from control loop (code change, wrap-up). **Wrap-up TODO:** consider decoupling avoider viz rate from controller_frequency and/or throttling at the bridge.

**2026-06-03 14:04 -04:00** — Operator throttled the avoidance viz topic to **1 Hz at the bridge** (CAMP load mitigation, lever 2); reports it "might have helped" CAMP responsiveness. Reran the near-pier survey → **got stuck again at the same obstacle**. Confirms the two-systems separation: the bridge throttle addresses CAMP bandwidth, NOT the `collision_monitor` CollisionStop polygon that halts the boat — same obstacle inside the stop zone, same stop. Stop-polygon retune deferred to wrap-up (CA fallback-only; near-pier clean line deprioritized for today).

**2026-06-03 14:04 -04:00** — **Phase shift: moving to sonar testing + Cod Rock survey** (away from the pier). Relevant open checklist for this leg: M3 1PPS lock confirm (sonar console, carry-forward from #205); cross-track/SE-undulation (#164) characterization on clean Cod Rock lines; OTH distance test (#130) if ranging to/from Cod Rock; command-logging verify (#209). Camera recording (bq9jc59wa) still running until ~14:57.

**2026-06-03 14:32 -04:00** — While hovering (salmon's agent fixing a CAMP bug), tested extended sea-surface perception range. SeaSurfaceLayer current config: `maximum_range: 150.0` (not the limiter), `min_grazing_angle_deg: 2.1` → effective range ~38 m at camera height ~1.4 m. Operator wanted ~100 m. Reach ≈ h/tan(grazing); for 100 m at h=1.4 m → grazing ≈ arctan(1.4/100) ≈ **0.80°**. **Set live** `ros2 param set /bizzy/local_costmap/local_costmap sea_surface_layer.min_grazing_angle_deg 0.8` → "Set parameter successful"; **verified** via `param get` = 0.8 (no zenoh silent-drop). maximum_range left at 150 (already > 100). Caveat noted to operator: pitch sensitivity dR/dθ ≈ 125 m/deg at 0.8° (±0.5° pitch ≈ ±60 m at far edge) — testable only because hovering (low pitch); will reintroduce near-horizon glare FPs the 6/01 tuning gated. **NOT durable** — reverts to 2.1 on nav relaunch; set back to 2.1 when done. Watching for far-field marks to ~100 m + FP rate.

**2026-06-03 14:34 -04:00** — At grazing 0.8° (~100 m), operator observed **noise beyond ~50 m** (matches the pitch-sensitivity prediction — far-horizon rays unreliable). Stepped grazing to **1.6°** (~50 m effective range) to cut the noisy near-horizon band. `param set` → success, `param get` verified = 1.6. Evaluating whether ≤50 m resolves cleanly; if so, 1.6°/~50 m is a candidate to bake into `nav2_overlay.yaml` for durability (vs current baked 2.1°/~38 m). **Empirical finding: ~50 m is roughly the clean/noise boundary for the 4-OAK rig at h~1.4 m.** Still not durable — reverts on relaunch.

**2026-06-03 14:42 -04:00** — Evidence-model tuning for "consistent targets persist, sporadic ones don't show until solid." SeaSurfaceLayer is a log-odds Bayesian buffer (occupancy_buffer.hpp): per-cell evidence, `max_evidence_step` caps per-observation contribution (flicker rejection), `decay_half_life_s` halves unobserved evidence, cost map = invisible below `free_threshold`, lethal above `lethal_threshold`, ramp between. Root of sporadic-shows: default `free_threshold:0.0` (any evidence visible) + one observation adds up to `max_evidence_step:0.85` (near the 1.0 lethal default) → single flicker = near-lethal instantly. Fix set live + verified: **`free_threshold 0.0→0.9`** (single hit ≤0.85 stays invisible; needs 2nd frame) + **`lethal_threshold 1.0→2.5`** (~3 consistent frames for solid). Left max_evidence_step (0.85), decay (60 s), obstacle_clamp (15), clear_floor (−0.5) as-is — those already give strong persistence for confirmed marks ("consistent remain"). Also bumped **grazing 1.6→1.0° (~80 m range)** per operator. All three verified via param get (0.9 / 2.5 / 1.0). Trade-off flagged: thresholds add detection latency (couple frames) — fine hovering, watch reaction distance underway. **NOT durable**; relaunch reverts to grazing 2.1 / free 0.0 / lethal 1.0. **Wrap-up candidate** if it performs: bake grazing + free/lethal thresholds into nav2_overlay.yaml.

**2026-06-03 14:45 -04:00** — Added per-pixel detection-strength gate. `obstacle_prob_min` = the segmentation-confidence gate: pixel contributes +obstacle evidence only if P(obstacle) > obstacle_prob_min (zero-crossing at that value, per segments_projection.hpp pixel_log_odds); also gates the binary waterline-contact hit (px ≥ obstacle_prob_min·255). Orthogonal to the temporal free/lethal thresholds. Set live + verified **`obstacle_prob_min 0.8→0.9`** ("only consider stronger targets"). Trade-off: drops weak-but-real low-contrast/distant targets the net is <90% sure of. **Full live SeaSurfaceLayer test stack now: grazing 1.0° (~80 m), free_threshold 0.9, lethal_threshold 2.5, obstacle_prob_min 0.9.** All runtime-only; relaunch reverts to grazing 2.1 / free 0.0 / lethal 1.0 / obstacle_prob_min 0.8. Wrap-up: if this stack performs, bake the set into nav2_overlay.yaml; watch for missed faint real targets at 0.9.

**2026-06-03 14:49 -04:00** — **obstacle_prob_min 0.9 confirmed too aggressive — losing legit targets** (operator). Reverted **0.9→0.8** (verified). Empirical result: **0.8 is the right per-pixel confidence gate** (confirms the 6/01 baked value); 0.9 drops real low-contrast/distant targets. Live stack now: grazing 1.0° (~80 m), free_threshold 0.9, lethal_threshold 2.5, **obstacle_prob_min 0.8**. So obstacle_prob_min stays at its baked default; the tuning candidates to potentially bake are grazing + free/lethal thresholds (pending how they perform).

**2026-06-03 14:50 -04:00** — Operator satisfied with the tuned stack; starting a **survey line** with it + an **updated CAMP** (salmon's agent's CAMP-bug fix presumably landed). Phase: hover-tuning → underway survey. Validation note: stack was tuned while hovering (low pitch); under way the grazing-1.0°/~80 m far field will see real pitch motion → the ~50–80 m band may get noisier than it looked stationary. The free 0.9 / lethal 2.5 evidence gating is expected to suppress that transient noise — this run tests exactly that. Camera recording bq9jc59wa still running (~7 min left, ends ~14:57).

**2026-06-03 14:59 -04:00** — Camera recording rollover (operator wants continuous hourly capture). Hour 1 (bq9jc59wa, bag `...T13.57.27_ffmpeg_seg`, ~2.6 GB) closed clean (exit 0). Hour 2 launched: bg task bo01iqkie, bag `bag_2026-06-03T14.58.57_ffmpeg_seg`, started 14:58:57, runs to ~15:58:57. **~90 s gap** between the two (hour 1 wrapped 14:57:27 → hour 2 started 14:58:57) — relaunch waits on completion notification + mcap flush, not seamless. For gap-free next time, start replacement ~30 s before prior ends (brief overlap). Will restart again at next wrap unless told otherwise.

**2026-06-03 15:01 -04:00** — Survey running with tuned SeaSurfaceLayer stack, BUT operator can't evaluate detections live: **CAMP still being fixed (salmon's agent), infrequent costmap updates in CAMP** → no live read on how the tuning performs. Mitigation/reassurance: the active recording (bo01iqkie) captures `/bizzy/local_costmap/costmap` + all 4 raw segmentation feeds + camera_info + TF — i.e. the exact inputs for `bag_to_costmap_video`/`sea_surface_tuner` offline replay (identical costmaps from the same algorithm). So the tuned-stack (grazing 1.0/free 0.9/lethal 2.5) detection performance is fully recorded for frame-by-frame wrap-up analysis despite the blind live view. Boat-side local costmap updating internally fine; only the bridged CAMP copy is stale (CAMP/bridge issue, not boat). Offered operator a live boat-side sanity check (lethal_grid publish / lethal-cell count) independent of CAMP — pending their call.

**2026-06-03 15:22 -04:00** — Operator asked about the separate forward camera. Findings: node `/bizzy/sensors/cameras/usb/usb_camera` (usb_cam driver, /dev/video0, 640×360 @ 5 FPS) running and **capturing fine** (node log: "Starting usb_camera … mjpeg2rgb at 5 FPS", "Timer triggering every 200 ms", no capture errors). **FAULT: its ffmpeg/h264 transport is broken** — `cannot open codec libx264: Generic error in an external library` / `cannot initialize encoder`, repeating (triggered when my hz probe subscribed to the lazy ffmpeg topic). OAK ffmpeg encoders are fine (recording proves it) — only the USB camera's. Working transports: raw `image_raw`, `compressed`, `zstd`. Operator confirms **compressed is bridged to the operator station and works**, but adding it to the **rqt image grid plugin crashed** the plugin. No custom image-grid plugin in workspace (ccomjhc/layers) → stock rqt, operator-station-side (salmon's agent territory; can't observe from gabby). Likely cause: grid enumerates ALL advertised transports for the topic, hitting the **broken `image_raw/ffmpeg`** and/or **`image_raw/compressedDepth` advertised on a color image** → classic rqt segfault. Workarounds given: pin transport to `compressed` explicitly; isolate in standalone rqt_image_view to confirm grid-vs-stream; else salmon's agent fixes grid transport handling. **Wrap-up TODOs (boat-side faults):** (1) usb_camera ffmpeg/libx264 encoder won't initialize on gabby; (2) usb_camera advertises compressedDepth on a non-depth color image — both destabilize naive viewers. Note: USB forward camera is NOT in the ad-hoc camera recording (recorder captures OAK ffmpeg topics only).

**2026-06-03 15:28 -04:00** — Operator reports forward USB camera shows black; asked me to capture a frame. Grabbed one frame off `/bizzy/sensors/cameras/usb/image_raw/compressed` (headless rclpy+cv2 decode): valid 640×360 JPEG, 4307 bytes, decodes fine, but **mean=0, min=0, max=0 — every pixel exactly 0 (pure black, confirmed for me too)**. Key diagnostic: **exactly-zero (not noisy-dark) rules out lighting/lens/exposure** — a real dark scene always has some sensor noise (min>0). The node, 5 FPS capture timer, JPEG encode, compressed transport, and station bridge are all alive and cycling — only the actual sensor pixels are missing → **driver publishing blank/zeroed buffers; sensor stream not reaching the pipeline.** Likely: pixel-format mismatch (driver started `mmap (mjpeg2rgb)`; device may not be delivering MJPEG / stream failed to start) or USB/sensor not streaming (device node present, no data). Field fix: USB re-seat / power-cycle that camera, then re-grab to confirm. Offered `v4l2-ctl --list-formats-ext`/`--all` on /dev/video0 to narrow format-vs-hardware. **Wrap-up TODO (3rd usb_camera fault):** forward USB camera (/dev/video0) emits all-zero black frames despite healthy pipeline — sensor/format capture fault.

**2026-06-03 15:33 -04:00** — **ROOT CAUSE of black frames found via v4l2-ctl.** Device healthy: "HD USB Camera", uvcvideo, USB enumerated, input ok. Offers MJPG at 1920×1080/1280×720/1024×768/640×480/800×600/1280×1024/320×240 — **no 640×360**. ROS driver started at **640×360** (unsupported) → device delivers 640×480 MJPG, size mismatch → mjpeg2rgb yields all-zero black buffers. **Exact source: `bizzyboat_project11/launch/perception_launch.py:115` hardcodes `'image_height': 360`.** Fix = one-liner **360→480** (640×480 supported; width 640 ok); requires camera/launch **restart** to take effect (device re-open) — deferred, not done mid-survey (sterile cockpit; forward cam non-critical to sonar/Cod Rock run). Side-findings: (a) a sibling **`izzyboat_project11/`** dir (izzy, not bizzy) holds correct `camera_forward_640_480.yaml` + `camera_forward.yaml` 1920×1080 but bizzy's launch doesn't use them — possible stray/duplicate dir, wrap-up glance; (b) **`bizzyboat_project11/config/bizzyboat_operator_camera_grid.yaml`** is the in-repo operator camera-grid config — candidate to inspect for the earlier rqt-grid crash. **Wrap-up TODOs updated:** fix perception_launch.py image_height 360→480 (the actual black-frame fix); plus the ffmpeg/libx264 + compressedDepth-on-color faults; review izzyboat_project11 stray dir; inspect operator_camera_grid.yaml for grid crash.

**2026-06-03 15:37 -04:00** — OTH + underway: operator reports **costmap not registering everything**. Likely cause = the evidence-gating raised while hovering (free 0.9 / lethal 2.5) being too strict underway (targets harder to confirm frame-to-frame in motion) → under-registration; the predicted "drops real targets underway" risk realized. (Alt cause: OTH comms dropping the bridged costmap to station — boat costmap fine; would be a #130 finding, not perception.) Operator chose partial revert ("a bit lower"). Set live + verified **free_threshold 0.9→0.5**, **lethal_threshold 2.5→1.5** (single solid hit now registers on ramp; ~2 frames to lethal vs ~3). Live stack: grazing 1.0°/~80 m, free 0.5, lethal 1.5, obstacle_prob_min 0.8. Next step if still under-registering: take to baked defaults (free 0.0 / lethal 1.0); if that doesn't fix it, missing marks = OTH comms link, not perception. **Lesson for wrap-up: tune SeaSurfaceLayer evidence thresholds UNDERWAY, not while hovering — hover over-confirms and hides the motion-confirmation difficulty.**

**2026-06-03 15:39 -04:00** — Operator: **"looks good"** with free 0.5 / lethal 1.5 — costmap registers properly at OTH/underway now. **Field-validated underway tuning: grazing 1.0° (~80 m), free_threshold 0.5, lethal_threshold 1.5, obstacle_prob_min 0.8.** Strong candidate set to bake into `nav2_overlay.yaml` at wrap-up (currently runtime-only; reverts on relaunch). Note the contrast: hover-tuned 0.9/2.5 under-registered underway; 0.5/1.5 is the motion-validated balance.

**2026-06-03 15:46 -04:00** — Boat **oscillating / not running straight**; operator unsure planner vs avoider (the "path keeps changing"). Confirmed control path: node `/bizzy/controller_server`, plugin `FollowPath` = AvoidanceController decorator (wraps crabbing inner), param `FollowPath.obstacle_avoidance_weight` (default 1.0). Offered the issue's prescribed diagnostic+mitigation `ros2 param set /bizzy/controller_server FollowPath.obstacle_avoidance_weight 0.0` (stops → avoider; persists → planner/controller). Suspect link: the threshold-lowering + 80 m range we just set makes the avoider register/chase more far-field marks → weaving. **Operator instead chose to try a fixed trackline** (removes planner replanning from the loop) — itself a clean isolation: trackline straight ⇒ planner-replanning was the cause; trackline still weaves ⇒ controller/avoider (then weight=0 test isolates the avoider). obstacle_avoidance_weight NOT changed (operator went the trackline route). Awaiting trackline tracking result.

**2026-06-03 15:46 -04:00** (cont.) — Trackline result: **still oscillating but doing better** → planner replanning was a real contributor (removing it helped), but **residual oscillation is in the controller/avoider layer** (persists on a fixed line). weight=0 test offered again as the unambiguous next isolation; operator pivoted to perception tuning instead.

**2026-06-03 15:51 -04:00** — Operator: not always detecting lobster pots; wants ~50 m range + more sensitivity. Lobster pots = small/low-contrast/low-in-water → weak per-pixel segmentation + few frames in view. Set live + verified: **grazing 1.0→1.6° (~50 m)**, **obstacle_prob_min 0.8→0.7** (primary weak/small-target lever), **free_threshold 0.5→0.3**, **lethal_threshold 1.5→1.2** (brief sightings register). Synergy: pulling range to 50 m also trims far-field marks that were feeding the oscillation. Trade-offs flagged: obstacle_prob_min<0.8 re-opens glare-FP risk the 6/01 tuning closed; more marks → more avoider reaction (watch oscillation). Live stack now: grazing 1.6/~50 m, free 0.3, lethal 1.2, obstacle_prob_min 0.7. Next lever if pots still missed: obstacle_prob_min → 0.65.

**2026-06-03 15:53 -04:00** — Operator: **glare causing false positives at obstacle_prob_min 0.7, but off-axis — not a big deal**, accepting the trade-off. Matches the predicted glare-FP cost of lowering obstacle_prob_min below the 6/01's 0.8. Off-axis = not in the heading corridor, so avoider/collision_monitor largely won't react; pot sensitivity retained. Lever if glare drifts on-axis or feeds the weave: obstacle_prob_min back toward 0.75. **Wrap-up note: 0.7 trades glare FPs for lobster-pot sensitivity — acceptable when FPs are off-axis; revisit whether 0.75 is a better default balance.**

**2026-06-03 15:54 -04:00** — ✅ **BOAT AVOIDED A REAL LOBSTER POT** (operator-reported). Validates both halves at once: (1) the perception sensitivity tuning (grazing 1.6/~50 m, obstacle_prob_min 0.7, free 0.3, lethal 1.2) detected a small low-contrast target; (2) the AvoidanceController deviated for a discrete real obstacle — the nav#63 "still deviates for a discrete obstacle" success criterion. First clean field demonstration of the full detect→avoid chain working as intended this deployment.

**2026-06-03 15:54 -04:00** — ⚑ **FLAGGED FOR REVIEW** (operator request). Event: the lobster-pot detection→avoidance above, ~15:54. Captured in camera recording **bag `bag_2026-06-03T14.58.57_ffmpeg_seg`** (task bo01iqkie) — segmentation (4 OAK) + `/bizzy/local_costmap/costmap` + TF, ~55 min into that bag. **Wrap-up: replay this window via `bag_to_costmap_video`/`sea_surface_tuner` to study the detection→avoid sequence and validate the tuned-stack params against the real pot.** Active tuning at the moment of the event: grazing 1.6°/~50 m, obstacle_prob_min 0.7, free_threshold 0.3, lethal_threshold 1.2 (all runtime-only).

**2026-06-03 15:56 -04:00** — **Traffic: BizzyBoat being overtaken by a speed boat** (operator-reported). Dynamic vessel passing. Heads-up given to operator: with current sensitive tuning (obstacle_prob_min 0.7) + decay_half_life_s 60 s, the speed boat + its wake may paint a trail of marks that linger ~1–2 min after it passes and could feed the avoider/weave in its wake. No mid-encounter action taken (sterile cockpit). Standing by for operator (manual takeover / collision_monitor watch / shorter decay to clear wake marks faster).

**2026-06-03 15:58 -04:00** — Operator checked recording status during the encounter. Hour-2 bag (bo01iqkie, `...T14.58.57...`) confirmed live (2.5 GB, writing) but due to wrap at 15:58:57 with the usual ~90 s rollover gap — right during the overtaking. To guarantee gap-free capture of the speed-boat pass, **proactively started an OVERLAPPING recorder** (bd6iopdwa, `bag_2026-06-03T15.58.30_ffmpeg_seg`) ~27 s before hour-2 ends → continuous coverage, no gap. Will NOT double-start when bo01iqkie wraps (bd6iopdwa covers it); next restart when bd6iopdwa ends (~16:58:30). This is the gap-free pre-overlap method (vs the ~90 s gap at the 14:58 rollover).

**2026-06-03 16:03 -04:00** — ⚑ **FLAGGED FOR REVIEW — manual takeover at a lobster pot, avoider direction disagreement.** Operator quote: *"had to take manual control to get around a lobster pot, estop slow and drifted the boat in one direction, but the avoider was suggesting the other direction."* Factual context: operator took manual control to clear a lobster pot; an e-stop/slow was applied → boat slowed and **drifted one direction** (with wind/current — a slowed/stopped hull loses steerage authority, so drift is environmental), while the **AvoidanceController had been suggesting the opposite side** to pass the pot. Note the distinction (no mechanism invented): the drift direction is set by wind/current under reduced steerage and is independent of the avoider's planned path, so "drifted one way, avoider suggested the other" is drift-vs-plan, not necessarily an avoider error — BUT *which side the avoider chose* is the real review question. Captured in recording **bd6iopdwa** (`bag_2026-06-03T15.58.30_ffmpeg_seg`) — segmentation + `/bizzy/local_costmap/costmap` + TF at ~16:03 (~5 min into that bag). **Wrap-up RCA:** replay this window — why did the avoider pick that side (costmap marks on the other side? glare FPs? the pot's mark geometry?), and review e-stop/slow behavior leaving the boat to drift near an obstacle. Active tuning at event: grazing 1.6/~50 m, obstacle_prob_min 0.7, free 0.3, lethal 1.2.

**2026-06-03 16:03 -04:00** — (Pending trackline task, paused for the above) Confirmed `FollowPath.default_speed` units = **m/s** (crabbing_path_follower.cpp fallback msg "%.3f m/s"); current 1.5 = **1.5 m/s ≈ 2.9 kn**, NOT 1.5 kn. Operator's requested **1.5 knots ≈ 0.77 m/s**. Trackline geometry + per-line speed are authored in CAMP → pushed via mission_manager/camp_interface → manda_coverage (agent does NOT author harbour-entry waypoints — no chart, safety-critical). Anchor fix at request time: lat 43.0546936, lon −70.7092194 (Little Harbour approach). To run 1.5 kn either set per-line speed in CAMP or lower controller default_speed to ~0.77 m/s — deferred until operator returns to trackline planning.

**2026-06-03 16:04 -04:00** — Operator: **"much smoother at 1.5 knts"** — tracklines running clean at 1.5 kn for the Little Harbour entry. **Key finding: the oscillation/weave is SPEED-DEPENDENT** — clean at 1.5 kn, oscillates at transit speed. Combined with the earlier trackline result (planner replanning was a contributor), this points to the controller/avoider (crabbing PID + cross-track / helm-gain) over-correcting at higher speed but tracking cleanly slow — #164 cross-track / #124 turning-limits territory. **Wrap-up candidate: speed-scaled controller gains, or characterize the cross-track PID overshoot vs speed.** (Speed set operator-side — not via agent.)

**2026-06-03 16:04 -04:00** — **Traffic: sailboat under power overtaking BizzyBoat** during the Little Harbour approach (operator-reported). Power-driven vessel (motoring). BizzyBoat slow at 1.5 kn with steerage for it to pass. Captured in recording bd6iopdwa; wake may leave brief lingering marks (sensitive tuning + 60 s decay). No mid-encounter action (sterile cockpit); standing by.

**2026-06-03 16:06 -04:00** — Operator: this trackline leg heads **into the glare**, "not too bad so far." This is the stress case for the sensitive `obstacle_prob_min 0.7` tuning — heading into the sun puts glare FPs **on-axis** (in the path corridor) vs the earlier off-axis glare. Watching whether on-axis glare marks start feeding the avoider/collision_monitor. Lever staged: raise obstacle_prob_min toward 0.75–0.8 if it gets bad. Holding at 0.7 per operator ("not too bad").

**2026-06-03 16:10 -04:00** — **Traffic: another speedboat passed** (operator-reported). Busy water in the Little Harbour area today (speedboat overtake 15:56, sailboat-under-power overtake 16:04, this speedboat 16:10). Captured in bd6iopdwa. No action needed.

**2026-06-03 16:39 -04:00** — **Traffic: another vessel passing** (operator-reported, back-to-back with the prior). Heavy boat traffic in Little Harbour this afternoon. Captured in bd6iopdwa.

**2026-06-03 16:40 -04:00** — **Traffic: a curious vessel passed closer** (operator-reported) — onlooker checking out the USV. Useful capture: a real moving vessel at short range in bag bd6iopdwa (~42 min in) — good sample for reviewing perception + collision_monitor/avoider response to a close dynamic contact at the sensitive 0.7 / 50 m tuning. No boat reaction reported by operator.

**2026-06-03 16:43 -04:00** — Operator **slowed to 1.5 kn amongst more obstacles** (denser mooring/traffic area). Prudent — more reaction margin for collision_monitor/avoider, smoother tracking. Speed via the resend method (nav#35).

**2026-06-03 16:44 -04:00** — **Traffic: a zodiac crossed BizzyBoat's bow** (operator-reported). Crossing-ahead = the prime CA-trigger scenario (obstacle directly in path → most likely to trip collision_monitor stop / avoider deviation). Good real-traffic CA test; captured in bd6iopdwa (~46 min in). Offered prompt-free collision_monitor log read to see if CA reacted — pending operator. No reaction reported.

**2026-06-03 16:49 -04:00** — Operator **switched to a line crossing UNDER the bridge, at 1 kn.** Heads-up given: **GPS multipath/dropout under the bridge span** — overhead structure can make the position estimate jump/degrade, and the trackline follower keys off position → tracking may get briefly ragged or fix quality drop under the bridge (would be the FIX, not the controller). Bridge abutments/pilings at waterline = real obstacles if close. 1 kn appropriate (limits how far a position glitch carries). Offered to check GPS/RTK fix quality (gps_rtk_diagnostics) if tracking misbehaves under the span.

**2026-06-03 17:00 -04:00** — Recording rollover: hour-3 (bd6iopdwa, `...T15.58.30...`) closed clean (exit 0); hour-4 started (b31msueyg, `bag_2026-06-03T17.00.37_ffmpeg_seg`, runs to ~18:00). **~2 min gap** (16:58:30→17:00:37, notification-driven restart, not pre-overlap). Offered pre-overlap for any specific event (e.g. under-bridge). Still auto-restarting hourly per operator's standing instruction.

**2026-06-03 17:04 -04:00** — ⚑ **FLAGGED FOR REVIEW — FAILED to cross under the bridge; cause = collision_monitor CollisionStop on the bridge.** Operator: "failed to cross under the bridge." Diagnosed via collision_monitor log (pid 12816): **repeated `CollisionStop` polygon events through the attempt** ≈17:00:46→17:03:46 (stops at 17:00:46, 17:01:12, 17:01:21/24, then a ~104 s clear, then 17:03:08/09/13/22/46) — boat halted over and over at the bridge threshold, couldn't proceed. **Root: the bridge (abutments/structure, or the dark under-span region segmented as obstacle) trips the reflex CollisionStop polygon.** This is the `collision_monitor` (reads `segments_to_pointcloud_reflex`, NOT the SeaSurfaceLayer costmap) → **none of today's perception tuning caused it**; the bridge is genuinely seen as lethal. NOT a GPS issue (signal is CA stops, not a fix dropout). Mitigation: **manual override to transit under the bridge** (CA can't distinguish a passable bridge from a wall), or temporarily relax/disable collision_monitor stop polygon (operator's safety call). **Operational limitation: BizzyBoat will NOT autonomously transit under a bridge — reflex CA stops it every time.** Captured in recording b31msueyg. **Wrap-up: bridge/overhead-structure handling for the reflex CA (how should it treat passable overhead spans?) — relates to nav#64 / CA-fallback-only posture.**

**2026-06-03 17:15 -04:00** — ⚑⚑ **FLAGGED FOR REVIEW — bridge is NON-TRANSITABLE: both control modes fail at the span.** Operator: "tried manual under the bridge but lost starlink, the currents pushed me back." Compounding failure: (1) **autonomy** can't cross — collision_monitor CollisionStop (17:04 entry); (2) **manual** can't cross — the bridge span **occludes Starlink** (sky-view-dependent satellite comms) → command link drops mid-passage → no control authority; (3) **current then dominates** — boat drifted back out (benign this time, but a different current set could push it INTO the bridge/pilings = the hazard). So the bridge is the unique spot where BOTH control modes fail at once and the boat is at the mercy of current. **Failsafe concern:** boat DRIFTED on comms loss rather than holding station → suggests no station-keep-on-comms-loss engaged (offered to check mavros/failsafe config). **Recommendation: do NOT re-attempt this bridge** without (a) a non-satellite comms link that survives under the span, or (b) an open-loop dead-reckoning "transit-under-bridge" maneuver needing no live comms — both wrap-up design items, not field fixes. Confirmed operator safety status pending. Captured in b31msueyg. **Wrap-up TODOs: comms-loss failsafe behavior (station-keep vs drift); under-bridge transit strategy (local comms or open-loop); add bridge occlusion to Starlink/comms limitations.**

**2026-06-03 17:19 -04:00** — Operator **abandoned the bridge (correct — non-transitable) and is back-tracking home at 3 kn.** Phase shift: **Little Harbour ops → return transit home** (likely heading toward recovery). Back-tracking the known-clear route out through the mooring field, then exposed water. Watch notes: at 3 kn in exposed Gulf of Maine the weave may return (speed + sea-state both up) but it's transit not survey = cosmetic; mooring-field visual-lookout caution on the way out. Recording b31msueyg running (~18:00).

**2026-06-03 17:35 -04:00** — Operator: "make it take fewer hits to stick to the costmap." Lowered **`lethal_threshold` 1.2 → 0.9** (verified) — with max_evidence_step 0.85, marks now latch in ~1 strong hit vs ~1.5. Live stack: grazing 1.6/~50 m, free 0.3, **lethal 0.9**, obstacle_prob_min 0.7. Trade-off: a single flicker/glare FP also latches more readily (less temporal rejection). Further levers offered: lethal→0.8 (instant 1-hit stick) or raise max_evidence_step. Done on the return transit. (Note: combined with this lethal-0.9, the running stack diverges further from the baked nav2_overlay defaults — all runtime-only, reverts on relaunch; wrap-up bake-decision pending.)

**2026-06-03 17:38 -04:00** — Operator **adjusted the trackline to improve sonar coverage** (M3 coverage/overlap optimization) during the return. Sonar data captured by the standing sonar_logger (pid 10029, bizzyboat.yaml), separate from the camera bag.

**2026-06-03 17:45 -04:00** — Operator: **sound speed sensor reported NaN for a while, now recovered.** Data-QC implication: sound speed feeds the M3 travel-time→depth conversion, so **bathymetry collected during the NaN window may be on stale/default sound-speed → flag for review/reprocessing** with correct SV. Overlaps the just-adjusted sonar-coverage trackline. Recovered ~17:45 (window start uncertain). Offered to pull the sound-speed sensor log to pin the exact NaN start→recovery window for documenting the affected data segment. **Wrap-up/QC TODO: bound the NaN window and check bathy SV correction over it; investigate the NaN dropout cause (sensor glitch vs comms).**

**2026-06-03 18:00 -04:00** — **BizzyBoat back at pickup, awaiting physical recovery** (operator-reported). Phase shift: **return transit → recovery.** Boat home safe after full day (pier launch → exposed survey/OTH → Little Harbour entry → bridge-crossing failure → return). Housekeeping queued: stop camera recording + hourly auto-restart loop once boat is out of water (pending operator confirm — currently still rolling for the recovery approach); all SeaSurfaceLayer runtime tuning reverts on next relaunch (wrap-up bake decision pending). Day's log carries multiple ⚑ FLAGGED-FOR-REVIEW items (lobster-pot detect→avoid success, U-turn course reversal, avoider-direction disagreement, bridge non-transitable / dual control-mode failure) + wrap-up TODOs for /wrap-up-deployment.

**2026-06-03 18:16 -04:00** — **Boat OUT of the water; RC + LOITER during recovery; on-water ops COMPLETE.** Recovery recorder (b4zs91i87, 15-min cover) stopped via SIGINT — clean (exit 0, mcap finalized); no restart. Confirmed no ad-hoc recorders running (standing launch loggers untouched — pattern targeted only the `timeout --signal=INT 900` recovery recorder). **Today's camera bags (all clean): `...T13.57.27` (h1), `...T14.58.57` (h2), `...T15.58.30` (h3, overlap/speed-boat), `...T17.00.37` (h4), `...T18.01.35` (recovery cover).** Pending: commit this host log (offered); then `/wrap-up-deployment` for deferred RCA on the flagged items + the wrap-up TODO backlog.

**2026-06-03 18:19 -04:00** — Closing notes (gabby on-water perspective; formal Summary/Lessons synthesis → dev log per host-log convention):
- **Biggest win:** live SeaSurfaceLayer perception tuning culminating in a validated **lobster-pot detect→avoid** (16:54 ⚑). Field-validated stack settled at **grazing 1.6° (~50 m), free_threshold 0.3, lethal_threshold 0.9, obstacle_prob_min 0.7** — all runtime-only; **bake-into-nav2_overlay.yaml decision is the top wrap-up item.**
- **Tuning lessons:** tune evidence thresholds **underway, not hovering** (hover over-confirms); tracking quality is **confounded by speed AND sea-state** (can't separate from today's runs — needs controlled exposed-vs-sheltered test).
- **Top operational finding:** the **bridge is non-transitable** — autonomy stops (collision_monitor reflex) AND manual fails (bridge occludes Starlink → lost link → current pushed back). Comms-loss → **drift, not station-keep** (failsafe gap).
- **Autonomy anomalies for RCA:** U-turn course reversal (16:15 ⚑); avoider direction disagreement at the lobster pot (16:03 ⚑); 4 m planned-vs-follower gap (likely avoider-adjusted path around glare FP, not float precision).
- **Hardware/data faults found:** forward USB camera **black frames** (640×360 unsupported → fix `perception_launch.py` 360→480), usb **ffmpeg/libx264 encoder broken**, **compressedDepth advertised on color** (likely the rqt-grid crash); **sound-speed NaN window** (bathy SV-correction QC).
- **Checklist wins:** **nav#35 mission re-send mid-line ✅** (class-significant); lobster-pot detect→avoid ✅. Clean-survey validation partly met via the perception work; the bridge/harbour detour dominated the afternoon.
- **Feature request logged:** live speed override without mission resend (interim: missions w/o per-line speed + live default_speed).
- Recovery clean: RC + loiter, boat out, 5 camera bags captured, recorders stopped. Hand off to `/wrap-up-deployment` (dev side) for RCA + bake decisions.

**2026-06-03 16:15 -04:00** — ⚑⚑ **FLAGGED FOR REVIEW — autonomous U-turn / course reversal during Little Harbour entry.** Operator quote: *"the boat did a u turn and started heading back out!! went to standby, then did a hover here towards the right direction."* During the 1.5 kn trackline entry, BizzyBoat unexpectedly **reversed course (U-turn) and headed back OUT** of the harbour on its own. Operator mitigated: **STANDBY → HOVER** re-oriented toward the correct direction; boat now stable in hover. No mechanism asserted — **review questions:** trackline/line ordering in the mission (did the next goal land behind the boat?), goal/waypoint sequencing in mission_manager/manda_coverage, possible relation to nav#35 (mission re-send mid-line) / nav#58 (mid-line resume after override). **Data for RCA:** camera bag bd6iopdwa has perception/costmap/TF/motion (NOT mission/goal topics); the boat's standing loggers (logger pid 10028 / sonar_logger 10029, bizzyboat.yaml topic set) + bt_navigator/mission_manager rosout logs are where the goal/plan sequence lives. **Do NOT re-engage autonomy into the harbour until the cause is understood** (operator's call). Offered a quick boat-side rosout check (bt_navigator/mission_manager — what goal it was chasing) before re-engage.

**2026-06-03 16:17 -04:00** — Operator: hover at end of planned line **turned the boat into the glare**, causing perception issues. Root: hover behavior (marine_nav_behaviors/hover.cpp) heading mode — default `point_at_target = true` rotates the boat to FACE the hold point (which faced the sun). **Lever: set `point_at_target = false` (hold-heading mode, "LOIT_TYPE=0")** so it keeps station without yawing to face the target — avoids swinging into glare, less yaw thrust. Live-tunable per the source comment ("Read the heading mode live so an operator can A/B it on the water"). Not set now (operator moved on to a new line); staged for next hover. **Wrap-up candidate: default hover to hold-heading, or glare-aware hover heading.**

> **Clarification (2026-06-04 wrap-up):** the operator does not recall a hover-into-glare
> event. The glare observation was most likely about a **trackline/transit leg heading into
> the sun** (see the 16:06 entry), not a hover rotation — the `hover.cpp point_at_target`
> mechanism above was the agent's inference, not a confirmed hover event. No issue filed; the
> into-glare geometry is an operational note, not a behavior bug.

**2026-06-03 16:17 -04:00** — Operator started a **new trackline at 2 kn** (up from 1.5 kn; operator/CAMP-side). Watch item: weave is speed-dependent (clean at 1.5 kn, oscillates at transit). 2 kn brackets the smooth/weave threshold — watching whether oscillation returns at 2 kn.

**2026-06-03 16:18 -04:00** — Operator: in CAMP the planned line and the path-follower's output line look **~4 m apart**; asked if it's a "map frame far from home" (float32) issue. Assessment: **float32-far-from-home does NOT fit at this range** — `bizzy/map` is a local ENU frame anchored at launch (home); ~2 km transited → float32 precision ≈ 0.0002 m (sub-mm); 4 m would need millions-scale (UTM/global, ~100s of km) coords. (Couldn't pull `bizzy/map→base_link` TF — timed out ×2, likely zenoh or the broken map_tide branch — but range reasoning holds.) **Likely cause instead: the path-follower output IS the AvoidanceController's ADJUSTED path** (FollowPath = AvoidanceController wrapping crabbing) — a 4 m gap = avoider steering ~4 m around a mark, prime suspect an on-axis glare FP given this into-glare leg at obstacle_prob_min 0.7. Same mechanism as the day's weave + "avoider suggested other direction." Decisive test offered: `FollowPath.obstacle_avoidance_weight 0` → lines snap together ⇒ avoider deviation (fix: raise obstacle_prob_min); stay apart ⇒ CAMP frame/render issue. weight=0 NOT run (operator moved on).

**2026-06-03 16:24 -04:00** — **BizzyBoat now sheltered in Little Harbour** (operator-reported). Phase: harbour entry → sheltered/holding. Calmer water + less glare/chop in here should reduce the weave + avoider-deviation triggers seen on the exposed legs. Staged levers if holding: hold-heading hover (`point_at_target=false`), weight=0 test for the 4 m offset. Recording bd6iopdwa running (to ~16:58).

**2026-06-03 16:25 -04:00** — Operator: **many sailboats moored in Little Harbour, need to be careful.** Dense mooring field — highest-attention environment of the day. Notes given: perception sees the hulls well (solid persistent targets at obstacle_prob_min 0.7 / 50 m) but small/low hazards (mooring balls, pennants, painters, dinghies) won't reliably register → visual lookout > costmap here. Under autonomy expect frequent collision_monitor stops (tight-quarters stop-polygon, as at pier) + busy avoider weaving between moorings; **manual recommended in the mooring field** (CA fallback-only). Standing by to check collision_monitor / stage avoider off if needed.

**2026-06-03 16:27 -04:00** — Operator: **1 kn trackline to the bridge** through Little Harbour. Slow (below the smooth 1.5 kn) — speed-driven weave shouldn't appear; good for the mooring field. Mooring-field cautions still apply (small/low hazards below costmap detection — visual lookout).

**2026-06-03 16:33 -04:00** — Operator: **tracking very good at 1 kn** through the mooring field toward the bridge. Confirms the speed-dependent tracking story at the low end — clean even in tight quarters at 1 kn. Reinforces wrap-up takeaway: smooth slow / weaves fast → speed-scaled controller gains (#164/#124).

**2026-06-03 16:34 -04:00** — **CONFOUND flagged by operator (corrects prior attribution):** the smooth 1 kn tracking is NOT cleanly attributable to speed — the slow legs also coincided with moving from the **exposed Gulf of Maine into the sheltered harbour** (flat calm). Open-water wave-driven pitch/roll/yaw disturbs the controller (and adds perception pitch-noise); sheltered water removes it. So today's data **confounds speed with sea-state** — both changed together. **Revised wrap-up takeaway: tracking improved with lower speed AND calmer water; the two are NOT separated by today's runs.** To isolate: controlled comparison (same speed exposed vs sheltered, e.g. 1 kn exposed vs 1 kn sheltered). Don't overclaim "speed-scaled gains" alone — sea-state is an unseparated factor (#164 cross-track characterization should control for sea state).

**2026-06-03 16:36 -04:00** — **FEATURE REQUEST (operator):** change trackline speed live **without resending the mission**. Current state: crabbing controller `FollowPath.default_speed` IS live-tunable via param service (`crabbing_path_follower.h` — "Live updates to default_speed via the node's parameter service"; desired_speed_ atomic, re-read live) → `ros2 param set /bizzy/controller_server FollowPath.default_speed <m/s>` works with no resend, BUT only when running on default_speed; CAMP per-line speeds override it, forcing a resend. Interim workaround: run missions WITHOUT per-line speeds, drive speed via live default_speed param. **Wrap-up/roadmap TODO:** (1) check how mission_manager/camp_interface passes per-line speed (overrides default_speed vs nav2 setSpeedLimit interface) to pick the hook; (2) add a live speed override/scale (param or CAMP control) that takes precedence over mission per-line speed — nav2 setSpeedLimit (already honored by crabbing controller) is a candidate hook. Add to docs/roadmap.md at wrap-up.

**2026-06-03 16:37 -04:00** — ✅ **nav#35 (mission re-send mid-line) VALIDATED on water** (class-significant). Operator changed speed and **resent the same trackline — it worked**, took effect without building a new mission. Confirms the mid-line re-send capability students will use to redirect mid-mission. Also a practical interim for live speed change (resend same line at new speed; lighter than a full new mission) pending the true resend-free override (16:36 feature request). Then operator **bumped speed to 1.5 kn** via this resend method (in sheltered Little Harbour; note the speed/sea-state confound from 16:34 still applies to any tracking-quality read here).

**2026-06-03 16:38 -04:00** — Operator **bumped to 2 kn** (resend method again). Natural mini-experiment on the confound: this is **2 kn in sheltered water** vs the earlier 2 kn on the exposed approach (16:17). If it tracks clean here but weaved at 2 kn outside → evidence pulling toward **sea-state** over speed as the dominant factor. Watching the comparison.

**2026-06-03 16:39 -04:00** — **Traffic: a boat overtaking BizzyBoat** in Little Harbour (operator-reported). Slow at 2 kn with steerage for it to pass. Captured in bd6iopdwa. No action needed.
