# 2026-06-10 — gabby log (BizzyBoat deployment a429b20)

Deployment issue: a429b20 — Deployment 2026-06-10: sensor validation (sidescan + M3)
Host: gabby
Side: field
Started: 2026-06-10 14:12 -04:00

> **Issue `## Logs`**: gabby log link not yet stamped under the deployment
> issue's `## Logs` section — field side is read-only on the issue. Stamp
> `- [gabby log](docs/logs/2026/2026-06-10_gabby_logs.md)` from dev next
> time `/start-deployment` runs there.

## 2026-06-10

**2026-06-10 14:12 -04:00** — Deployment mode activated on gabby (session start). Pre-session state: `make sync` + `make build` run, all 5 layers green; cube_bathymetry (issue-31) rebuilt with the new source. Sound-speed probe checked on `/dev/ttyS0` @ 9600 — streaming clean `$AML,SVM,0.000,...` (out of water, link good).

**2026-06-10 14:21 -04:00** — Sidescan controls check (node already up, launched with `debug_raw:=true` — `debug/raw`, `debug/raw_status`, `debug/raw_config` all present). Control interface healthy on the boat side: `state` (RadarControlSet) publishing all 4 controls (Status standby/transmit, Range 1–60 m, TVG, Interference); `change_state` topic + `set_transmit` service present. TX safely OFF at this point, SV interlock armed. SVS now reads valid in-water SV — `/bizzy/sensors/sound_speed/sound_speed` = **1496.9 m/s** (was `0.000`/dry earlier in session; boat now in water).

**2026-06-10 14:28 -04:00** — Operator reports sidescan **controls not actuating in rqt over udp_bridge** (the marine_tools#31 deliverable). Boat-side control interface is healthy; the gap is the topside→boat command path. *Observation for wrap-up RCA (deferred, not diagnosed live):* `bizzyboat.yaml` udp_bridge sender (~L250–270) and the bag/logger lists carry the sidescan **readback** (`state`/`status`) but I see no `change_state` anywhere in this config — the topside→boat control return path may be unconfigured here or live only in the topside bridge config. Verify at wrap-up.
  - **Mitigation:** started transmit directly on the boat via `set_transmit {data:true}` → `success=True, message='transmitting'`. Confirmed `tx=ON`, `transmitting=true`, pings port/stbd/down incrementing (85/85/86), `sonar_image_port` ~7.7 Hz (gcv20 decode path working). Pinging is live.

**2026-06-10 14:32 -04:00** — Set **Range = 60 m** on operator request, via boat-side `change_state` (`{key: range, value: '60'}`) since the rqt path is down. `state` readback confirms `range = 60.0`; TX still healthy (`tx=ON`, pings 1860/1858/1863, sv=1497.5). Note: the boat-side `change_state` command path **accepts commands fine** — reinforces that the broken link is specifically the udp_bridge topside→boat relay, not the node's control handling.

**2026-06-10 14:48 -04:00** — Operator reports **M3 sonar updates stopped**. Path check, stage by stage:
  - `kongsberg_em_bridge` up; `/bizzy/sensors/m3/detections` **@ ~10 Hz** ✓
  - `detections_to_pointcloud` `active`; `/bizzy/sensors/m3/soundings` **@ ~10 Hz** ✓
  - `cube_bathymetry` (m3) lifecycle `active`, **BUT `/bizzy/sensors/m3/grid` SILENT** (0 msgs in 7 s) ✗ ← the break.
  - **Prime suspect — issue-31 stale-build regression.** Running `cube_bathymetry_node` (PID 192008) started **13:57:25**, ~1 min *after* this session's **13:56:29** rebuild that installed the issue-31 grid.cpp/node.cpp changes (+132/−27). So the silent grid stage is running brand-new code from today's build. The issue-31 build also threw warnings in `publishGrid()` (signed/unsigned loop compare, unused `cell_counts`) — consistent with a grid-emit regression. C++ RCA deferred to wrap-up (not diagnosed live).
  - **Data NOT lost:** the sonar bag records raw `/bizzy/sensors/m3/detections`, so the grid is re-derivable offline. Only the *live* grid view is down.
  - launch.log carried no cube_bathymetry error lines (node logs not landing there); rosout not deep-read live.

**2026-06-10 15:02 -04:00** — Mitigation attempt #1: **lifecycle `deactivate`→`activate` cycle** on `/bizzy/sensors/m3/cube_bathymetry`. Both transitions "successful", state verified `active [3]→inactive [2]→active [3]`. **Grid still silent** (0 msgs in 9 s) while soundings continue @ 10 Hz. → Rules out a transient lifecycle stall; **confirms a code-level grid-emit regression in the issue-31 build** (node active + ingesting soundings but `publishGrid()` produces nothing). Reactivation can't fix code. Note: respawn won't help either — `cube_bathymetry_launch.py` has `respawn=True` but configure/activate is a one-shot `LifecycleTransition` (respawned node comes back *unconfigured*), and it'd respawn the same issue-31 binary. Remaining levers: (a) continue survey — raw `/m3/detections` still bagged, grid re-derivable offline, fix issue-31 at wrap-up; or (b) relaunch M3 group on pre-issue-31 cube_bathymetry build to restore live grid now. Awaiting operator decision.

**2026-06-10 15:12 -04:00** — **ROOT CAUSE (operator ground truth):** NOT a cube_bathymetry code regression / TF failure. It's a **udp_bridge issue — the CUBE grid grew too big to send over the bridge.** As the survey covers more area the `GridMap` message grows unbounded; past some size the bridge can't reliably carry it, so topside stops receiving grid updates. Explains "worked for a while then stopped" (size threshold crossed mid-run) better than the TF/regression hypotheses I chased earlier (my boat-local `topic hz` silence was misleading — a very large message publishing slowly reads as silence in a ~9 s window; the boat-side publisher was fine). My earlier TF "No transform bizzy/map <- bizzy/m3" warning was a **startup-only** transient (~13:57), not the live cause. **Action item (design, deferred — NOT a live code change):** scheme to avoid sending the full grid over the bridge (tiled/dirty-cell deltas, or a decimated/windowed operator-view grid, vs. full-res recorded on the boat). Possible secondary effect to check: a bridge saturated by the giant grid may also degrade other bridged topics (cameras/telemetry/sidescan controls). Track as follow-up issue on cube_bathymetry / udp_bridge.

**2026-06-10 15:16 -04:00** — Operator asked to **restart cube for a fresh empty grid** (live mitigation for the oversized-grid/bridge issue). SIGTERM'd m3 `cube_bathymetry_node` (PID 192008); `respawn=True` brought it back as PID **216290**, `unconfigured` (one-shot launch `LifecycleTransition` does not re-fire on respawn), then manual `configure`→`activate` (both successful, state `active`). Fresh empty grid; small again so it crosses the bridge until it regrows. (Note: `cleanup→configure` would NOT work as a no-kill reset — on_configure calls `declare_parameter` unconditionally and on_cleanup doesn't undeclare, so re-configure throws `ParameterAlreadyDeclared`. Process restart is the only reliable reset today.)
  - **No `clear_grid` service exists** — cube node has only lifecycle + parameter services. Discussed adding one (`std_srvs/srv/Trigger` "clear_grid" that rebuilds `map_sheet_` in-place; ~15 min code / ~30 with a test; field-mode commit to `jazzy` + rebuild + relaunch). Strictly better than process-restart for emptying the grid. Awaiting operator go/defer. Pairs with the decimated/windowed-downlink design item above.

**2026-06-10 15:29 -04:00** — Operator gave go-ahead; **built the `clear_grid` service.** `cube_bathymetry` commit **`64e907d`** on `jazzy` (field mode, agent identity): `std_srvs/srv/Trigger` "clear_grid" → rebuilds `map_sheet_` empty in-place + forces immediate republish; `cell_size`/`grid_cell_count` promoted to members; race-free under the SingleThreadedExecutor. Added unit test `test_map_sheet::ReconstructClearsAccumulatedData`. Single-package build green (20.3s, only pre-existing warnings); `test_map_sheet` 11/11 pass. **Not yet deployed to the running node** — loading the new binary needs one cube relaunch (kill→respawn→configure→activate, which clears the current grid). Awaiting operator timing. Not pushed to gitcloud yet (local commit on gabby).

**2026-06-10 15:39 -04:00** — **Deployed + verified `clear_grid` live.** Relaunched m3 cube (kill PID 216290 → respawn PID 222712 → configure → activate; `/proc/222712/exe` confirms it's the new 15:22 build binary, which `strings` shows contains `clear_grid`). Service call returned **`success=True, message='grid cleared'`**, node stayed `active`. **Service name is `/bizzy/sensors/m3/clear_grid`** (under the node namespace, like the `grid`/`soundings` topics — NOT `.../cube_bathymetry/clear_grid`; first calls to the wrong path timed out). Discovery gotcha: had to `ros2 daemon stop/start` to see the fresh service in `node info`/`service list` (stale CLI graph cache, on top of the session's general zenoh discovery lag). Usage: `ros2 service call /bizzy/sensors/m3/clear_grid std_srvs/srv/Trigger {}`. Still local-only on gabby (not pushed to gitcloud).

**2026-06-10 15:40 -04:00** — **Pushed `clear_grid` to gitcloud** (`git push origin jazzy`, `b83c818..64e907d`); origin/jazzy now matches local. Field-mode push, no PR — reconcile to GitHub later via `/import-field-changes` for review against the Quality Standard. Other field hosts will pick it up on their next pull. Still open: durable downlink fix (decimated/windowed grid) follow-up issue — `clear_grid` is a manual mitigation, grid still grows unbounded.

**2026-06-10 15:41 -04:00** — **ACTION ITEM (open as a GitHub issue from a dev host — no `gh` on gabby): durable CUBE-grid downlink scheme.** Root cause: the M3 `cube_bathymetry` `GridMap` (`/bizzy/sensors/m3/grid`) grows unbounded with survey extent and outgrows the udp_bridge, so topside stops getting grid updates ("worked then stopped" today). Today's mitigations are both manual/temporary: restart the node, or the new `clear_grid` service (commit `64e907d`). Need a scheme that bounds the per-message payload regardless of survey extent. Options discussed (in increasing effort): (1) **decimated + windowed operator-view grid** — separate downlink topic cropped to an N×N window around the vehicle and/or downsampled; full-res stays on the boat + bag *(recommended first step — simple, bounded, operator usually only needs situational awareness)*; (2) **tiled dirty-updates** — fixed tiles, send only changed tiles, topside reassembles + keyframe/resync *(full map at full res topside; real protocol work)*; (3) **sparse cell-delta + periodic keyframe** *(smallest bandwidth, most complexity)*; (4) **compression only** *(stopgap — only a constant factor, doesn't fix unbounded growth)*. Repos: `cube_bathymetry` (+ possibly `udp_bridge`). Also verify whether the oversized grid was starving other bridged topics (cameras/telemetry/sidescan controls). Pairs with the `clear_grid` work already landed.

**2026-06-10 15:47 -04:00** — Operator reports **sidescan range still auto-changing**. Looked into the `garmin_sidescan` driver: **not the driver.** `_controls['range']` is only ever written by our own commands (startup / `change_state`); no code path auto-adjusts it, nothing re-sends range. `state` topic confirms driver holds **`range: 60.0`** (our 14:32 set; sidescan node was not restarted). So the change is at the **GCV hardware — it's auto-ranging and overriding the fixed command**, and the operator sees the real imagery swath change while the driver still believes it's at 60. **Why the driver can't pin it:** `commands.py` has transmit/TVG/interference/range builders but **no auto-range-disable / force-manual command**; `build_range_cmd` only writes a range *value*. The `debug/raw_config` CDP stream (`239.254.2.11:51000`) exists precisely because it "carries the active range **under auto-range**" → auto-range is active on the Garmin marine network, almost certainly driven by a **chartplotter** sharing that network with the GCV. (raw_config `hz` read empty, but that's ambiguous under this session's zenoh discovery lag; not decoded anyway — see marine_tools#26.) **Immediate mitigation is chartplotter-side** (set fixed range / standby its sonar), not driver-side — pending operator confirmation of the physical setup. **Durable fix (deferred, dev-host issue):** reverse-engineer the GCV auto-range-disable / manual-mode command and have the driver assert it with the range; relates to marine_tools#26 (CDP range decode). Did NOT fire raw protocol guesses at the live GCV.

**2026-06-10 15:56 -04:00** — Operator asked to log raw sidescan. Standing `sonar_logger` records the **decoded** `sonar_image_*` + `state`/`status`/`raw_status`/`raw_config` but **NOT `debug/raw`** (the ~255 Hz raw GCV imagery). Found `debug_raw` param was **False** (so the standing logger's raw_status/raw_config were capturing nothing either — separate item: decide if debug_raw should be persistently true for the wet run). Started **20-min ad-hoc capture** via `bizzyboat_project11/scripts/record_sidescan_debug.sh 1200` (the purpose-built script — enables debug_raw, records all 11 driver topics incl. `debug/raw`, restores debug_raw=false on exit). Verified live: debug_raw→True, rosbag2 subscribed incl. `debug/raw`, bag growing ~1.2 MB/s (→ ~1.4 GB est.). Output: `~/data/logs/bizzy_sidescan/bag_2026-06-10T15.54.41_sidescan_raw/` (zstd mcap). Auto-stops at 20 min, restores debug_raw=false. Disk fine (1.7 T free).

**2026-06-10 15:58 -04:00** — Operator: **"went over Cod Rock and the sidescan auto-changed range"** (operator-reported, ~just now). Confirms the GCV auto-range behavior diagnosed at 15:47 — a depth change (passing over the Cod Rock shoal) drove an automatic range change, which is exactly what bottom-tracking auto-range does. **Captured in the ongoing 20-min raw bag** (`bag_2026-06-10T15.54.41_sidescan_raw`, recording 15:54–~16:14) on `debug/raw` + `debug/raw_config` — useful ground truth for the offline range/auto-range decode (marine_tools#26).

**2026-06-10 16:05 -04:00** — Operator: **went over Cod Rock again** (operator-reported). Second pass — also within the running 20-min raw capture (`bag_2026-06-10T15.54.41_sidescan_raw`, ends ~16:14), so a second auto-range transition over the same feature is on `debug/raw`/`debug/raw_config`.

**2026-06-10 16:20 -04:00** — **20-min raw sidescan capture complete** (clean exit, debug_raw restored to False). Bag `~/data/logs/bizzy_sidescan/bag_2026-06-10T15.54.41_sidescan_raw` — 159 MB zstd mcap, duration 1199.3 s, 276,034 msgs: **`debug/raw` 227,777** (~190 Hz), `debug/raw_config` 1,497 (incl. both Cod Rock auto-range transitions), `debug/raw_status` 215, `sonar_image_{down,port,starboard}` ~9,117 each. Good dataset for the marine_tools#26 range/auto-range decode.

**2026-06-10 16:20 -04:00** — **Boat recovered** (operator-reported). Phase → recovery. Expect SVS to go dry (`sv→0.000`/invalid); the sidescan sound-speed safety interlock (`sound_speed_safety_enabled`, `require_sv`) should auto-stop transmit when SV drops below valid. Holding off any hardware commands per recovery protocol — operator has ground truth.

**2026-06-10 16:24 -04:00** — Session wrap-up. All ad-hoc captures confirmed stopped (no `ros2 bag record` / recorder processes running). Post-recovery, sidescan/SVS topics no longer visible from this shell — consistent with the sensor stack being shut down (not investigated per recovery protocol; could also be this session's zenoh discovery lag). Field-log entries complete through recovery. Day's deferred action items recorded inline above (udp_bridge `change_state` control path not bridged; durable grid-downlink scheme; GCV auto-range-disable driver cmd + marine_tools#26 decode; debug_raw persistence decision; check whether oversized grid starved other bridged topics). Per convention, the `## Summary` / `## Lessons Learned` consolidation belongs in the dev log (dev-side wrap-up), not this per-host log.

**2026-06-10 14:41 -04:00** — Operator reports **artifacts still in the sidescan imagery**; checked whether latest code is applied. **It is.** marine_tools source = `5510d9d` (PR #31/issue-30), `HEAD..origin/jazzy` empty → nothing newer on gitcloud. Running node (PID 192269, started 13:57, after the 13:18 build) imports `decode.py` from its egg-link build copy, which `diff`s **byte-identical to source `5510d9d`**. That version already includes `03982ab` *"strip per-packet trailer + leading ping header (echo_layer)"* — the artifact-relevant decode fix. **Conclusion:** artifacts are NOT a stale-code problem; the strip fix is live. Remaining artifacts are a different/unaddressed source. *Limit:* "latest" = latest on gitcloud (field remote) only — can't verify against GitHub from gabby (no `gh`); a newer fix pushed to GitHub but not gitcloud wouldn't be here. Deferred to wrap-up / dev-host check. Artifact characterization (banding? range-dependent? per-channel?) not done live.
