# 2026-06-15 — gabby log (BizzyBoat deployment — issue TBD)

Deployment issue: TBD — no `deployment`-labeled issue exists yet for the 2026-06-15 Lake Massabesic survey. Field hosts have no GitHub access to create one.
Host: gabby
Side: field
Started: 2026-06-15 08:37 -04:00

> **Issue not yet created.** First day of Lake Massabesic survey ops; no
> deployment issue exists for it and field side cannot create issues. Create
> the deployment issue from a dev host (`/start-deployment` on deadpool, dev
> side), run `issue_sync.dev_push`, then `field_pull` here and stamp the issue
> ID into this header and add `- [gabby log](docs/logs/2026/2026-06-15_gabby_logs.md)`
> under the issue's `## Logs` section.
>
> **Site note:** Lake Massabesic is freshwater — no tidal/current signal.
> Pre-flight tides/currents are skipped for this deployment (dev-side only
> regardless).

## 2026-06-15

**2026-06-15 08:37 -04:00** — Deployment mode activated on gabby (session start) — first day of Lake Massabesic survey ops. No deployment issue yet (field side can't create one); issue creation deferred to a dev host. Urgency contract loaded for this session. Repo on `jazzy`, tree clean.

**2026-06-15 08:41 -04:00** — Operator applied an Ubuntu software update on gabby and is rebooting the machine. Session to be resumed after restart.

**2026-06-15 08:47 -04:00** — Back after reboot. `make sync` + `make build` run. Sync pulled updates to `sea_surface_segmentation` (segments_to_pointcloud projection changes + perception capability report), `marine_tools/garmin_sidescan` (decode/node/safety updates), and `unh_marine_perception` (docs/figures). Build: **all 5 layers green** (underlay 6, core 27, platforms 6, sensors 18, site 1). Only stderr was warnings (GDAL unused-result in cube_bathymetry, PCL CMP0144 CMake policy in sea_surface_segmentation) — no errors.

**2026-06-15 08:52 -04:00** — Operator starting the ROS stack on gabby.

**2026-06-15 09:06 -04:00** — Bring-up check. 127 nodes up. All 10 nav2 lifecycle nodes report `active [3]` (controller/planner/bt_task_navigator/behavior/smoother/velocity_smoother/waypoint_follower/docking/global_costmap/local_costmap) — clean lifecycle, no startup stall. `ca_safety` (default helm gate) + `oak_forward/segments_to_pointcloud_reflex` (its input pointcloud) both present. Notes: (a) `/bizzy/sensors/sbg_device` appears **twice** in node list — possible duplicate-name collision, to eyeball; (b) `/bizzy/sensors/deltat/*` nodes present but DeltaT/Imagenex 837 is known-off/not-installed on BizzyBoat — ignore. Liveness (topic hz) not yet checked — node presence ≠ data flow.

**2026-06-15 09:09 -04:00** — Operator reports "the sidescan driver seems to be struggling." On gabby: no `garmin_sidescan`/sidescan node in ROS graph, no matching process (`ps`), no sidescan/sonar topics. Sidescan driver is not running on gabby — consistent with the driver living on mercat (which proxies :50050 status/:51000 config to gabby). **Hypothesis to check:** `make sync`+`make build` ran on **gabby only** this session; `garmin_sidescan` (marine_tools) was updated in the sync. If the driver runs on mercat, mercat is on a pre-sync build → host version mismatch. Awaiting operator confirmation of where it runs + the symptom.

**2026-06-15 09:16 -04:00** — Symptom found: sidescan IS on gabby (`garmin_sidescan-21` launch proc) but **crashes at import** — `node.py:30 from marine_control_py import ControlServer` → `ModuleNotFoundError: No module named 'marine_control_py'`. Root cause: today's `make sync` advanced marine_tools `1d274c8→d2cf79a` (PR #39/#40), which added `<depend>marine_control_py</depend>` + the import. `marine_control_py` ships in a separate **`marine_control` repo that is NOT in this workspace** (no source, no built pkg, not importable; only referenced in `.agent/scripts/stage_rosdep_manifests.sh`). Pre-sync garmin_sidescan (1d274c8, known-good on 06-10) did not import it. `make build` was green because colcon doesn't catch unsatisfied runtime Python imports. marine_tools is clean at d2cf79a (no local work) → rollback of the 6 garmin_sidescan files to 1d274c8 is a safe, reversible on-water mitigation; adding the `marine_control` repo is the proper dev-side fix.

**2026-06-15 09:16 -04:00** — Operator: a fix is being pushed from dev soon. Holding the rollback; standing by to pull + rebuild once it lands. When it arrives: `make sync` (or targeted fetch) → rebuild garmin_sidescan (+ `marine_control` if that's how the fix delivers `marine_control_py`) → re-source → relaunch the sidescan node.

**2026-06-15 09:27 -04:00** — Fix located. Operator pushed it as a manifest update: platform-config commit `c72f923` "manifest: add marine_control to core layer" adds `marine_control` (`git@gitcloud:field/marine_control.git`, jazzy) to `config/repos/core.repos` → clones to `core_ws/src/marine_control`, provides the `marine_control_py` device lib (mirrors DEV unh_marine_autonomy #155). **Divergence wrinkle:** platform config repo is behind origin/jazzy by 20 (incl. this fix + unrelated work: reflex confidence floor, sidescan VPN bridging, logger garmin temp/depth) and ahead 4 with local gabby usb_camera/perception hotfixes — full pull mid-op would rewrite local history + drag 20 commits of unrelated change. Plan (minimal, additive): clone marine_control into core_ws/src → build it → rebuild garmin_sidescan → re-source + relaunch sidescan. Full config-repo reconcile (preserve the 4 local commits) deferred to a between-runs breakpoint. Awaiting operator go-ahead.

**2026-06-15 09:33 -04:00** — Steps 1–3 done (operator approved). (1) Cloned `field/marine_control` (jazzy) → `core_ws/src/marine_control` — 3 pkgs: `marine_control`, `marine_control_py`, `marine_control_interfaces`; `marine_control_py/__init__` exports `ControlServer`, `ControlServerOptions`. (2) Built all 3 in core layer (symlink-install), green. (3) Rebuilt `garmin_sidescan` (sensors), green. Verified: with `core_ws/install/setup.bash` sourced, `from marine_control_py import ControlServer` resolves. **Handoff for relaunch (step 4, operator):** the sidescan node must be relaunched in a shell that sources the updated core layer install so `marine_control_py` is on `PYTHONPATH` — i.e. re-source the workspace/layer setup before relaunch, else the import will still fail. Not yet committed; `core_ws/src/marine_control` is a fresh clone (untracked by the workspace repo), local `core.repos` still lacks the manifest entry (deferred reconcile).

**2026-06-15 09:35 -04:00** — Operator restarting the whole stack (picks up the new core install + rebuilt garmin_sidescan in a fresh env). Standing by to verify sidescan node comes up clean (no import crash) once back.

**2026-06-15 09:46 -04:00** — Stack restarted, verified post-restart. **Sidescan FIXED:** `/bizzy/sensors/sidescan/garmin_sidescan` up with live proc (PID 28437, no import crash), advertising full topic set incl. new `control/state` + `control/change` (marine_control ControlServer iface), `sonar_image_down/port/starboard`, `nadir_depth`. marine_control_py clone+build resolved the `ModuleNotFoundError`. Rest of stack healthy: all 10 nav2 lifecycle nodes `active [3]`; `ca_safety` + `oak_forward/segments_to_pointcloud_reflex` present; 128 nodes total. Standing item (pre-existing, not new, survived restart): `/bizzy/sensors/sbg_device` still appears **twice** — eyeball between runs. Sidescan data-flow (topic hz) not yet checked — node/topics present ≠ sonar imagery flowing.

**2026-06-15 10:02 -04:00** — Chart datum for Massabesic. Confirmed via source (`chart_datum_node.cpp`/`datum_config.hpp`) that the override datum is **not live-updatable**: `lake_datum` is read once at node construction, no `add_on_set_parameters_callback`, recalc loop uses the cached member → `ros2 param set` won't take effect; requires relaunch. Checked configs: `lake_datum` was **unset** (bizzyboat.yaml `/**/chart_datum:` only set frames; launch passes only geoid/vdatum/datum_config_path; no VDatum coverage inland) → chart_datum was absent at the lake. Operator provided lake datum = **52.3 m** (height rel. WGS84 ellipsoid, positive=above; consistent w/ inland NH lake vs coastal MLLW ~−28 m). Set `lake_datum: 52.3` in `bizzyboat.yaml` `/**/chart_datum:` block (left `lake_datum_mhhw` unset — tidal, N/A on freshwater). Install copy is a symlink (symlink-install) so no rebuild needed; value is live in install space. **Pending:** chart_datum must be relaunched to pick it up (params read at construction). Edit uncommitted (field-mode, config repo diverged — folds into the deferred reconcile).

**2026-06-15 10:04 -04:00** — Operator confirmed 52.3 is ellipsoidal height (matches node convention) and is restarting the stack to apply `lake_datum`. Standing by to verify chart_datum resolves via PARAM source + publishes `bizzy/chart_datum` TF.

**2026-06-15 10:08 -04:00** — Stack restarted, datum override VERIFIED. `chart_datum` up, `lake_datum` param = 52.3. Startup log: "lake_datum override set: chart_datum 52.300 m (rel. ellipsoid) — wins over VDatum and config everywhere". Live resolution: "Datum at (42.9906, -71.3930): chart_datum 52.300 m [source: param]" — coords = Lake Massabesic, resolving at boat position. Rest of stack healthy post-restart: sidescan node back, all 10 nav2 lifecycle nodes `active [3]`, 128 nodes. Chart datum now established for the survey.

**2026-06-15 10:27 -04:00** — Started sidescan pinging (operator request). Verified interface from source first: transmit is a marine_control bool control `transmit` (group sonar), commanded via `ControlValue{name: transmit, value: "true"}` on `…/control/change`; SV watchdog guards it. Pre-check: transmit was `false`, range default, sound_speed fresh **1495.088 m/s** (guard satisfied). Published transmit=true (5×@2Hz for reliable zenoh delivery). Confirmed: `control/state` transmit=`'true'`; `nadir_depth` publishing fresh returns (frame bizzy/garmin_sidescan_nadir) → pinging + receiving. Note: ad-hoc ros2 CLI needs `core_ws/install/setup.bash` sourced for `marine_control_interfaces` (built this session, not in parent shell env). `debug/raw_status` one-shot echo hung (no sample) — avoid; unrelated to sonar health.

**2026-06-15 10:30 -04:00** — Operator asked if sidescan streams go out udp_bridge. **No** — verified at runtime: `ros2 node info /bizzy/udp_bridge` has zero sidescan subscriptions. `sonar_image_port/starboard/down` subscriber = `sonar_logger` only (recorded to bag, not bridged); `control/state` 0 subs. Sidescan data is logged locally but NOT transmitted off-boat. Cause: the udp_bridge sidescan send-list config is the **#257 work** (dc7514f/aa4cc7b/f7e691a "bridge sidescan port/stbd + down channel over VPN"), which lives in the 20 incoming commits the running config is behind — current live config only has sidescan in the LOGGER list, not the bridge list. Enabling off-boat bridging = integrate #257 (the deferred reconcile) + relaunch udp_bridge. Awaiting operator decision.

**2026-06-15 10:45 -04:00** — Bridged sidescan over VPN at runtime (operator request) via `udp_bridge/remote_advertise` (verified from source this is the correct service — `remote_advertise`→local `addSubscriberConnection` sends our topic out; `remote_subscribe` is the wrong direction). Confirmed remote `operator` / connection `vpn` live (salmon.vpn.bizzy.p11.lan, cap 1 MB/s). Advertised 5 topics (remote=operator, connection_id=vpn, destination empty→same name), mirroring the origin/#257 vpn spec: sonar_image_port/starboard/down @ q50 period 0 (full rate); nadir_depth @ q10 period 1.0 (1 Hz, per #257 design); water_temperature @ q10 period 0. Verified: udp_bridge now a subscriber on all 5 (imagery subs=2 incl. logger, depth/temp subs=1). **Caveats:** (a) runtime-only — NOT in running config, so a udp_bridge relaunch wipes these; durable version = integrate #257 config (deferred reconcile); (b) vpn link already near its 1 MB/s cap with resend drops before this — watch throughput.
